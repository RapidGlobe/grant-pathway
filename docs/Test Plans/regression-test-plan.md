# Grant Pathway — Regression Test Plan

**Version:** 2.27
**Date:** 2026-06-15
**Last updated:** 2026-08-20 (✅ **`RT-14` passes on production with row-level proof — 21 rows across seven tables went to 0.** Before: `auth.users` 1, `application_items` 5, `application_guidelines` 1, `applications` 1, `charity_profiles` 1, `ai_usage_log` 11, `user_profiles` 1. After: all seven **0**, and the old credentials are refused. **`ai_usage_log` = 11 matched the dashboard's "11 of 50 AI requests used" exactly**, which is what confirmed the snapshot was reading the right account on the right project. The confirmation email arrived (`FR-44`). ✅ **The 2026-07-23 `service_role` grant defect is confirmed fixed on production** — checked directly before the attempt, and `application_items`, the table that broke the cascade then and carried the largest count now, deleted cleanly. **RT-14's notes had recorded that migration as unapplied to production since 2026-07-23.** ⚠️ **One real defect found and fixed: contradictory banners on the sign-in page (`D-022`).** After deletion the page showed **"Your account has been deleted"** and, once a sign-in was attempted with the deleted credentials, **"Your email address or password is incorrect"** — both at once, a green success notice stacked directly above a red error. **Cause:** both status banners are driven by URL query parameters (`?deleted=true`, `?timeout=true`) and the form posts back to the same URL via `useActionState`, so the parameter survives the submission. **It was not specific to deletion** — `?timeout=true` behaved identically, so **`RT-15`'s inactivity message had the same fault**: one cause, two symptoms, and the second would never have been found by looking at RT-14 alone. **Fixed** in `components/sign-in-form.tsx`: both banners are suppressed once the form has an error to report, on the reasoning that a status message describes what happened _before_ the page loaded, and once the user has tried something and it failed, the failure is the only thing worth showing. Both paths verified locally — banner on load, error alone after a failed submission. **Found by WJ, who called it "not a huge problem but does need tidying up".** Also: **`RT-15` passes on production at FULL-LENGTH 55/60 timers — the first complete sequence ever observed, and it closes a question this case had left open in writing.** Modal at 55 with correct text, sign-out at 60, explanatory message on the sign-in page, and the application intact on return with its last-updated timestamp unchanged. **The 2026-07-28 close rested on 1–2 minute diagnostic timers**, and a genuine full-length run that day had shown **no modal at all** — recorded then as unconfirmed, with Chrome background-tab throttling suspected. It now demonstrably fires at 55 minutes. ⚠️ One boundary: step 7 was verified at dashboard level, not by re-reading the Step 4 answers field by field. Also: **`RT-12` passes on production** — password changed, and the new password then used to sign back in, which is the half a success message does not prove. Also: **`RT-16` PASSES on production — its first execution in any environment, and the reuse path's first positive test anywhere in the functional suite.** Guidelines and AI summary carried over with **no AI call at all**, proven three independent ways: zero `ai_usage_log` rows in the window, no `/api/generate-summary` request against the new application in Axiom, and no `[generate-summary]` log line — the last verified against a control query that returned yesterday's runs, so the empty result is evidence about the app and not about the query. Answer text matched the source application's exported Word document verbatim, citation included, and carried-over answers render badged **"Carried over — please review"** and still awaiting approval; the source application is untouched. ⚠️ **One usability observation, not a defect:** the funder name's **em dash** had to be pasted — a typed hyphen soft-missed the match, the offer silently did not appear, and the run proceeded to Step 2 as a fresh application. That is `getPreviousApplicationForFunder`'s documented trade-off working as designed, but the user-visible symptom is the feature appearing not to exist. See Document History v2.28. Also: **RT-00 through RT-11 and RT-13 now all pass on production — only RT-12 (change password) and RT-14/RT-15/RT-16 remain, and RT-04 carries a deferred caveat (`D-021`).** Also: **RT-05 passes on production, recorded on `ABC-08`'s evidence — the first execution of the refine path on production, and the `ai_request_type` `D-020` would genuinely have broken.** Also: ⚠️ **RT-04 is Pass (caveat) on production — `D-021` deferred to post-launch by WJ.** Project start and end dates render as prose textareas with word counters and AI assist; extraction was obeying the prompt, nothing is lost or misrepresented, and the correct fix needs a schema change the supporting-document work also needs. Also: **RT-03 now passes on production too — unblocked by the two flagship runs, and read against real varied data including an Ineligible application.** Also: **`P5.5` restarted and RT-00, RT-01a and RT-01b all pass against production — the first clean runs of the smoke tests anywhere but dev.** Also: **RT-00 strengthened — it now runs the parity audit and one real AI call, because the version that passed on 2026-08-18 did so over both `D-020` and `D-018`. Its Pass should not be carried forward.** Also: **D-020 has a second instance** — found by the environment parity audit, repaired, re-run clean. **D-017 CLOSED as not a defect** — Sentry was working all along and the "no events in 14 days" reading was wrong. See Document History v2.18 and v2.19)
**Status:** RT-00 through RT-15 all Pass — **RT-09's caveat is now closed.** It passed on 2026-07-28 over a live defect — the Word export discarded every line break in an answer (`GAP-41`, Defect Log D-015), found 2026-08-06 in a real user's downloaded document, not by this plan. `GAP-41` was fixed the same day, with automated coverage that generates a real `.docx` and counts `<w:br/>`. **2026-08-14: WJ ran the still-outstanding manual re-run** — a genuinely multi-line, multi-paragraph answer opened cleanly in Word with paragraph breaks, blank lines and bulleted layout all intact. RT-09 is now a clean Pass, D-015 closed. RT-03 and RT-11's caveats are stale plan text, corrected; RT-14 confirms the 2026-07-23 `service_role` grant fix holds on `grant-pathway-dev`, `grant-pathway-prod` still unpatched; RT-15 closed after fixing two real defects found via diagnostic re-testing — see Defect Log D-013/D-014.
**Tester:** WJ
**Test account:** grantpathway+idle100@gmail.com

> ⚠️ **Running this against production? Read [`RUNNING-AGAINST-PRODUCTION.md`](RUNNING-AGAINST-PRODUCTION.md) first.** This plan was written against `grant-pathway-dev` and any account it names below is a **dev** account. Production uses one shared account, its own URL, and a different result-recording convention — all held in that document, which **wins** wherever this plan disagrees with it.

---

## ⚠️ Read this before running v1.1 for the first time

On 2026-07-01, a full audit found that `grant-pathway-dev` and `grant-pathway-prod` had been silently missing critical schema for weeks: the AI usage-cap RPC functions (`reserve_ai_slot` and friends — used by **every** AI call) were absent from **both** projects, and the transactional `approve_application`/`reopen_application` RPCs were absent from **prod**. This has now been fixed and verified on both projects (see `docs/Implementation Plan/CHANGELOG.md`, 2026-07-01 entries) — but it means:

- **Every "passed" result recorded against any funder test plan in this folder predates the fix** (all funder testing finished by 2026-06-17; the AI-cap RPC dependency was only introduced into the route code on 2026-06-22, and the approve/reopen RPC on 2026-06-29). None of those passes are evidence that the _current_ codebase works end-to-end.
- **This regression plan had zero recorded executions until 2026-07-03**, when RT-00 was run and passed against `grant-pathway-dev`. RT-15 was confirmed live 2026-07-04; RT-01–14 remain unrun.
- RT-00 below is new — run it **first**, every time, before trusting any other result in this plan or any funder plan.

## Purpose

This plan is a **regression test** — not a funder UAT. Its purpose is to answer one question after every dependency update, code change, or deployment:

> Has anything that previously worked stopped working?

It does not test whether AI output is accurate or funder-specific question extraction is correct. Those are covered by the funder test plans in this folder.

**Run this plan:**

- After merging dependency update PRs
- After any change to the auth, session, or middleware layer
- After any change to the AI API routes
- Before deploying a new release to production
- After any infrastructure change (Supabase, Vercel, Node version)
- **Before relying on any historical funder test result** — if the schema has drifted from tracked migrations before, it can happen again; RT-00 catches it in under a minute

---

## Test Tiers

This plan has three tiers:

| Tier | Name              | When to run                                             | Time    |
| ---- | ----------------- | ------------------------------------------------------- | ------- |
| 0    | Environment check | First, every session — before trusting any other result | ~2 min  |
| 1    | Smoke             | Every dependency update / quick confidence check        | ~10 min |
| 2    | Full regression   | Before any production deployment                        | ~30 min |

Tier 0 is marked **[ENV]** and always runs first. Tier 1 tests are marked **[SMOKE]**. Run all Tier 1 tests first; proceed to Tier 2 only if all pass.

---

## Test Data

This plan uses a pre-seeded test account with an existing in-progress application. No new application creation is required for Tier 1.

**Using a freshly registered account instead (e.g. to verify full functionality end-to-end rather than reuse prior state):** run **RT-01a** (below) first, immediately after RT-00 and before RT-01. A fresh account has no charity profile and no pre-seeded application, so RT-01a covers registration, email verification, and charity profile setup, then hands off to Appendix A to create a new application before continuing to RT-04 onward.

| Item                      | Value                                                                                                                                                                                                                                                                                                                            |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test URL                  | https://grant-pathway-three.vercel.app                                                                                                                                                                                                                                                                                           |
| Test URL Supabase project | `grant-pathway-dev` — confirmed 2026-07-01 via the `sb-stanwaejdvlvremtffkf-auth-token` session cookie (DevTools → Application → Cookies while signed in). Nothing currently points to `grant-pathway-prod` — it has no live consumer yet. Re-check the same way if this ever changes (e.g. after the P5.6 DNS/go-live cutover). |
| Test account              | grantpathway+idle100@gmail.com                                                                                                                                                                                                                                                                                                   |
| Display name              | Testname                                                                                                                                                                                                                                                                                                                         |
| Pre-seeded application    | Henry Smith Foundation — "Test Application"                                                                                                                                                                                                                                                                                      |
| Application status        | In progress (Step 4, Q&A writing interface)                                                                                                                                                                                                                                                                                      |
| Funder                    | Henry Smith Foundation                                                                                                                                                                                                                                                                                                           |

**Pre-test check:** Confirm the test account still has the "Test Application" in "In progress" status at Step 4. If it has been deleted or exported, a new application will need to be created first (see Appendix A).

---

## Test Results Summary

| Test ID | Test Name                                             | Tier  | Result        | Date       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------- | ----------------------------------------------------- | ----- | ------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RT-00   | Environment and schema verification                   | Env   | Pass          | 2026-08-18 | **Re-run against `grant-pathway-prod` — the first RT case ever executed against production.** All 5 functions present; 32 migrations applied, latest `20260814000000`, matching the repo exactly. Previous run 2026-07-03 against `grant-pathway-dev`.                                                                                                                                                                               |
| RT-01a  | Account registration (fresh account only)             | Smoke |               |            |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-01b  | Charity Commission lookup — found and not-found paths | Smoke | Pass          | 2026-07-28 | Confirmed live as a byproduct of eligibility testing — see RT-01b notes.                                                                                                                                                                                                                                                                                                                                                             |
| RT-01   | Sign-in and session persistence                       | Smoke | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-02   | Unauthenticated redirect                              | Smoke | Pass          | 2026-08-18 | **Re-run against production 2026-08-18 — pass.**                                                                                                                                                                                                                                                                                                                                                                                     |
| RT-03   | Dashboard renders with data                           | Smoke | Pass (caveat) | 2026-07-28 | Plan text was stale (missing Ineligible counter) — see RT-03 notes.                                                                                                                                                                                                                                                                                                                                                                  |
| RT-04   | Step 4 Q&A interface loads                            | Smoke | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-05   | AI refine-answer endpoint                             | Smoke | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-06   | Answer approval and progress bar                      | Full  | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-07   | Preparation checklist gate                            | Full  | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-08   | Senior review screen                                  | Full  | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-09   | Final review, approval, and Word export               | Full  | Pass          | 2026-08-14 | Originally passed 2026-07-28 over a live defect — `GAP-41`/D-015. Fixed in code 2026-08-06, covered by a packed-`.docx` test. **Manual re-run completed 2026-08-14** — a genuinely multi-line answer opened cleanly in Word, layout intact. Clean Pass, D-015 closed.                                                                                                                                                                |
| RT-10   | Plain text export                                     | Full  | Pass          | 2026-07-28 | Line-break fidelity step added 2026-08-06 as the comparison case for RT-09; this path was never affected by `GAP-41`.                                                                                                                                                                                                                                                                                                                |
| RT-11   | Dashboard reopen application                          | Full  | Pass (caveat) | 2026-07-28 | Plan text was stale (said "View", app shows "Re-open") — see RT-11 notes.                                                                                                                                                                                                                                                                                                                                                            |
| RT-12   | Change password                                       | Full  | Pass          | 2026-08-20 | **Re-run against production 2026-08-20 — pass.** Password changed, success message shown, and **the new password used to sign back in**, which is the half a success message does not prove. Previous run 2026-07-28 against `grant-pathway-dev`.                                                                                                                                                                                    |
| RT-13   | Sign out                                              | Full  | Pass          | 2026-07-28 |                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RT-14   | Delete account                                        | Full  | Pass          | 2026-08-20 | **Re-run on production with a before/after DB snapshot — 21 rows across seven tables went to 0**, old credentials refused, confirmation email received. The 2026-07-23 `service_role` grant fix is confirmed present on production, closing an item the notes had carried since then. ⚠️ Found `D-022` — contradictory sign-in banners — fixed the same day.                                                                         |
| RT-15   | Session timeout (inactivity)                          | Full  | Pass          | 2026-08-20 | **Re-run on production at FULL-LENGTH 55/60 timers — the first complete sequence ever observed.** Modal at 55, sign-out at 60, explanatory message, work preserved. **The 55-minute modal had never been seen at real timers**: unobserved on 2026-07-04, absent entirely on 2026-07-28, and D-013/D-014's fixes confirmed only at 1–2 minute diagnostic values. Previously: closed 2026-07-28 on that diagnostic basis.             |
| RT-16   | Reuse a previous application                          | Full  | Pass          | 2026-08-20 | **First execution in any environment, and the reuse path's first positive test in the functional suite.** Reuse offered, accepted, guidelines and summary carried over with **no AI call** — proven three ways (see notes). Answer text verbatim against the source's exported `.docx`; carried-over answers badged and unapproved; source untouched. ⚠️ Em-dash funder name had to be pasted — a typed hyphen soft-misses silently. |
| RT-17   | Two users using the service at the same time          | Full  | Pass          | 2026-08-20 | **First test of this service with more than one user on it.** Two browsers, two accounts, two different funders fired together: no cross-contamination, fair-use counts per user (reservations 1.7s apart), concurrent writing and sign-out clean. ⚠️ One 70s duration, **not** concurrency — a second instance of `GAP-115`'s wasted confirmation call.                                                                             |

---

## Tier 0 — Environment Check

Run this before every session, every time, no exceptions. It exists because on 2026-07-01 the hosted `grant-pathway-dev` and `grant-pathway-prod` Supabase projects were found to be missing schema that every other check (CI, local dev, prior test sessions) had no way of detecting — the gap was invisible from inside the app until someone deliberately checked the database directly.

---

### RT-00 — Environment and Schema Verification [ENV]

**What this tests:** That the environment being tested has the full, current schema applied, **that a signed-in user can actually use it, and that its credentials work** — not just that the app loads.

⚠️ **Strengthened 2026-08-19, because the version of this case that passed on 2026-08-18 was not worth much.** It passed that morning while **five tables were unusable by every signed-in user** (`D-020`) and while **no AI feature worked at all** (`D-018`). Both were found later the same day by ordinary testing. **It compared the _set_ of applied migration versions, and a version row proves bookkeeping, not execution** — twice now, a migration has been recorded as applied on production while its statements plainly did not run. Steps 5 and 6 exist to close exactly that gap: one checks what the database will actually permit, the other checks that a credential works rather than that it is present.

**Steps:**

1. Confirm which Supabase project backs the test URL. Sign in at the test URL, open DevTools → **Application** tab → **Storage** → **Cookies**, and find the cookie named `sb-<project-ref>-auth-token`. The ref tells you dev (`stanwaejdvlvremtffkf`) or prod (`mvmjryipieepvsjudche`). (Checking Vercel's env vars directly doesn't reliably work — sensitive-marked variables can't be revealed in the dashboard once saved, only overwritten. The cookie is visible regardless, since most Supabase calls in this app happen server-side and never appear in the Network tab.)
2. Run this against **that** project's SQL Editor:
   ```sql
   select proname from pg_proc where proname in
     ('reserve_ai_slot', 'update_ai_slot_token_count', 'cancel_ai_slot',
      'approve_application', 'reopen_application');
   ```
3. Confirm all 5 rows are returned.
4. In a terminal, change into the repository root — the directory containing `supabase/` and `package.json` (path corrected 2026-07-30: this step previously hardcoded the author's full local path, which `AGENTS.md` §4 forbids in documentation because it is machine-specific and will differ for any other contributor). The CLI needs to be run from inside the project directory to find the linked project config, then run `supabase migration list --linked` (after linking the CLI to the correct project) and confirm every row shows a matching Local/Remote timestamp — no blanks.

   **Acceptable substitution when testing production**, used on 2026-08-18 and recorded here so it need not be re-justified: run `select count(*), max(version) from supabase_migrations.schema_migrations` in the SQL Editor and check both against the contents of `supabase/migrations/`. Same assurance, and it avoids pulling the production database password onto the machine.

5. **Run the environment parity audit.** Open `scripts/parity/snapshot.sql`, paste it into **each** project's SQL Editor in turn, and save the single JSON value each returns as `scripts/parity/dev.json` and `scripts/parity/prod.json` (both gitignored). Then, from the repository root:

   ```bash
   npm run parity
   ```

   Confirm it exits 0, or that every **BLOCKER** finding is understood and either repaired or accepted in writing. This compares table grants, tables and RLS flags, functions and their ACLs, policies, columns, enums, storage policies and buckets, constraints, triggers and indexes.

   ⚠️ **This is the step that would have caught `D-020`**, and it found a second unexecuted migration on 2026-08-19. **It replaces the standalone `authenticated` grant check** that this case was previously going to grow — the script covers that and considerably more.

   ⚠️ **Two limits, so a clean run is not over-read.** It is a **comparison**, so it can only find something missing from one project that the other has — a migration unexecuted on **both** is invisible to it, and it says nothing about a project's absolute correctness. And it sees the database only: **credentials, Supabase Auth settings and Vercel configuration are all outside it.** Steps 2–4 remain the standalone check on whichever project is under test.

6. **Make one real AI call** (`DEPLOYMENT-CHECKLIST.md` v1.6). Sign in at the test URL, go to **/profile**, and run the charity lookup for registration number **1194917**. Confirm **"What does your charity do?"** and **"Who does your charity help?"** are populated and that the AI-provenance notice appears.

   ⚠️ **This is the step that would have caught `D-018`**, where production's `AWS_SECRET_ACCESS_KEY` did not match its access key ID and every Bedrock call returned a 403. **Four separate checks said "configured" and none said "working"** — the variables were present, correctly scoped, and the access key ID was genuinely valid. **Presence is not validity, and only a real call distinguishes them.** It is also the cheapest AI call in the product.

**Expected result:**

- All 5 functions present
- No blank rows in `migration list` (or a matching count and latest version)
- `npm run parity` exits 0, or every BLOCKER finding is repaired or accepted in writing
- The charity lookup returns both AI-written descriptions

**If this fails:** stop. Do not proceed to RT-01 or any funder test plan — every AI feature and the approve/reopen flow will fail, and the failure will look like a product bug rather than a missing-migration issue. Fix the schema gap first (see `docs/Implementation Plan/CHANGELOG.md`, 2026-07-01, for the exact remediation SQL if this recurs).

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes (record which project — dev or prod — this session tested against):** Confirmed via the `sb-stanwaejdvlvremtffkf-auth-token` cookie — `grant-pathway-dev`. Run 2026-07-03 ahead of MKCF Oak Grants testing.

**Notes — 2026-08-18, `grant-pathway-prod` (`mvmjryipieepvsjudche`):** ✅ **Pass.** All five functions returned — `approve_application`, `cancel_ai_slot`, `reopen_application`, `reserve_ai_slot`, `update_ai_slot_token_count`. **Step 4 was run differently and the substitution is recorded rather than glossed:** instead of `supabase migration list`, which needs the CLI linked to prod and would have prompted for the database password, the applied set was counted directly — `select count(*), max(version) from supabase_migrations.schema_migrations` returned **32** and **`20260814000000`**, matching the 32 files in `supabase/migrations/` and the newest of them (`20260814000000_gap93_output_token_tracking.sql`). **Same assurance, no production secret pulled onto the machine.** ⚠️ **This proves the migration set matches; it does not prove no drift inside those migrations** — the scheduled Schema Drift Check covers that and passed 2026-08-18 at 07:37 UTC. **`P5.5` item 1 is therefore unblocked on the schema precondition.**

**Notes — 2026-08-19:** ⚠️ **That Pass is now known to have been hollow, through no fault of the tester.** Within hours of it, `D-020` (no DML privileges for `authenticated` on five tables) and `D-018` (every Bedrock call failing on a signature mismatch) were found by ordinary testing. **This case has been given steps 5 and 6 so that neither could pass unnoticed again**, and the parity run has since found a **second** migration recorded as applied on production whose statements never executed. **Re-run this case in full against production before `P5.5` restarts** — the 2026-08-18 Pass should not be carried forward.

**Notes — 2026-08-19 re-run, `grant-pathway-prod`:** ✅ **Pass, on the strengthened case.** **Step 5:** `npm run parity` against snapshots captured the same morning — **no structural differences, exit 0.** The single `NOTE` is the `ai_request_type` labels in a different storage order, which is consequence-free and does not block a run. **Step 6:** the `/profile` lookup of **1194917** returned both AI-written descriptions with the provenance notice — _"The descriptions below were drafted by AI from your Charity Commission entry"_. **This is the first Pass of `RT-00` that establishes a signed-in user can actually use the database and that the AI credentials work**, rather than that the migration bookkeeping matches. The charity profile save was also confirmed working, closing the last open thread from `D-020`.

---

## Tier 1 — Smoke Tests

---

### RT-01a — Account Registration and Email Verification [SMOKE]

**Applies only when testing with a freshly created account** — skip this and go straight to RT-01 if reusing the pre-seeded test account.

**User guide reference:** Sections 1–2 (Registering, Verifying Your Email)
**What this tests:** The registration and onboarding path a brand-new user goes through — account creation, email verification, and charity profile setup — none of which the pre-seeded account exercises, since it already has all of this done.

**Steps:**

1. Open https://grant-pathway-three.vercel.app in a fresh browser tab (or private/incognito window)
2. Click **Register** / **Create an account**
3. Enter a new test email (e.g. `grantpathway+<label>@gmail.com`), a compliant password (12+ characters, letters and digits), first name, and last name. Deliberately note whether you tick or leave unticked the feedback-consent checkbox — this is needed later for P5.5's `feedback_consent` verification.
4. Submit registration
5. Open the verification email and click the verification link. **Note:** the link expires after 1 hour (see `PRD-Grant-Pathway.md` Section 7, Screen 3 -- formerly `screen-requirements.md`, retired 2026-07-13 -- and `acceptance-criteria.md`, both corrected to say 1 hour on 2026-07-02, previously wrong at 24 hours) — the user guide (`grant-pathway-user-guide-*.docx`) still needs the same correction, not yet done. Don't leave this step for later in the session — it also passes through the `/verify-email/confirm` auto-confirm step (D-012).
6. Confirm the app shows a success state after verification
7. Sign in with the new account
8. Complete charity profile setup — either look up the charity by name/registration number, or enter the fields manually
9. Confirm you land on the dashboard showing the correct empty state (no applications yet)

**Expected result:**

- Registration succeeds and the verification email arrives promptly
- Verification link works within the 1-hour window
- Charity profile saves successfully
- Dashboard renders the correct empty state

**Note:** After this test, the account has no application yet. Use Appendix A to create one before continuing to RT-04 onward (which assume an application already exists at a specific step). RT-01 (sign-in) is effectively already covered by step 7 above — running it again with this account is optional, not required.

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes (record the test email used, and the feedback-consent choice made in step 3):** ✅ **Pass — `grant-pathway-prod`, 2026-08-19, no issues.** Account `grantpathway+Prodtest@gmail.com`. ⚠️ **This supersedes the part-run of 2026-08-18, which is void** — it ran under an environment in which `authenticated` had no DML privileges (`D-020`) and every Bedrock call failed (`D-018`), so nothing it appeared to show meant anything. **Run after `RT-00` passed on its strengthened case**, and the charity profile save — the step that failed on 2026-08-18 — now succeeds.

**Notes (original, pre-2026-08-19):**

---

### RT-01b — Charity Commission Lookup — Found and Not-Found Paths [SMOKE]

**Prerequisite:** A signed-in account with no completed charity profile yet (or use the Charity Profile page's edit/re-lookup entry point on an existing account).

**What this tests:** The Charity Commission registration-number lookup used in charity profile setup — both the found path (pre-fills fields) and the not-found path (falls back to manual entry). Neither path has had a dedicated regression case before; RT-01a exercises the found path only incidentally, as one option among two ("look up... or enter manually") without confirming the not-found path actually works.

**Steps:**

1. On the Charity Profile page, enter a real, currently-registered charity number (e.g. **1194917**, Harry's Rainbow — used throughout this suite's flagship plans) and click **Look up charity**
2. Confirm **What does the charity do** and **Who does your charity help** populate automatically from the Charity Commission record
3. Clear the form (or start a fresh profile) and enter an invalid/non-existent registration number (e.g. **0000001**) and click **Look up charity**
4. Confirm the app handles the not-found case gracefully — no crash, a clear message that the charity wasn't found, and the manual-entry fields remain available and enterable
5. Complete the manual fields and click **Save profile**

**Expected result:**

- Found path: fields pre-fill correctly from the Charity Commission record
- Not-found path: a clear, non-alarming message is shown; no error page or unhandled exception; manual entry remains fully usable
- Both paths result in a profile that saves successfully

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, `grant-pathway-prod`:** ✅ **Pass, no issues.** ⚠️ **This is the case whose failure on 2026-08-18 uncovered `D-016`, `D-017` and `D-018`** — the charity lookup returned a name and two empty description fields, and an hour of elimination traced it to a mismatched AWS secret. **It now returns both AI-written descriptions with the provenance notice**, on the same lookup (1194917). **First clean run of this case against production.**

**Notes:** Confirmed live 2026-07-28 as a byproduct of `eligibility-check-test-plan.md`'s EL-01/EL-03 charity-profile setup, rather than a dedicated standalone run. Both paths exercised: a valid registration number pre-filled the charity profile fields correctly, and an invalid/non-existent number fell back gracefully to manual entry with no crash. No issues found.

---

### RT-01 — Sign-in and Session Persistence [SMOKE]

**User guide reference:** Section 1 (Signing In)
**What this tests:** `@supabase/ssr` cookie/session layer, auth middleware, protected route access

**Steps:**

1. Open https://grant-pathway-three.vercel.app in a fresh browser tab (or private/incognito window)
2. Confirm the sign-in page loads with email and password fields
3. Enter email `grantpathway+idle100@gmail.com` and the correct password
4. Click **Sign in**

**Expected result:**

- Sign-in page loads without console errors
- Login succeeds — user is redirected to the dashboard
- Dashboard shows "My Applications" heading and the user's name ("Testname") in the nav bar
- No error message displayed

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-02 — Unauthenticated Redirect [SMOKE]

**User guide reference:** Implied — unauthenticated users cannot access protected routes
**What this tests:** Auth middleware correctly protects routes; session cookie required

**Steps:**

1. Open a new private/incognito window (no active session)
2. Navigate directly to https://grant-pathway-three.vercel.app/dashboard

**Expected result:**

- User is redirected to the sign-in page (/)
- Dashboard content is not shown
- No error page displayed

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes:** Live-tested 2026-07-28, no issues.

**Notes — 2026-08-19, production:** ✅ **Pass, re-run on the restarted `P5.5`, no issues.** The 2026-08-18 Pass below stands and is consistent with it; both are recorded because the earlier run took place under `D-020` and `D-018`, and route protection is one of the few things those defects could not have affected.

**Notes — 2026-08-18, production:** ✅ **Pass on `grant-pathway-three.vercel.app`.** `/dashboard` requested with no session returned the sign-in page at `/`; no dashboard content rendered and no error page. Read from the accessibility tree rather than a screenshot, so the assertion is about what the page actually contained. **Two things observed in passing and worth recording:** the footer read **`v2026.08.18-144b9ae`**, the current `master` commit, confirming **GitHub → Vercel auto-deploy is working again after the 2026-08-17 outage** and that production is not stale; and `/privacy` renders **v1.8, effective 18 August 2026**, so today's policy publication is live.

---

### RT-03 — Dashboard Renders with Data [SMOKE]

**User guide reference:** Section 2 (Your Dashboard)
**What this tests:** Supabase data queries, dashboard page rendering, application status labels, AI usage counter

**Prerequisite:** RT-01 complete (signed in)

**Steps:**

1. On the dashboard, verify the following elements are present:
   - Summary bar showing application counts (Not started / In progress / Approved / Exported / Ineligible — the Ineligible count was added with `DR-EL-001`'s eligibility hard-stop and is missing from this step's original wording, corrected 2026-07-28)
   - AI requests counter ("X of 50 AI requests used this month")
   - At least one application card showing funder name, grant name, status, and last updated date
   - "+ New Application" button
2. Confirm the "Test Application" (Henry Smith Foundation) shows status **In progress**
3. Confirm the application card shows a **Continue** button (not just View — user guide v1.14 says "View" but the app shows "Continue"; record which is shown)

**Expected result:**

- All dashboard elements render correctly
- Application data loaded from database (not empty/loading state)
- Status labels match Section 16 of the user guide (Not started / In progress / Approved / Exported)

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production: ✅ Pass.** Run once the state it needs existed. **The dashboard was read against genuinely varied real data**, which is more than the dev runs had: two completed applications from the flagships (A B Charitable Trust and MK Community Foundation) **and one Ineligible** from the Walton Charity eligibility run the same afternoon — so the status labels and the summary bar's counts, including the `DR-EL-001` "Ineligible" count the dev run could only note as missing plan text, were all exercised on production against applications this account actually created. ⚠️ **The order was forced and is worth recording:** this case could not run until a flagship had created a completed application, so **the suite's own sequence does not hold on a fresh environment.** RT-03 sits at position three in a plan whose prerequisites are not produced until the flagships run. **Superseded block below, kept for the trail:**

**Notes — 2026-08-19, production: ⚠️ BLOCKED, and the block is expected rather than a defect.** This case reads a dashboard carrying at least one completed application, and **no account on `grant-pathway-prod` has ever completed one** — production had never been usable by a signed-in user until 2026-08-19 (`D-020`). **Deferred until a flagship end-to-end plan has been run against production**, which creates exactly that state; this case is then re-run against real data rather than against an empty dashboard. **The same applies to any later case in this suite that assumes existing applications.** ⚠️ **A related gap in this plan, noted 2026-08-19:** the test account named in the header is a **dev** account, and production testing uses `grantpathway+Prodtest@gmail.com`. Cases that name a specific user cannot be run verbatim against production. **The header should carry both accounts**, which is a plan fix owed rather than a test result.

**Notes:** Live-tested 2026-07-28 — dashboard rendered correctly, all real data. Plan text was out of date: the summary bar now also shows an "Ineligible" count (`DR-EL-001`), not listed in step 1's original wording — corrected above. Button label was "Continue" as expected.

---

### RT-04 — Step 4 Q&A Interface Loads [SMOKE]

**User guide reference:** Section 7 (Write Your Answers)
**What this tests:** Dynamic route rendering for `[id]/step/4`, Supabase question data fetch, Step 4 server component

**Prerequisite:** RT-03 complete

**Steps:**

1. Click **Continue** (or View) on the "Test Application" (Henry Smith Foundation — In progress)
2. Confirm Step 4 loads — the Q&A writing interface

**Verify the following elements are present:**

- Application progress navigation (steps 1–5, Step 4 highlighted as current)
- Funder name and grant name displayed at the top
- At least one question card with a text box
- Word or character counter beneath each text box
- **Help me improve this** button on at least one non-budget question card
- **Approve this answer** button on each card
- Progress bar showing "X of Y sections approved"

**Expected result:**

- Step 4 loads within 5 seconds
- Question cards rendered with content from the database
- No "Page not found" or error state
- No console errors

**Result:** ☑ Pass (caveat) &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production: ⚠️ Pass (caveat) — `D-021` deferred.** **Recorded as FAIL when first run, and changed to Pass (caveat) the same day on WJ's decision, with the reasoning kept rather than the verdict quietly swapped:** the interface loads and every question is captured, saved and exported correctly. **`D-021` is cosmetic — an applicant types a date into a prose box and is offered an AI-assist button they do not need. Nothing is lost or misrepresented**, unlike the 400-words-minimum failure this same run ruled out. **Deferred to post-launch, and deliberately not fixed by suppressing date extraction**, which would re-open `GAP-39`/`GAP-40`. The correct fix needs a question-type field the extraction schema does not have — **the same contract change the supporting-document work needs (`docs/impact-assessment-supporting-document-questions.md`), so the two are cheaper together than separately.** Run against **The Radcliffe Trust** application form, a docx of six tables across sections A–F. ⚠️ **The fixture was not in the repository** — The Radcliffe Trust appeared nowhere in the docs set, having never been added to any list; it was copied into `docs/Grant Org Guidelines/` during the run. **The interface itself loaded correctly** — 15 cards rendered, citations present on every one, budget cards correctly amber with AI assist disabled, and the two tight word limits (200 and 40) shown. **What fails is what is on two of the cards:** `Project Start Date` and `Project End Date` are narrative textareas with word counters and AI-assist buttons. See `D-021`, which records that **extraction was obeying the prompt** and the fault is the control the question is rendered with. ✅ **Two things this run proves that the dev run could not.** The **400-words-minimum** instruction on `Project Description` was **not** turned into a cap — the failure mode that would have warned an applicant for writing what the funder asked for. And the **six budget "Description / Notes" fields were not over-extracted**, so the `MKCF` fix has not swung too far the other way. **Re-run owed once `D-021` is decided and fixed.**

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-05 — AI Refine-Answer Endpoint [SMOKE]

**User guide reference:** Section 7 — Getting AI Help
**What this tests:** `@anthropic-ai/sdk`, `/api/refine-answer` route, Bedrock connectivity, AI suggestion rendering, and — critically — the `reserve_ai_slot` RPC that every AI route calls before touching Bedrock. This is the single most important test in this plan: if the AI cap-check schema is missing on the environment under test, this is where it surfaces (as a generic error, not an obvious "missing function" message).

**Prerequisite:** RT-04 complete (Step 4 visible with question cards)

**Steps:**

1. Locate the first question card that has a **Help me improve this** button (not a budget section)
2. Confirm the text box contains at least a few words of existing answer text
3. If the text box is empty, type a short placeholder answer (e.g. "Our charity supports people in the local community.")
4. Click **Help me improve this**
5. Wait for the AI response (up to 30 seconds)

**Expected result:**

- While processing: button changes state (loading indicator or button disabled)
- After processing: a "Suggested improvement" panel appears below the text box
- The suggestion contains coherent text (not empty, not an error message)
- Two options are shown: **Use this improved version** and **Keep my original**
- No error toast or console error

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production: ✅ Pass, recorded on `ABC-08`'s evidence rather than by a standalone run (WJ's decision).** `ABC-08` ran on `grant-pathway-prod` the same day: a narrative answer written, **AI-assisted via "Help me improve this", and approved.** Same route, same `reserve_ai_slot` RPC, same environment, same day — **and the first time the refine path had ever executed on production.** ⚠️ **Why that matters more than a routine pass:** this case exists to catch a missing AI cap-check schema, which surfaces as a generic error rather than an obvious fault — and `refine_answer` is the **one** `ai_request_type` the `D-020` enum gap would genuinely have broken. Its success on production is evidence about that repair, not just about the button. ⚠️ **One check not independently confirmed:** that **both** options render — "Use this improved version" **and** "Keep my original". `ABC-08` records an approved AI-assisted answer, which requires the first; the second is inferred from the same component rendering both, not observed. **Recorded as inferred rather than claimed.** A standalone run would cost one AI call against the monthly 50 and was judged not worth it.

**Notes:** Live-tested 2026-07-28, no issues.

---

## Tier 2 — Full Regression Tests

Run these after all Tier 1 tests pass.

---

### RT-06 — Answer Approval and Progress Bar [FULL]

**User guide reference:** Section 7 — Approving Answers
**What this tests:** Approval server action, progress bar update, "Approve this answer" → "Approved" state transition, re-approval after edit

**Prerequisite:** RT-04 complete

**Steps:**

1. On Step 4, locate a question card with existing answer text
2. Click **Approve this answer**
3. Confirm the card shows an "Approved" state
4. Confirm the progress bar at the top increments (e.g. "1 of 5 sections approved")
5. Click inside the approved answer box and make a small edit (add a word)
6. Confirm the approval is removed (card returns to unapproved state, progress bar decrements)
7. Re-approve the answer

**Expected result:**

- Approval toggles correctly on click
- Progress bar updates immediately after each approval/unapproval
- Editing an approved answer removes the approval (as per user guide: "If you edit an approved answer, the approval is removed")
- **Ready to assemble** button remains inactive while any answer is unapproved

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — derived from the flagship runs, not re-run standalone.** `ABC-09` and `MKCF-08` both approved answers and assembled a draft on `grant-pathway-prod` the same day, which exercises the approval server action, the "Approve this answer" → "Approved" transition and the progress bar.

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-07 — Preparation Checklist Gate [FULL]

**User guide reference:** Section 6 (Before You Begin Writing)
**What this tests:** `draft_status = 'not_started'` gate renders checklist; "I have what I need" sets `draft_status = 'in_progress'`

**Note:** This test requires an application that has NOT yet passed the checklist gate (i.e. `draft_status = 'not_started'`). The pre-seeded "Test Application" is already at `in_progress` and will show the Q&A interface directly. Use the "New application" card on the dashboard (status: Not started) if it has reached Step 4, or create a new application via Appendix A up to the point of clicking Continue on Step 3.

**Steps:**

1. Navigate to an application that is at Step 4 for the first time (draft_status = not_started)
2. Confirm the preparation checklist screen appears (not the Q&A interface)
3. Verify the screen prompts the user to have financial information ready (per user guide Section 6)
4. Verify the note about involving a senior colleague (e.g. CEO, treasurer, or trustee) before the financial questions is shown
5. Click **I have what I need — start writing**
6. Confirm the Q&A interface (Step 4 main view) now loads

**Expected result:**

- Checklist gate shown on first visit
- Clicking the button transitions to the Q&A interface
- Q&A interface loads correctly on first visit with no "No questions found" fallback

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — derived from the flagship runs.** `ABC-05` passed the `draft_status = 'not_started'` gate on production: the checklist rendered and "I have what I need — start writing" advanced to `in_progress`.

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-08 — Senior Review Screen [FULL]

**User guide reference:** Section 8 (Prior Export)
**What this tests:** `draft_status = 'ready_to_assemble'` gate, senior review screen, "Yes — assemble my draft" action

**Prerequisite:** RT-06 complete (all answers approved on a test application); **Ready to assemble** button active

**Steps:**

1. Confirm **Ready to assemble** button is active (all answers approved)
2. Click **Ready to assemble**
3. Confirm the "Before we put it together" screen appears (Section 8 of user guide)
4. Confirm it prompts for senior review of financial/budget content
5. Click **Yes — assemble my draft**

**Expected result:**

- Senior review screen appears after clicking Ready to assemble
- Screen matches Section 8 description in user guide
- Clicking "Yes — assemble my draft" proceeds to the final review (Step 5 / Section 9)

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — derived from the flagship runs, and observed directly.** Both flagships passed through the `ready_to_assemble` gate and used "Yes — assemble my draft". **WJ screenshotted this screen on production the same day** while reviewing `PDR-UI-007`'s new supporting-documents block, so the render is evidenced, not only inferred.

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-09 — Final Review, Approval, and Word Export [FULL]

**User guide reference:** Section 9 (Review), Section 10 (Export)
**What this tests:** Step 5 review screen, confirmation checkboxes, the combined approve+download action, and the resulting Word document — clicking a download button calls `approveApplication()`, which runs the `approve_application` Postgres RPC, then downloads immediately. This RPC was missing from production until 2026-07-01; this test is the direct end-to-end check that it's present and working, and that the resulting export is correct.

**Note:** there is no separate "Approve" button or confirmation modal. This was deliberately removed on 2026-06-12 (see `CHANGELOG.md`) — the previous flow took 6 interactions (3 checkbox ticks → Approve button → modal confirm → download click); three deliberate checkbox ticks already demonstrate intent, so the modal was judged redundant friction. Approval and download are now a single action, so this test covers approval and the Word export together rather than as two separate tests (as it briefly did earlier today) — RT-10 covers the second format (plain text) as its own test, since downloading it afterwards exercises a genuinely distinct scenario (the re-export confirmation dialog, D-WF-04). See `AC-FR-33-01` through `AC-FR-33-03`.

**Prerequisite:** RT-08 complete

**Steps:**

1. Confirm the Step 5 review screen loads
2. Verify all approved answers are displayed for review
3. Confirm the download buttons are disabled before any checkboxes are ticked
4. Tick all three confirmation checkboxes
5. Confirm both download buttons become enabled
6. Click **Download as Word document**
7. Confirm the download begins immediately, with no intermediate modal
8. Confirm a persistent "Application approved" banner now replaces the checklist
9. Open the downloaded .docx file

**Verify the document contains:**

- Application title (grant name)
- Funder name
- Export date
- All approved answers in a readable, structured format
- **Line-break fidelity (added 2026-08-06, `GAP-41`).** At least one answer under test **must** be multi-line — blank lines between paragraphs, and a hyphen-bulleted list. Confirm the Word document reproduces the layout the applicant typed: paragraph breaks intact, blank lines intact, each bullet on its own line. **This step exists because a passing RT-09 did not catch `GAP-41`** — the check below reads "no corrupted or missing content", which single-paragraph test answers satisfy even when every line break in the document is being silently discarded. Do not substitute a one-paragraph answer here; that is the exact hole this closes.
- AI disclaimer text
- Footer reading "Prepared using Grant Pathway v[version number] — grantpathway.org.uk"
- A page number ("Page N of NN") in the footer, below the attribution line (added 2026-07-02 — see `PDR-DH-003`)
- No corrupted or missing content

**Expected result:**

- Review screen matches Section 9 of user guide
- Download buttons disabled until all three checkboxes are ticked
- Clicking download approves and exports in one action, no confirmation modal
- Application status on dashboard updates to **Exported**
- Document opens cleanly in Word or equivalent; content matches the approved answers entered in Step 4

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — derived from the flagship runs.** `ABC-10` and `MKCF-09` both completed the Step 5 confirmations and the combined approve-and-download action, producing a `.docx` that opened cleanly. **`MKCF-09` additionally confirmed multi-line fidelity**, which is what closed `ABC-10`'s caveat.

**Notes:** Live-tested 2026-07-28, no issues — **but that pass did not cover the line-break fidelity step, and must not be read as if it did.** `GAP-41` was live in the code on 2026-07-28 and this test passed straight over it, because the answers used were single paragraphs. The result stood for what it checked. `GAP-41` was fixed on 2026-08-06 — automated coverage generates a real `.docx`, unzips it and counts `<w:br/>`, and the old code was confirmed to produce zero.

**2026-08-14 — the outstanding manual re-run is complete, closing this caveat.** WJ ran this live as part of the Stony Stratford revised-form retest, with genuinely multi-line, multi-paragraph narrative answers (the form's own structure — need/identification, funding history, general activities — naturally produces this, not a contrived test string). Opened the downloaded `.docx` in Word: paragraph breaks, blank lines and bulleted-line layout all reproduced correctly, matching what was typed in Step 4. No further observations. RT-09 moves from **Pass (caveat)** to a clean **Pass** — the caveat this case has carried since 2026-07-28 is closed. See Defect Log D-015 (now closed).

---

### RT-10 — Plain Text Export [FULL]

**User guide reference:** Section 10 (Export)
**What this tests:** Export API route's `format=txt` path — a separate code path from the Word export, not previously covered by its own test. Also confirms the re-export confirmation dialog (D-WF-04) correctly appears for a second download of an already-exported application.

**Prerequisite:** RT-09 complete (application already approved and exported as Word)

**Steps:**

1. Click **Download as plain text**
2. Confirm the re-export confirmation dialog appears (expected — the application was already exported as Word in RT-09, not a defect)
3. Confirm through the dialog
4. Open the downloaded .txt file in a plain-text editor (not Word — confirm it is genuinely plain text, no formatting)

**Verify the file contains:**

- Application title (grant name), funder name, and export date as the opening lines
- AI disclaimer text
- All approved answers, each clearly separated (e.g. by a rule of dashes)
- Closing line reading "Prepared using Grant Pathway v[version number] — grantpathway.org.uk"
- **Line-break fidelity (added 2026-08-06, `GAP-41`).** Using the same multi-line answer as RT-09, confirm the `.txt` file preserves its blank lines and bulleted lines. This path builds its output by joining an array of lines, so it was **not** affected by `GAP-41` — the check is here to keep the two formats compared against each other, since a divergence between them is the fastest way to spot a regression in either.
- No page numbers (plain text has no concept of pages)
- No corrupted or missing content, no stray formatting characters

**Expected result:**

- Re-export confirmation dialog appears and can be confirmed
- .txt file downloads without error
- Content matches the approved answers entered in Step 4
- Content matches the Word export's content (same answers, same order), just without Word formatting

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — run by WJ and confirmed directly, 2026-08-19.** ⚠️ **Recorded separately from RT-06–09 because it is not derivable from the flagships:** they export **Word**, and `format=txt` is a distinct code path this case exists to cover.

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-11 — Dashboard Reopen Application [FULL]

**User guide reference:** Section 2 (Your Dashboard) — reopening an approved application
**What this tests:** The dashboard "Reopen" action, which calls `reopenApplication()` → the `reopen_application` Postgres RPC. Like RT-09, this RPC was missing from production until 2026-07-01. This is the only test in this plan that exercises the reopen path — RT-06's approve/unapprove cycle is a different code path (direct `is_approved` column update, not this RPC) and does not cover it.

**Prerequisite:** RT-09 complete (an approved application exists)

**Steps:**

1. From the dashboard, locate the application approved in RT-09
2. Click **Re-open** on that application's card (corrected 2026-07-28 — this button read "View" when this step was originally written; the app has since renamed it to "Re-open" for an approved/exported application) — it directly opens the re-open confirmation modal, with no intermediate navigation into the application first
3. Confirm the modal reads "Re-open application" with the warning that re-opening removes approval and answers will need re-reviewing
4. Click **Re-open** in the modal
5. Confirm the application returns to Step 4 with status **In progress**
6. Confirm all previously-approved answers now show as unapproved (per the "resets all approvals" behaviour)
7. Confirm `current_step` is 4 and the Q&A interface loads correctly, not a blank or error state

**Expected result:**

- Reopen action completes without error
- Application status changes from Approved back to In progress
- All answer approvals are reset
- Step 4 Q&A interface loads correctly on return

**Result:** ☑ Pass (caveat) &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — run by WJ and confirmed directly, 2026-08-19.** ⚠️ **Not derivable from the flagships:** "Back to editing" reaches the same screen without touching the `reopen_application` RPC, which is the thing this case tests — and which was itself missing from production until 2026-07-01.

**Notes:** Live-tested 2026-07-28 — reopen mechanism itself worked correctly, no issues. Plan text was out of date: step 2 said "Click View", but the card's button now reads "Re-open" for an approved/exported application (matches the already-documented `FR-17` rename) — corrected above.

---

### RT-12 — Change Password [FULL]

**User guide reference:** Section 15 (Managing Your Account) — Change your password
**What this tests:** The password-update flow on the Account settings screen.

**⚠️ Uses the shared regression test account — you must change the password back at the end of this test (step 5), or every subsequent regression session will fail to sign in with the documented test credentials.**

**Prerequisite:** RT-01 complete (signed in)

**Steps:**

1. Click the user icon in the top right corner of any page and select **Account settings**
2. Under **Change your password**, enter the current password, then a new password (at least 10 characters) in both **New password** and **Confirm new password**
3. Click **Update password**
4. Sign out and sign back in using the new password to confirm it took effect
5. Repeat steps 1–3 to change the password back to the original — do not skip this step

**Expected result:**

- Password updates without error
- Signing in with the new password succeeds
- Password is successfully reverted at the end, leaving the shared test account in its documented state

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes:** ✅ **Re-run against production 2026-08-20 — Pass.** "Your password has been updated." shown on the Account settings screen, and **WJ then signed back in with the new password**, which is the step that actually proves the change took — a success message is written by the same code path whether or not the credential was persisted. Previously: live-tested 2026-07-28 on `grant-pathway-dev`, no issues, password reverted per step 5.

---

### RT-13 — Sign Out [FULL]

**User guide reference:** Section 15 (Managing Your Account) — Sign out
**What this tests:** That signing out actually clears the session server-side, not just hides the UI.

**Prerequisite:** RT-01 complete (signed in)

**Steps:**

1. Click the user icon in the top right corner and select **Account settings**
2. Click **Sign out**
3. Confirm you land on the public sign-in page
4. Navigate directly to `/dashboard`
5. Confirm you are redirected back to the sign-in page, not shown dashboard content

**Expected result:**

- Sign out redirects to the sign-in page
- The session is genuinely cleared — direct navigation to a protected route redirects again, the same as RT-02's unauthenticated check, but exercised via the actual sign-out action rather than a fresh incognito window

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes — 2026-08-19, production:** ✅ **Pass — run by WJ and confirmed directly, 2026-08-19.** ⚠️ **Not derivable from the flagships:** this case tests that the session is cleared **server-side**, which no flagship step exercises.

**Notes:** Live-tested 2026-07-28, no issues.

---

### RT-14 — Delete Account [FULL]

**User guide reference:** Section 15 (Managing Your Account) — Deleting Your Account
**What this tests:** The account deletion flow — confirmation gating and that the account, profile, and applications are actually removed.

**⚠️ Never run this against the shared regression account (`grantpathway+idle100@gmail.com`) or any funder test account still in active use — deletion is permanent and irreversible. Register a disposable throwaway account for this test.**

**Prerequisite:** A disposable test account, registered and signed in (see RT-01a for the registration flow)

**Steps:**

1. Click the user icon in the top right corner and select **Account settings**
2. Click **Delete my account**
3. Confirm the warning screen lists what will be permanently deleted: charity profile, all grant applications and AI-generated content, account and login details
4. Confirm the **Permanently delete my account** button is disabled (or the action is blocked) until **DELETE** is typed in the confirmation box
5. Type **DELETE** in the confirmation box
6. Click **Permanently delete my account**
7. Confirm you are redirected away from the app (e.g. to the public sign-in page)
8. Attempt to sign in again with the deleted account's credentials and confirm it fails

**Expected result:**

- Confirmation requires typing DELETE before deletion can proceed
- Account, charity profile, and applications are permanently removed
- Signing back in with the deleted account's credentials fails
- Clicking **Cancel** on the confirmation screen (if tested instead) returns to Account settings without deleting anything

**Optional add-on — Storage deletion (`GAP-47`, added 2026-08-06).** Steps 1–8 cannot observe the Storage half of deletion, because the bucket is empty by the time you get there: `/api/upload/process` deletes each object seconds after extracting its text. To exercise it deliberately, you have to catch a file mid-flight:

1. Note the disposable account's user id (Supabase → Authentication → Users)
2. ⚠️ **This step named the wrong window and cannot be done by hand — corrected 2026-08-20 after two failed attempts on production.** It used to say "close the tab while the summary is still generating". **By then the object is already gone.** It exists only from the moment the upload lands until `/api/upload/process` finishes extracting the text — a couple of seconds — and the 20–60 second summary that follows runs with no file present. **A slower-summary fixture does not help**, which is the obvious wrong idea: the MKCF Oak Grants (44s) and Walton Charity (59.8s) documents are slow in the phase _after_ deletion. Extraction time scales with file size, so the largest fixture in the corpus (`clothworkers-open-grants-guidance-and-sample-forms.pdf`, 1.2 MB) is the best available shot and still closed faster than a human could react. **To exercise this deliberately you need an instrumented run** — a delay injected into extraction on dev, as `RT-15` did with shortened timers — and then the `RT-15` caveat applies: a result earned inside a diagnostic harness is evidence about the logic, not about the shipped path
3. In Supabase → Storage → `guidelines-temp`, confirm an object named `<user_id>_<timestamp>` is present. **Objects are flat at the bucket root — there are no per-user folders.** If nothing is there, extraction already finished and cleaned up; retry with a larger file
4. Immediately run steps 1–8 above to delete the account
5. Re-check the bucket: the `<user_id>_…` object should be gone

If it is still present, that is a real `GAP-47` regression — but check Sentry before filing it. The Storage step reports failures as a warning and deliberately does **not** fail the deletion, so a Storage error looks identical to a missing step from the UI. Note also that `cleanup-guidelines` runs every 30 minutes and would remove the object anyway, so this check is only meaningful **immediately** after deletion.

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes (2026-08-20, production — with row-level evidence):** ✅ **`RT-14` passes on production with row-level proof — 21 rows across seven tables went to 0.** Before: `auth.users` 1, `application_items` 5, `application_guidelines` 1, `applications` 1, `charity_profiles` 1, `ai_usage_log` 11, `user_profiles` 1. After: all seven **0**, and the old credentials are refused. **`ai_usage_log` = 11 matched the dashboard's "11 of 50 AI requests used" exactly**, which is what confirmed the snapshot was reading the right account on the right project. The confirmation email arrived (`FR-44`). ✅ **The 2026-07-23 `service_role` grant defect is confirmed fixed on production** — checked directly before the attempt, and `application_items`, the table that broke the cascade then and carried the largest count now, deleted cleanly. **RT-14's notes had recorded that migration as unapplied to production since 2026-07-23.**

**Two things the run covered that the plan does not ask for, both worth keeping.** **(1) The confirmation is genuinely case-sensitive:** lowercase `delete` and partial `DElte` were both refused, so the gate tests the exact string rather than merely a non-empty field. **(2) Step 4's first clause does not match the implementation, and the second does.** The button is **not** disabled before `DELETE` is typed — it is only disabled while a deletion is in flight (`disabled={isPending}`) — and the gate lives in the submit handler, which refuses and shows "Please type DELETE in capitals to confirm." **This is a pass under the step's "(or the action is blocked)" wording, and arguably the better pattern**, since a disabled button explains nothing. Recorded because it looked like a defect on sight and cost a code read to settle. ⚠️ **A related nit, not a defect:** the confirmation field uses `DELETE` as its **placeholder**, so at a glance it reads as already filled in — only the grey-versus-black text distinguishes them.

⚠️ **One real defect found and fixed: contradictory banners on the sign-in page (`D-022`).** After deletion the page showed **"Your account has been deleted"** and, once a sign-in was attempted with the deleted credentials, **"Your email address or password is incorrect"** — both at once, a green success notice stacked directly above a red error. **Cause:** both status banners are driven by URL query parameters (`?deleted=true`, `?timeout=true`) and the form posts back to the same URL via `useActionState`, so the parameter survives the submission. **It was not specific to deletion** — `?timeout=true` behaved identically, so **`RT-15`'s inactivity message had the same fault**: one cause, two symptoms, and the second would never have been found by looking at RT-14 alone. **Fixed** in `components/sign-in-form.tsx`: both banners are suppressed once the form has an error to report, on the reasoning that a status message describes what happened _before_ the page loaded, and once the user has tried something and it failed, the failure is the only thing worth showing. Both paths verified locally — banner on load, error alone after a failed submission. **Found by WJ, who called it "not a huge problem but does need tidying up".**

⚠️ **ATTEMPTED AND NOT EXERCISED — 2026-08-20, production. Recorded as a limitation of the method, not as a pass or a fail.** Two attempts on a disposable account (`grantpathway+throwaway@gmail.com`, id `8cb6fae8`): the 1.2 MB Clothworkers PDF uploaded, the tab closed **as soon as the upload bar appeared**, and `storage.objects` queried directly for the user id prefix. **Zero rows before deletion and zero after** — so there was never an object to delete and nothing was tested.

**Why the add-on as written is impractical, which is the useful part.** The step says to interrupt while the summary is generating, and that is the wrong window. **The object exists only from the moment the upload lands until `/api/upload/process` finishes extracting its text** — a couple of seconds — and the summary generation that follows takes 20–60 seconds with the file already gone. **Choosing a fixture with a slower summary does not help**, which was the obvious next idea and is why it is written down: the MKCF Oak Grants document (44s) and Walton Charity (59.8s) are slow in the phase that happens _after_ the deletion. Extraction time scales with file size, so the 1.2 MB fixture — already the largest in the corpus — was the best available shot, and it still closed before a human could react.

**Decision: WJ, 2026-08-20 — stop chasing it.** The Storage deletion carries **14 automated tests**, including one that asserts the documented-but-nonexistent `guidelines-temp/<user_id>/` folder form is rejected — which is the failure mode that actually mattered, since code written against the wrong path would have deleted nothing **silently**. This manual add-on is confirmation on top of that, not primary cover, and a few-second window on production is poor value for the risk it retires. **If it is ever wanted, it needs a deliberately instrumented run** (a delay injected into extraction on dev, the way `RT-15` used shortened timers) rather than faster clicking — and the `RT-15` lesson applies: a result earned inside a diagnostic harness is evidence about the logic, not about the shipped path.

**Evidence:** `scripts/rt14-account-deletion-snapshot.sql`, run before and after against the same user id. ⚠️ **Two faults in that script's first outing, both now fixed and documented in the file, because both wasted a run:** the Supabase SQL Editor shows only the **last** statement's result, so a four-section paste runs everything and displays one; and the email comparison was **case-sensitive** while Supabase stores `auth.users.email` lowercased, so a mixed-case address matched nothing and returned **seven zeroes** — indistinguishable from an account already deleted. **Match on the user id where you have it.**

**Notes (2026-07-28):** Live-tested against `grant-pathway-dev`, no issues — deletion completed cleanly. This is the first fresh confirmation since the 2026-07-23 `service_role` grant fix (see `CHANGELOG.md` 2026-07-23): `application_items`/`application_guidelines` were missing `service_role` table grants, causing every deletion to fail with `42501: permission denied` on the very first cascade step. Migration `20260723000000_grant_service_role_item_graph_tables.sql` fixed this on `grant-pathway-dev` (this plan's test environment) — today's clean pass confirms it holds. That migration is **still not applied to `grant-pathway-prod`** as of this test — same outstanding gap tracked since 2026-07-23, due at P5.4.

---

### RT-15 — Session Timeout (Inactivity) [FULL]

**User guide reference:** Not currently documented in the user guide (gap — the guide has no "session timeout" section; consider adding one)
**Spec reference:** `docs/Technical Decision and Design/ADR-SEC-003-session-timeout.md`; NFR (`docs/non-functional-requirements.md`, "Session timeout — automatic logout after 60 minutes of inactivity"); acceptance criteria FR-06 (`docs/PRD inputs/acceptance-criteria.md`)
**What this tests:** That an authenticated session is automatically ended after 60 minutes of inactivity, that the warning modal appears beforehand, and that in-progress work is not lost when the user signs back in.

**Steps:**

1. Sign in and open an application at Step 4 (Draft Answers) with at least one question in progress
2. Leave the session idle (no mouse movement, keypresses, clicks, or touches) for 55 minutes
3. Confirm a warning modal appears with a countdown and an **"I'm still here"** button
4. Let the countdown run out without interacting (or skip ahead to test the 60-minute mark directly)
5. Confirm you are signed out and redirected to the sign-in page with a message indicating you were signed out due to inactivity
6. Sign back in
7. Confirm you return to the same application at the same step, with all previously-entered data and approval state intact

**Expected result:**

- Warning modal appears at 55 minutes, dismissible via "I'm still here"
- Automatic sign-out occurs at 60 minutes of inactivity
- Redirect lands on the sign-in page with an explanatory message
- No data loss — in-progress answers and application state are exactly as left before the timeout

**Result:** ☑ Pass &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked

**Notes (2026-08-20, production — the first full-length, full-sequence run):** ✅ **Pass, and it closes the question the case had left open since 2026-07-28.** Observed on `grant-pathway-prod`, account `TestRT01`, in Chrome, with the shipped **55/60-minute** values — no diagnostic shortening. All four behaviours in one sequence:

1. **The warning modal appeared at 55 minutes**, reading "You'll be signed out in 3 minutes due to inactivity. Make sure you've saved any work before your session ends." with **"I'm still here"** and **"Sign out now"**. ✅ **D-014's fix holds at full length** — the space before "due" is present.
2. **The countdown was allowed to run out** with no interaction.
3. **Sign-out occurred and landed on the sign-in page** with the explanatory message: "You've been signed out due to inactivity. Your work has been saved — sign in again to carry on."
4. **Signing back in returned the work intact** — the A B Charitable Trust / `Prodtest190826 - General Grant` application still present, **In progress**, **0 approved**, **last updated 19 August 2026 — unchanged**, which is the useful detail: the timeout did not touch the record.

⚠️ **Why this supersedes the 2026-07-28 close rather than merely repeating it.** That close rested on the two defects being fixed and confirmed **at 1–2 minute diagnostic timers**, and the case itself recorded the open question in writing: a genuine full-length re-run that same day had shown **no modal at all**, with Chrome's background-tab timer throttling suspected and explicitly "unconfirmed and untested". **A fix demonstrated only at diagnostic values is not the same as the shipped behaviour working**, and until today nobody had seen the 55-minute modal fire at 55 minutes. It does.

⚠️ **One boundary, stated rather than glossed:** step 7 was verified at the **dashboard** level — application present, correct status, correct approval count, last-updated timestamp unchanged. The individual answers inside Step 4 were not re-opened and compared field by field. The unchanged timestamp is strong evidence nothing was written, but it is not the same check as reading the answers back.

**Notes (2026-07-04):** Confirmed live during Clothworkers testing (not a scripted run of this exact test case — WJ was genuinely away from the session for over an hour). On return, the sign-in page was shown as expected; after signing back in, the Clothworkers application was exactly as left — "In progress" at Step 4, all 8 questions present, 0 approved (matching state before the break). The 55-minute warning modal itself was not directly observed this time (WJ was away from the screen), so that specific behaviour is unconfirmed — only the 60-minute sign-out and state-preservation-on-return are confirmed.

**Notes (2026-07-28, diagnostic setup):** WJ attempted a genuine scripted re-run of this test case in Google Chrome and saw neither the 55-minute warning modal nor the 60-minute sign-out — the session simply stayed active. `WARNING_MS`/`TIMEOUT_MS` in `components/session-timeout-provider.tsx` were temporarily shortened to 1/2 minutes (diagnostic only, not a product change) so the behaviour could be observed within minutes instead of an hour, to isolate whether the sign-out logic itself was sound.

**Notes (2026-07-28, two real defects found and fixed):** With the shortened timer, the warning modal did appear — but moving the mouse toward its buttons made it vanish immediately, reappearing a full warning-window later. **Root cause:** the document-level activity listener (`mousemove`/`keydown`/`click`/`touchstart`) reset the inactivity timers — including closing the modal — on any matching event anywhere on the page, with no check for whether the modal was already open. Moving the mouse toward "I'm still here" or "Sign out now" triggered this before the click ever landed, so the modal was never actually clickable. This is a real, previously-invisible production defect — it likely made the warning modal unusable the entire time it has existed, since nobody could have clicked either button without dismissing it first. **Fixed** in `components/session-timeout-provider.tsx`: a ref now tracks whether the modal is open, and the activity listener ignores ambient events while it is — only the modal's own two buttons can end the warning state once it has appeared.

A second, unrelated defect was found once the modal could be read properly: the warning text rendered as "You'll be signed out in 1 minutedue to inactivity" — missing the space between the minute count and "due". **Root cause:** a JSX line-wrap whitespace-trimming gotcha in `components/session-timeout-modal.tsx`, not a plain typo — the sentence was written as JSX text wrapped across two source lines starting right after `{minuteLabel}`; Babel's JSX whitespace algorithm trims the leading space of each line before rejoining lines, silently eating the one space that mattered, while the space between `{minutesRemaining}` and `{minuteLabel}` (on a single unwrapped line) survived. **Fixed** by rewriting the sentence as a single JS template literal, which is immune to this class of bug regardless of how the surrounding JSX gets reformatted.

**Notes (2026-07-28, closed):** WJ re-tested with the shortened timer and confirmed both fixes: the modal now stays on screen and is clickable even as the mouse moves toward its buttons, and the warning text reads correctly ("You'll be signed out in 1 minute due to inactivity."). `WARNING_MS`/`TIMEOUT_MS` have been reverted to 55/60 minutes in `components/session-timeout-provider.tsx` — the shortened values were diagnostic-only and are no longer present in the codebase. Closing RT-15 on that basis: the two real defects the diagnostic surfaced (D-013, D-014) are both fixed and confirmed. Whether Chrome's background-tab timer throttling contributed to the original full-length (55/60-minute) re-run showing no modal at all remains unconfirmed and untested — not pursued further, since the modal and sign-out logic are now demonstrated to work correctly.

---

### RT-16 — Reuse a Previous Application [FULL]

**User guide reference:** Section 3 (Starting an application) — reusing a previous application to the same funder
**What this tests:** P6.5's reuse path — when a user starts a second application to a funder they have applied to before, the service offers to carry over the previous guideline extraction and previously-approved answers instead of asking for them again.

⚠️ **Why this case exists, and why it is here rather than in a flagship.** Until 2026-08-17 the reuse path had **no positive test anywhere in the functional suite**. Both flagships mention it only to assert the prompt does **not** appear — `AB-Charitable-Trust-test-plan.md:129` and `MK-Community-Foundation-test-plan.md:171` — which tests the feature's absence on a fresh account. The path had been driven end to end exactly once, by `accessibility-test-plan.md`'s **AC-14 (Redundant Entry)** on 2026-08-12, against **dev**, under a WCAG pass criterion that could hold while the feature was functionally broken. Since Phase 6 is the stated reason the go-live gate exists, every Phase 6 feature was proven except this one. Placed here rather than in a flagship (WJ's decision, option A) because reuse is funder-agnostic mechanics, and this plan already runs on a pre-seeded account — which reuse needs anyway.

**Prerequisite:** RT-09 complete — an application to a known funder exists with at least one **approved** narrative answer.

**Steps:**

1. From the dashboard, start a **new application**
2. Enter the **same funder name** used in the earlier application (Step 1 is free text — match it as the user would type it)
3. Confirm the service offers to reuse the previous application. **Record the exact wording of the prompt and where it appears**
4. Accept the offer
5. Confirm the guidelines are **carried over without re-uploading and without re-generating the AI summary** — no upload screen, no generation spinner, no new `ai_usage_log` row
6. Continue to Step 4 and confirm previously-approved answers are **present in the answer fields**, not blank
7. Pick one carried-over answer and confirm the text **matches the earlier application's approved text exactly** — compare against the source, not from memory
8. Confirm carried-over answers are presented as **drafts requiring review**, not as already-approved
9. Confirm the earlier application is **unchanged** — still approved, its answers untouched

**Expected result:**

- The reuse offer appears for a repeat funder and is accepted without error
- Guideline extraction and AI summary are reused, not regenerated — no AI request is made
- Previously-approved answer text is carried across verbatim
- Carried-over answers require review before approval
- The source application is left exactly as it was

**Result:** ☑ **Pass** &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked &nbsp;&nbsp; — 2026-08-20, `grant-pathway-prod`, tester WJ

**Notes:** ✅ **Pass — 2026-08-20, production, first execution anywhere.** Source application: the `MK Community Foundation — Oak Grants` / `Prodtest - Community Health programme 190826` application, status **Exported**, created by the `MKCF` flagship on 2026-08-19. It was **not** re-opened.

**Step 3 — exact wording, recorded as the case requires.** The offer renders in **Step 1**, below both name fields, as a two-radio fieldset that appears **on blur of the funder field**. Heading: **"You've applied to MK Community Foundation — Oak Grants before"**. Options: **"Start fresh — upload guidelines and let the AI read them again"** (selected by default) and **"Start from your last application to MK Community Foundation — Oak Grants (Prodtest - Community Health programme 190826, updated 19/08/2026) — carries across the questions and your previous answers for you to review"**. **The default is Start fresh**, so reuse is opt-in.

**Step 5 — the check the case warns about, and it was not waved through.** Accepting the offer went straight to **Step 3 (AI Summary)** with the summary already populated: no upload screen, no spinner. Three independent negative proofs were taken, because content being present is satisfied by a re-generation as well as by a reuse:

1. `ai_usage_log` queried for the preceding 30 minutes — **0 rows**.
2. Axiom, dataset `vercel`, searched for the new application's id over the last 45 minutes — `/applications/…` page requests present, **no `/api/generate-summary`**.
3. Axiom, `| where message contains "[generate-summary]"` over the last 45 minutes — **0 rows**.

⚠️ **Proof 3 was itself verified before being trusted.** The identical filter widened to `ago(3d)` returned 15 lines, the most recent **Aug 19, 17:00:43**, so the empty result is evidence about the application rather than about the query. **This step should not be recorded as passed without that control run** — an empty log query is indistinguishable from a broken one.

**Steps 6–8.** Answers pre-filled. **Q20 compared against the source application's exported Word document** — the actual delivered artefact rather than a database read — and the text matches verbatim; the **citation carried across too** (`Page 6 of the guidelines`). Carried-over answers render with a **"Carried over — please review"** badge and still show **Approve this answer**, so they are drafts, not inherited approvals. **Step 9:** the source card still reads **Exported, last updated 19 August 2026** — unchanged.

⚠️ **One usability observation, raised because the run hit it for real rather than in theory.** The first attempt typed the funder name with a **plain hyphen**; the stored name carries an **em dash**. `getPreviousApplicationForFunder` matches case-insensitively and trimmed but otherwise exactly, so it soft-missed, **no offer appeared, and no message explained why** — the run continued to Step 2 as a fresh application and was one click from re-generating the summary. Pasting the exact name produced the offer immediately. **This is the documented trade-off working as designed** (`actions/applications.ts`, and `DR-FD-001` v1.4 removed the stable funder identity it would otherwise match on) — a soft miss is deliberately preferred to a wrong match. **The finding is that the failure is silent**: to a charity typing a name a second time months later, the feature simply does not exist. **Not raised as a defect and not fixed — held for WJ**, since the alternatives (fuzzy matching, or offering a "you may have applied to a similar funder" list) trade away the never-wrongly-match property this design chose deliberately. ✅ **DECIDED 2026-08-20 (WJ): leave as is, and wait for real user feedback before deciding anything further.** No code change, no gap raised. **The reasoning is that the evidence for a fix does not exist yet** — the only observation of the silent miss is a tester typing a name he had just read on screen, which is not the same as a charity returning to a funder months later, and the two alternatives (fuzzy matching, a near-miss "did you mean…" list) both trade away the never-wrongly-match property deliberately, at real cost, to solve a problem whose frequency is unmeasured. **Revisit if a user reports that reuse did not offer itself, or asks for a previous application that the service did not surface.**

⚠️ **What this case still does not cover:** reuse where the source application is **not** the most recent match, and reuse from an application at Step 4 rather than Exported. `getPreviousApplicationForFunder` takes the single most recently updated candidate, and only the single-candidate path has been exercised.

---

### RT-17 — Two Users Using the Service at the Same Time [FULL]

**User guide reference:** None — this tests the service, not a user-facing feature
**What this tests:** That two different users, signed in simultaneously with their own credentials, can use the service without interfering with each other.

⚠️ **Why this case exists.** **Every result in this suite, both flagships, the capability matrix, the eligibility plan and the accessibility plan was earned with exactly one user on the system.** Raised by WJ, 2026-08-17. `non-functional-requirements.md`'s **`NFR-03`** commits to roughly **10 concurrent users at launch**, up to **10 simultaneous Bedrock calls**, and states the expected outcome as _"All succeed; each takes 20–45s independently; no cross-user interference"_. That expectation has never been tested at any number above one. This case tests **two**, the smallest number that can reveal a cross-user fault; the 10-user load test is deliberately out of scope — see the closing note.

**Prerequisite:** Two production accounts, each with its own credentials and charity profile, and **email addresses never used on dev** — `resendRatelimit` is keyed by email address and shared between environments (`GAP-105`, `G2`), so an address used on dev may silently have no verification resends left on production.

**Steps:**

1. Sign in as **User A** in one browser, and as **User B** in a separate browser or private window. **Do not use two tabs of the same browser session**
2. Confirm each dashboard shows only that user's own applications, with no trace of the other's
3. With both signed in, have **User A** start an application and upload guidelines while **User B** does the same with a **different** funder
4. **Trigger both AI summaries as close to simultaneously as possible.** Record the start and finish time of each
5. Confirm both summaries complete, and that **each is about its own funder's guidelines** — the highest-value check in this case
6. Confirm each user's fair-use count incremented by one, and **only their own** — the monthly cap lives per user in `ai_usage_log`
7. Have both users write and approve an answer at the same time; confirm neither sees the other's text and neither autosave overwrites the other
8. Have **User A** sign out while **User B** continues working; confirm User B's session is unaffected
9. Compare the two recorded AI durations against the single-user baseline from `NFR-01`

**Expected result:**

- Neither user can see any data belonging to the other at any point
- Both AI summaries succeed, each reflecting its own funder's guidelines
- Fair-use counts increment per user, independently
- Concurrent writing and autosave do not interfere
- One user signing out does not affect the other
- AI durations under two-user load stay within `NFR-01`'s targets — 30s standard, **45s large documents**

**Result:** ☑ **Pass** &nbsp;&nbsp; ☐ Fail &nbsp;&nbsp; ☐ Blocked &nbsp;&nbsp; — 2026-08-20, `grant-pathway-prod`, tester WJ

**Notes (2026-08-20, production):** ✅ **`RT-17` PASSES on production — the first time this service has been tested with more than one user on it.** Two real browsers, two accounts (`grantpathway+prodtest@gmail.com` in Chrome, `grantpathway+edgert17test@gmail.com` in Edge), two different funder documents fired as close to simultaneously as hands allow. **No cross-contamination:** each summary was about its own funder — Idlewild arts guidelines for one, MK Community Foundation Oak Grants for the other. **Fair-use counts are per user and independent**, with the two reservations landing **1.7 seconds apart** (13:10:39 and 13:10:41 UTC), so the concurrency was genuine rather than nominal. Concurrent writing and approval did not interfere, and signing one user out left the other working normally.

⚠️ **One duration exceeded `NFR-01` — 70s against the 45s large-document target — and it is NOT a concurrency effect. It is a second real instance of `GAP-115`.** Axiom gives the breakdown for application `765f31df`: a first Bedrock call of **41.4s** (8,746 in / 4,227 out), then a **second confirmation call of ~29s** returning _"eligibility mismatch NOT confirmed on second call — treating as not a mismatch"_. **So roughly 29 seconds were spent re-checking a mismatch that did not exist.** The other user's request was a single 35.9s call, matching its 44s wall clock. **Both first calls were normal, so two-user load did not slow anything** — which is the answer step 9 was asking for. **This strengthens the case for `GAP-115` option (c)**, the cheaper confirmation call: the first recorded instance at least confirmed a genuine rejection, whereas here the entire summary was regenerated to re-read one boolean and the answer came back negative.

⚠️ **One deviation from the prerequisite, recorded because it does not weaken the result but does change what was proved.** The case asks for **two** accounts whose email addresses have never been used on dev. Only one was new (`edgert17test`); the other was the existing shared production account, `Prodtest`. **Two distinct users is what the case needs and what it got** — separate credentials, separate profiles, separate `ai_usage_log` rows. What was _not_ exercised is a second **fresh registration**, so `GAP-105`'s shared `resendRatelimit` remains untested at two users.

**Prior context — the automated half, recorded earlier the same day:** ✅ **The automated half now passes against PRODUCTION at both 2 and 10 concurrent users (2026-08-20)** — 10/10 succeeded, no cross-contamination, p50 **22.1s at 1.01×** the single-user baseline, max 23.5s, fair-use counts per user and only their own. **The first concurrency test of any kind on production**, and `GAP-113`'s required half is closed by it. ⚠️ **It does not close this case** — see the harness note below; and it does not measure cold starts, since the harness warms the route before timing. Earlier the same day it also passed at 2 and 10 users: no cross-contamination, 10/10 and 2/2 succeeded, p50 22.5s at ten against a 22.8s single-user baseline — **0.99× baseline, effectively flat**, and better than the 1.11× recorded on 2026-08-17. **This is a confirming re-run, not new ground** — `NFR-03` already carried 3/5/10-user figures from 2026-08-17 — and it does not close this case. ⚠️ **A production run of the harness is blocked, and the blocker is worth knowing before anyone tries:** `.env.local` holds **dev** credentials, and the script's production guard checked only the target URL, so pointing it at production would have created its users in **dev**, failed every sign-in against production, and left junk behind in the wrong project. `--expect-supabase-project <ref>` was added the same day to make that impossible; a production run needs that project's three Supabase values read from the **Supabase dashboard** and exported for one shell session — **not** `vercel env pull`, which returns the literal `[SENSITIVE]` for all three (tried 2026-08-20). **A harness exists for the automated half — `scripts/load-test.ts`** (`npx tsx scripts/load-test.ts --users 2`), which drives N users through concurrent AI generation and checks canary phrases for cross-contamination. **It does not replace this case**: RT-17 exercises the real browser path — sign-in, dashboard isolation, concurrent writing and autosave, one user signing out while the other works — none of which the harness touches. Run both. ⚠️ **Step 5 is the point of the case.** Two users' guideline text passing through the same AI routes at once is where a cross-contamination fault would appear, and it would be a serious one — one charity seeing another's guidelines or answers. ⚠️ **Two tabs in one browser is not this test** — they share a session cookie and prove nothing about two users. ⚠️ **A load test at `NFR-03`'s stated ~10 concurrent users is NOT covered here** — it is **required** (WJ, 2026-08-17: _"The load test has to be attempted, full stop"_) and tracked separately as `GAP-113`, awaiting only a decision on where it sits. Do not read RT-17 passing as the concurrency question being answered. This case proves two users do not interfere; it says nothing about throughput, queueing or Bedrock throttling at ten.

---

## Defect Log

| ID    | Test   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Severity | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-021 | RT-04  | **Project start and end dates are extracted as narrative writing cards — a textarea with a word counter and an AI-assist button, for an answer that is a date.** Found 2026-08-19 on production during `RT-04`, against The Radcliffe Trust application form (a docx of six tables; fixture added to `docs/Grant Org Guidelines/` the same day, having never been in the repository). Step 3 reported **14 questions plus 1 financial detail**; items **6 (Project Start Date)** and **7 (Project End Date)** are the defect. ⚠️ **The model was obeying the prompt, and that is what makes this more than a typo.** `lib/prompts.ts`'s exclusion list does name "dates" among data-entry fields — but a later clause in the same rule states: _"NEVER drop a question solely because its answer is a number, a date, a duration, a quantity, or a few words"_ and names _"when it starts or finishes"_ as an example of a real application question. **That clause was added to fix `GAP-39`/`GAP-40`, where genuine project questions were being silently dropped.** Extraction did what it was told. **So the fault is downstream of extraction:** the question is correctly identified as a project question and then **rendered with the wrong control.** `item_type = 'date'` already exists in the database enum and in `database.types.ts`, and has never been written or read by any code — only `'narrative'`, `'number'` and `'data'` are ever used. ⚠️ **Two fixes, and choosing between them is a product decision, not a bug fix.** **(a) Stop extracting pure date fields** — a small prompt change, but it contradicts the `GAP-39`/`GAP-40` clause head-on and risks reinstating the defect that clause exists to prevent. **(b) Extract them and render a date input** — correct, uses the enum value already defined, but would be **the first new item type the product renders**, so it touches the Step 4 client type, the export, and the item-graph work `ADR-DATA-006` governs. **Deliberately not fixed on 2026-08-19.** WJ was away, and a prompt change made without him would have been a change to a safety-relevant rule on a guess. **This project's own history is the argument:** `GAP-39`, `GAP-40` and `GAP-51` were each a rule written and then found to behave differently live. **Two observations recorded alongside, neither logged as defects.** **(1) `Project Description` shows no limit at all**, where the form says **"400 words minimum"** — ✅ **the good outcome, since a minimum rendered as a cap would warn the applicant for writing what the funder asked for**, but the funder's instruction is lost entirely, and this is the question the Trust says it bases its assessment on. **(2) "Have you received funding from The Radcliffe Trust in the last five years?" renders as free text**, where MKCF's yes/no facts get a dropdown — arguably right, since the Trust likely wants detail, but inconsistent. **Also unverified:** only **one** financial detail was extracted (expenditure) where the form asks for organisation income **and** expenditure. Income may simply not be one of the five fixed `governanceFacts` keys, in which case this is correct by design — **not checked.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Medium   | ⚠️ **OPEN — DEFERRED to post-launch (WJ, 2026-08-19), and RT-04 recorded as Pass (caveat) rather than Fail.** **The reasoning for deferring:** nothing is lost or misrepresented — the answer is captured, saved and exported correctly; the applicant simply meets the wrong control. **Option (a) is ruled out**, not merely unchosen: suppressing date extraction trades a cosmetic fault for the silent data-loss fault `GAP-39`/`GAP-40` exist to prevent. **Option (b) is the fix, and it is blocked on something bigger:** the extraction schema has no field that can express what kind of question this is — **the same contract change `docs/impact-assessment-supporting-document-questions.md` identified for supporting-document questions.** Doing both together is cheaper than either alone, which is the argument for sequencing rather than rushing. **Re-run RT-04 when it is built.** |
| D-020 | RT-01a | **Production has never worked for a signed-in user: the `authenticated` role had no DML privileges on five tables.** Found 2026-08-18 when the charity profile save failed. `authenticated` held only `REFERENCES`, `TRIGGER` and `TRUNCATE` on **`user_profiles`, `charity_profiles`, `applications`, `ai_usage_log` and `funders`** — no `SELECT`, `INSERT`, `UPDATE` or `DELETE` — so every read and write by a signed-in user failed with **`42501 permission denied`**. ⚠️ **The dashboard's "You don't have any applications yet" was a permission error rendered as an empty state**, which is why nothing looked wrong until a write was attempted. **The surviving tables are the clue.** `application_guidelines` and `application_items` were healthy; both are granted by the **July** migration `20260723000000`. Every broken table is granted by the **May** migration `20260521000000` — which `supabase_migrations.schema_migrations` **records as applied on production**. ⚠️ **A version row proves bookkeeping, not execution — and `RT-00` compares the set of applied versions, so it passed this morning while five tables were unusable.** That is a weakness in the case, not just in the database. **Ruled out, so nobody re-treads it:** the RLS hardening script (all 139 lines are policies, no `REVOKE` — suspected first and exonerated), the RLS policies themselves (present and correct), and `service_role`/`postgres` (full privileges throughout). ⚠️ **Not proven either way: whether the May migration never executed or a later `REVOKE` removed its work.** The repair is identical; the cause matters only for whether anything _else_ from that migration is missing. **Repair:** `20260818000000_restore_authenticated_table_grants.sql` committed, and the same five statements run against production by WJ (no errors). ⚠️ **Manual SQL against production is forbidden by `ADR-DATA-004` — this is a recorded exception**, taken because `supabase db push` would have skipped a version production already claims to have applied. ⚠️ **The first version of that script failed with `42P01`**: it granted `application_answers` and `user_tooltip_dismissals`, both **dropped in July** (`20260714000000` and `20260725000000`). **Production's 7 base tables are the correct set** — an earlier reading of the inventory as "schema materially incomplete" was wrong and is corrected here. **2026-08-19 — the parity audit answered the open question, and the answer is the worse of the two.** `scripts/parity/snapshot.sql` was run against both projects and diffed with `npm run parity`. **A second migration is recorded as applied on production while its statements plainly did not execute:** `20260528000001_add_qa_model_enum_values.sql`, whose two `alter type ... add value` statements were absent, so production's `ai_request_type` enum held three labels against dev's five — **`refine_answer` and `assemble_draft` both missing.** ⚠️ **Consequence had `P5.5` restarted without this:** every per-question AI assist (`POST /api/refine-answer`, S6.6) and every final assembly (`POST /api/assemble-draft`, S6.7) writes an `ai_usage_log` row of that type, so both would have failed at the log write — **after the AI call was already paid for.** ⚠️ **Correction, 2026-08-19, same day:** the `assemble_draft` half of that consequence is **wrong and is retained rather than quietly edited.** `assemble_draft` exists as an `ai_request_type` value but **nothing in the codebase ever writes it** — `grep` across `app/`, `lib/`, `actions/` and `components/` returns one hit for `refine_answer` (`app/api/refine-answer/route.ts:155`) and **none for `assemble_draft`**. There is no assemble-draft AI route: assembly is `assembleAndAdvance` in `actions/applications.ts`, plain string concatenation with no AI call and no `ai_usage_log` write. **So the real blast radius was the per-question AI assist alone** — still a genuine defect on the single most-used AI feature in the product, but half the scope first claimed. **The error came from reasoning about the enum's names instead of checking their call sites**, which is the same mistake as `D-017` in a different costume: a plausible reading asserted without the one grep that would have tested it. **Two independent migrations, five months apart, both recorded-and-unexecuted, makes a later `REVOKE` the less likely explanation and a systemic recording problem the more likely one** — this is no longer plausibly a one-off. **Repaired by WJ the same morning** (both `alter type` statements run in the production SQL Editor, a second recorded `ADR-DATA-004` exception on the same grounds as the first), **and the re-snapshot is clean: zero structural differences across all eleven compared sections.** ⚠️ **What the audit does not settle:** it compares the two databases, so it can only find a missing migration whose effect dev actually has. A migration unexecuted on **both** projects is invisible to it, and nothing yet explains _why_ the recording and the execution came apart. **Status: both instances repaired and verified by a clean parity run. The mechanism behind them is not diagnosed and this row stays OPEN on that basis. `RT-00` still needs a privilege check added — and should now call `npm run parity` outright, which subsumes it.** |
| D-019 | RT-01a | **The charity profile save discarded the database error, so a total failure produced only "Please try again."** Found 2026-08-18. `saveCharityProfile` returned a generic message and threw the Supabase error away — no code, no message, no hint, nothing to Sentry, nothing to Axiom. **The user is told to retry an operation that could never succeed.** **Same class as `D-016`, in a different action** — the third instance found in one session, which is what makes it a pattern rather than an oversight. **Fixed:** the branch now logs `code`, `message`, `details` and `hint` and captures to Sentry with the `isFirstSave` flag. ✅ **It paid for itself immediately**: the very next attempt produced `code=42501 permission denied for table charity_profiles`, which is `D-020` — a defect that had been invisible for an unknown length of time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-018 | RT-01b | **No AI feature worked on production — Bedrock was rejecting every request with a 403 signature error, and nothing anywhere said so.** Found 2026-08-18, the root cause behind D-016. **Production's `AWS_SECRET_ACCESS_KEY` did not match its `AWS_ACCESS_KEY_ID`**; AWS returned _"The request signature we calculated does not match the signature you provided"_ on every `bedrock:InvokeModel` call. **Confirmed by elimination and then by the log line itself**, once `actions/charity.ts` was instrumented: `[lookupCharity] bedrock paraphrase failed for 1194917: 403 ... Check your AWS Secret Access Key`, Axiom dataset `vercel`, 16:48:00. ⚠️ **The scope is every AI feature, not one form field.** Guideline summaries, refine-answer and the charity paraphrase share these credentials, so **all three flagship plans would have failed at Step 3** — the AI summary step — had P5.5's runs started with them. **This is the single most valuable thing found on production so far, and it was invisible on dev**, which holds a different IAM user's key by design (`DEV-PROD-PARITY-CHECKLIST.md` G4, `VQ-021`). **Why four separate checks all said "configured":** the variables were present, scoped to Production, and the access key ID was genuinely valid — AWS confirmed Key 1's last use was in `eu-west-2`, so only the secret was wrong. **Presence was checked; validity never was.** **Why `ai_usage_log` was empty and misled the diagnosis:** `reserve_ai_slot` did insert a row, then `cancel_ai_slot` removed it when Bedrock failed — correct behaviour, so users are not charged for service-side failures, but it erases the only durable evidence that an attempt happened. **"No row" was read as "never called" for most of an hour.** **Fix (WJ, 2026-08-18):** new access key pair created for IAM user `grant-pathway-prod`, both `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` replaced in Vercel Production, redeployed. **Old Key 1 to be deleted once the new pair has been exercised further.** ✅ **Verified live on production:** the lookup now returns both descriptions with the AI-provenance notice — _"The descriptions below were drafted by AI from your Charity Commission entry"_ — and the text is materially correct for the charity. **First successful AI call ever made on production.** ⚠️ **Two follow-ups this leaves open.** A credential check belongs in `DEPLOYMENT-CHECKLIST.md`: **one real AI call against production, not a variable-presence check.** And `P5.4`'s sign-off treated AI as configured on the strength of the variables existing — worth noting as a pattern rather than a one-off. **Status: FIXED and verified. The silent-failure handling from D-016 stays in place — it is what found this.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-017 | RT-01b | **Sentry has received no events at all — 14 days, every environment, no filters — so the project's error visibility is an assumption rather than a fact.** Found 2026-08-18 while diagnosing D-016. Instrumentation was added to `actions/charity.ts` precisely so a production failure would report itself; the failure was then reproduced on production and **nothing arrived in Sentry.** Widening the search to **14 days, all environments, no query and no `is:unresolved` filter** returned nothing either. ⚠️ **"Nothing went wrong" is not a credible reading of that window.** `GAP-101` (production on the built-in mailer), `GAP-106` (the wrong password message) and `GAP-041` (the Word export destroying line breaks) were all live in it, and `actions/auth.ts` calls `Sentry.captureException` on registration failure by design — a fix made specifically so the real Supabase error would be visible. **What has been ruled out:** `SENTRY_DSN` **is** present in Vercel production (checked by WJ), and `sentry.server.config.ts` has no sampling and no `beforeSend` rule that would drop a message — it only scrubs PII fields. **What has not been established:** whether the DSN in Vercel points at this project, whether it is still valid, and whether this project has ever received a server-side event. WJ is checking Sentry → Settings → Client Keys against the Vercel value. ⚠️ **The consequence is wider than one form field, which is why it is logged separately from D-016.** `ADR-OPS-004` treats Sentry as the place incidents surface, and `GAP-03`'s P95 alerting assumes events arrive. **If they do not, every silent-failure fix in this codebase reports into nothing, and the project has no error visibility on production at all.** **Mitigation already applied:** `actions/charity.ts` now also writes console lines, which reach Axiom — a channel confirmed working on 2026-08-17. **Diagnosis should not depend on a single reporting path**, and that is the lesson worth keeping from this whether the DSN turns out to be wrong or not. ✅ **CLOSED 2026-08-19 — there was never a fault. Sentry has been receiving and displaying events throughout.** WJ opened the Issues list filtered to project `grant-pathway`, environment `production`, last 30 days: **seven issues, including both of the previous day's real defects** — `permission denied for table charity_profiles` (`D-020`, 16 hours old) and the AWS `403 ... signature we calculated does not match` (`D-018`, 17 hours old), plus the deliberate `P5.4` test error from three days earlier and four older issues going back a month. ⚠️ **So the 2026-08-18 observation — "no events in 14 days across every environment" — was simply wrong**, and it was already contradicted by the evidence in this repository: `GAP-21` was closed on 2026-08-16 by capturing a tagged event on production and reading it in Sentry, two days inside the window that was reported as empty. **The contradiction sat unexamined for a day and gated `P5.5` while it did.** **The lesson is about the search, not the tool:** a dashboard query returning nothing is evidence about the query until the query itself is verified — the same shape as `D-018`, where `ai_usage_log` being empty was read as "never called" for most of an hour. **Nothing was changed in the code or in Sentry to close this**, which is the strongest available evidence that nothing was broken. **Status: CLOSED — not a defect.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-016 | RT-01b | **A failed Charity Commission governing-document call is completely silent — the user sees two blank required fields and no indication anything went wrong.** Found 2026-08-18 during the first production run of RT-01b. Looking up **1194917** on production pre-filled charity name and registration number correctly, but **What does your charity do?** and **Who does your charity help?** came back empty. Those two fields are not copied from the register — they are **AI-paraphrased from the charity's `charitable_objects` text by Bedrock**, which needs a second API call to `/charitygoverningdocument/{n}/0`. **The upstream data is present:** the same endpoint called directly with the key from `.env.local` returns the objects text for this charity, so the register is not at fault. **And Bedrock was never reached** — `ai_usage_log` on production has no `charity_paraphrase` row at all, which places the failure before the AI call, in the governing-document fetch. ⚠️ **The defect is the silence, not the empty fields.** `actions/charity.ts` treats a non-2xx status as non-fatal and catches network errors with a bare `catch {}` — **no Sentry capture, no log line, no user-facing message.** The lookup reports success. So the failure mode is indistinguishable from a charity whose register entry genuinely has no objects text, and **nothing anywhere records which happened.** That is why this could not have been noticed until someone compared the two environments by hand. **Degrading gracefully is right; degrading invisibly is not.** At minimum the failed call should reach Sentry, and the form should say the register lookup could not retrieve a description rather than leaving two required fields blank with no explanation. **Suspected cause of the production failure itself (not yet confirmed):** production's `CHARITY_COMMISSION_API_KEY` differs from the working local value or was truncated when pasted — the first endpoint succeeds with it, so the key is valid for at least one API. WJ re-set the value and redeployed 2026-08-18; retest pending. If it still fails, the next check is whether that key's Charity Commission subscription covers the Governing Document API, which is per-API on some plans. **Status: OPEN — silent-failure handling not yet fixed. The production symptom may clear with the key re-set; the missing error handling will not.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-015 | RT-09  | **The Word export destroys every line break in an answer (`GAP-41`).** Found 2026-08-06 by WJ, not by running this plan but by reading a real `.docx` his wife downloaded after completing a genuine Stony Stratford Town Council application. She had laid out a 215-word answer with blank lines between paragraphs, a hyphen-bulleted list of facilities, a "Funding to Date" block and a worked calculation; the exported document rendered all of it as one continuous run of prose. Root cause: `app/api/export/[applicationId]/route.ts` puts the whole answer body into a **single** `TextRun` (both the `assembled_draft` branch and the per-answer fallback), and the `docx` library ignores `\n` inside a run. Nothing else in the pipeline loses it — the `<Textarea>` stores `\n` intact, Step 5's preview renders correctly via `whitespace-pre-wrap`, and `format=txt` is unaffected. Agreed fix: one `TextRun` per line with `break: 1` on all but the first. **What makes this a test defect as well as a code defect: RT-09 passed against this exact code on 2026-07-28.** Its verification list said "no corrupted or missing content", which single-paragraph answers satisfy while every line break is being discarded. RT-09 and RT-10 both gained an explicit multi-line fidelity step in v2.12 so the hole cannot reopen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | High     | ✅ **Fixed 2026-08-06** — one `TextRun` per line with `break: 1`, proven against the packed `.docx`. **Closed 2026-08-14** — RT-09's manual re-run confirmed a genuinely multi-line answer opens cleanly in Word, layout intact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-014 | RT-15  | **Warning modal text rendered "1 minutedue to inactivity" — missing space before "due".** Root cause: a JSX line-wrap whitespace-trimming gotcha in `components/session-timeout-modal.tsx` (not a plain typo) — the sentence was written as JSX text wrapped across two source lines starting right after `{minuteLabel}`; Babel's JSX transform trims the leading space of each line before rejoining, silently eating the space that mattered. Fixed by rewriting the sentence as a single JS template literal, immune to this class of bug regardless of future reformatting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Cosmetic | ✅ **Fixed and live-confirmed 2026-07-28** — see CHANGELOG.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-013 | RT-15  | **Warning modal dismissed itself the instant the mouse moved toward its buttons, reappearing a full warning-window later — meaning it was never actually clickable.** Found while diagnosing RT-15's fresh re-run (see RT-15 notes) using a temporarily shortened timer. Root cause: the document-level activity listener (`mousemove`/`keydown`/`click`/`touchstart`) reset the inactivity timers — including closing the modal — on any matching event anywhere on the page, with no check for whether the modal was already open. This is a real, previously-invisible defect that likely existed in production since the modal was first built. Fixed by tracking modal-open state in a ref and having the activity listener ignore ambient events while the modal is showing; only its own two buttons can now end the warning state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Blocking | ✅ **Fixed and live-confirmed 2026-07-28** — see CHANGELOG.md                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-012 | RT-01a | **Two chained defects found during the first live run of RT-01a, both now resolved.** (1) Registration failed with the generic "Something went wrong" message for every new account — root cause confirmed by calling Supabase Auth's `/auth/v1/signup` directly (bypassing the app): `{"code":500,"error_code":"unexpected_failure","msg":"Error sending confirmation email"}`, an email-delivery failure inside Supabase Auth's SMTP relay (Resend), caused by a stale/invalid API key. Fixed by rotating the key. (2) Once email delivery started working again, verification links started showing "This link has expired" within minutes of being sent — root cause confirmed via `auth.users` timestamps across 5 accounts spanning a month: every verification link was being confirmed 15-80 seconds after being sent, regardless of browser (Comet, Chrome, Edge). This is Gmail's own server-side link-scanning (spam/phishing detection) visiting the single-use link before the real user ever opened it -- entirely independent of browser/device. Fixed by no longer completing verification on page load; `/auth/callback` now redirects signup confirmations to `/verify-email/confirm`, which submits automatically via JavaScript on mount (Gmail's scanner fetches over HTTP and does not execute JS) with no visible button -- closing the gap while keeping it a single click for a real user. Re-tested successfully across Comet, Chrome, and Edge post-fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Blocking | ✅ **Fully resolved 2026-07-02** — see CHANGELOG.md for complete detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Fixed alongside this finding:** `actions/auth.ts` now calls `Sentry.captureException(error, { tags: { action: 'registerUser' } })` before returning `{ error: 'unknown' }`, so the real Supabase error is visible in Sentry next time instead of vanishing.

**Root cause and fix (2026-07-02):** The Resend API key configured as the SMTP password in Supabase Dashboard → Authentication → Emails → SMTP Settings (dev project) was invalid/stale — confirmed by querying Resend's API directly with the key on file, which returned `"API key is invalid"`. Resend's own dashboard showed zero sent emails in the last 15 days, consistent with the key never having worked recently rather than a fresh regression. Domain verification was not the issue — `resend._domainkey.grantpathway.org.uk` (DKIM) and `send.grantpathway.org.uk` (SPF) were both correctly configured. Wac generated a new Resend API key (`grant-pathway-supabase-smtp`, Sending-access scope) and updated the SMTP Settings password field. Verified fixed by repeating the direct `/auth/v1/signup` call — Supabase returned a full success response with `confirmation_sent_at` set, no error. **Prod SMTP settings have not been checked** — prod has no live consumer yet, so this was correctly deferred, but should be verified before prod is used for anything.

---

## Appendix A — Resetting or Creating Test Data

If the pre-seeded "Test Application" is no longer available (deleted, exported, or at the wrong step) — or you're using a freshly registered account per RT-01a, which has no application at all yet — create one:

1. Sign in as `grantpathway+idle100@gmail.com` (or skip this if you're already signed in from RT-01a with a fresh account)
2. Click **+ New Application**
3. Select **Henry Smith Foundation** from the funder picker
4. Enter grant name: **"Regression Test Application"**
5. Click **Continue**
6. Upload or paste guidelines (use `henry-smith-holiday-grants-application-template.docx` or any guidelines text)
7. Click **Continue** → wait for AI summary → click **Continue**
8. The application is now at the preparation checklist — leave it here if testing RT-07, or click through to Step 4 for RT-04/RT-05/RT-06

For RT-08/RT-09/RT-10, approve all answers in Step 4 before running those tests.

---

## Appendix B — Known Behaviours

| Ref                 | Description                                                                                                                                                                                                                                                 |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cache               | After dependency updates, the `.next` build cache may need clearing before step pages compile correctly. If step pages return 404 on a fresh server start, stop the server, delete `.next/`, and restart. Verified 2026-06-15.                              |
| Button label        | User guide v1.14 (Section 2) states "View" for continuing an application; the app currently shows "Continue". This is a user guide discrepancy, not a bug.                                                                                                  |
| AI latency          | `/api/refine-answer` typically responds in 5–10 seconds. Up to 30 seconds is acceptable. Over 30 seconds indicates a potential Bedrock or timeout issue.                                                                                                    |
| 50 AI request limit | The test account shares the 50 AI requests/month limit. RT-05 uses one request. If the limit is reached, RT-05 will show a usage limit error — this is expected behaviour, not a regression. Use a different test account or wait for the counter to reset. |

---

## Document History

| Version | Date       | Author         | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------- | ---------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2.31    | 2026-08-20 | Rapidglobe Ltd | ✅ **`RT-14` passes on production with row-level proof — 21 rows across seven tables went to 0.** Before: `auth.users` 1, `application_items` 5, `application_guidelines` 1, `applications` 1, `charity_profiles` 1, `ai_usage_log` 11, `user_profiles` 1. After: all seven **0**, and the old credentials are refused. **`ai_usage_log` = 11 matched the dashboard's "11 of 50 AI requests used" exactly**, which is what confirmed the snapshot was reading the right account on the right project. The confirmation email arrived (`FR-44`). ✅ **The 2026-07-23 `service_role` grant defect is confirmed fixed on production** — checked directly before the attempt, and `application_items`, the table that broke the cascade then and carried the largest count now, deleted cleanly. **RT-14's notes had recorded that migration as unapplied to production since 2026-07-23.** ⚠️ **One real defect found and fixed: contradictory banners on the sign-in page (`D-022`).** After deletion the page showed **"Your account has been deleted"** and, once a sign-in was attempted with the deleted credentials, **"Your email address or password is incorrect"** — both at once, a green success notice stacked directly above a red error. **Cause:** both status banners are driven by URL query parameters (`?deleted=true`, `?timeout=true`) and the form posts back to the same URL via `useActionState`, so the parameter survives the submission. **It was not specific to deletion** — `?timeout=true` behaved identically, so **`RT-15`'s inactivity message had the same fault**: one cause, two symptoms, and the second would never have been found by looking at RT-14 alone. **Fixed** in `components/sign-in-form.tsx`: both banners are suppressed once the form has an error to report, on the reasoning that a status message describes what happened _before_ the page loaded, and once the user has tried something and it failed, the failure is the only thing worth showing. Both paths verified locally — banner on load, error alone after a failed submission. **Found by WJ, who called it "not a huge problem but does need tidying up".** **Two things the run covered that the plan does not ask for, both worth keeping.** **(1) The confirmation is genuinely case-sensitive:** lowercase `delete` and partial `DElte` were both refused, so the gate tests the exact string rather than merely a non-empty field. **(2) Step 4's first clause does not match the implementation, and the second does.** The button is **not** disabled before `DELETE` is typed — it is only disabled while a deletion is in flight (`disabled={isPending}`) — and the gate lives in the submit handler, which refuses and shows "Please type DELETE in capitals to confirm." **This is a pass under the step's "(or the action is blocked)" wording, and arguably the better pattern**, since a disabled button explains nothing. Recorded because it looked like a defect on sight and cost a code read to settle. ⚠️ **A related nit, not a defect:** the confirmation field uses `DELETE` as its **placeholder**, so at a glance it reads as already filled in — only the grey-versus-black text distinguishes them. |
| 2.30    | 2026-08-20 | Rapidglobe Ltd | ✅ **`RT-15` passes on production at the shipped 55/60-minute timers — the first complete sequence ever observed, and it closes a question the case had recorded as open.** Modal at 55 with the wording correct, countdown allowed to run out, sign-out at 60 onto the sign-in page with "You've been signed out due to inactivity. Your work has been saved", and the application intact on return with **its last-updated timestamp unchanged**. ⚠️ **The 2026-07-28 close rested on 1–2 minute diagnostic timers**, and a genuine full-length run that same day had shown **no modal at all** — written down then as unconfirmed, Chrome background-tab throttling suspected. **A fix demonstrated only at diagnostic values is not the shipped behaviour working.** It now demonstrably fires at 55 minutes. ⚠️ One boundary: step 7 was checked at dashboard level, not by re-reading the Step 4 answers field by field. Also **`RT-14` groundwork:** a before/after DB snapshot was written (`scripts/rt14-account-deletion-snapshot.sql`) because the case's steps 1–8 cannot distinguish a clean deletion from a half-completed cascade — sign-in fails either way. **Its first run cleared RT-14's standing risk:** all four `service_role` grant rows on `application_items` and `application_guidelines` are present on production, so migration `20260723000000` **is** applied there — the case notes had recorded it as outstanding since 2026-07-23.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2.29    | 2026-08-20 | Rapidglobe Ltd | **RT-12 passes on production, and the load harness re-runs clean at 2 and 10 concurrent users.** RT-12: the success message appeared **and the new password was then used to sign back in** — the half a success message does not prove, since the message is written by the same code path whether or not the credential persisted. **The harness (RT-17's automated half) passed at both sizes** — no cross-contamination, all requests succeeded, and ten users came out at **0.99× the single-user baseline** against 1.11× on 2026-08-17, so the near-flat curve is now observed twice on different builds. ⚠️ **It does not close RT-17**, whose browser half (dashboard isolation, concurrent autosave, one user signing out while the other works) the harness does not touch. ⚠️ **A production harness run is blocked, and the blocker was a defect in the harness:** its production guard checked only whether the target URL was non-local, not which Supabase project the credentials write to — so aiming it at production would have created its users in **dev**, failed every sign-in against production, and left test users behind in the wrong project. Fixed the same day: a non-local target now requires `--expect-supabase-project <ref>` matched against the credentials actually loaded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2.28    | 2026-08-20 | Rapidglobe Ltd | **RT-16 passes on production — its first execution in any environment, and the first positive test of P6.5's reuse path anywhere in the functional suite.** Both flagships only ever asserted the prompt's **absence** on a fresh account; the path had been driven end to end once, by `AC-14` on dev, under a WCAG criterion that could hold while the feature was functionally broken. ✅ **Step 5, the case's own stated weak point, was proven three independent ways** — zero `ai_usage_log` rows, no `/api/generate-summary` request against the new application, and no `[generate-summary]` log line — **and the third was validated by a control query** that returned yesterday's runs, because an empty log query is otherwise indistinguishable from a broken one. Answer text was compared against the source's **exported Word document** rather than a database read, and matched verbatim including its citation. ⚠️ **One usability observation, held for WJ and not raised as a defect:** the funder name's **em dash** had to be pasted — a typed hyphen soft-missed, **no offer appeared and nothing said why**, and the run was one click from re-generating the summary as a fresh application. That is `getPreviousApplicationForFunder`'s deliberate never-wrongly-match trade-off, but **the failure mode is silent**, and to a user returning to a funder months later the feature simply does not exist. **Two gaps in coverage recorded in the case:** reuse where the source is not the most recent match, and reuse from a source at Step 4 rather than Exported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.27    | 2026-08-19 | Rapidglobe Ltd | **RT-06 through RT-11 and RT-13 recorded as production passes.** ⚠️ **Provenance is deliberately split, because it differs.** **RT-06–09 are derived from the flagship runs** — approval and progress bar (`ABC-09`/`MKCF-08`), the preparation-checklist gate (`ABC-05`), the senior-review screen (both, and screenshotted directly), and the Step 5 approve-and-download (`ABC-10`/`MKCF-09`). **RT-10, RT-11 and RT-13 were run and confirmed directly by WJ**, and are recorded separately because **none is derivable from a flagship**: the flagships export Word rather than exercising `format=txt`; "Back to editing" reaches the assembly screen without touching the `reopen_application` RPC this case exists to test; and no flagship step checks that sign-out clears the session **server-side**. **The distinction is kept so a future reader can tell observed from inferred.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.26    | 2026-08-19 | Rapidglobe Ltd | **RT-05 recorded as a production Pass on `ABC-08`'s evidence rather than by a standalone run (WJ's decision).** `ABC-08` wrote, AI-assisted and approved a narrative answer on `grant-pathway-prod` the same day — same route, same `reserve_ai_slot` RPC, same environment. **This is the first execution of `/api/refine-answer` on production, and `refine_answer` is the one `ai_request_type` the `D-020` enum gap would genuinely have broken**, so the pass is evidence about that repair as well as about the feature. ⚠️ **One check is inferred, not observed, and is recorded as such:** that both "Use this improved version" and "Keep my original" render — the approved AI-assisted answer requires the first, the second comes from the same component. A standalone run would cost an AI call against the monthly 50 for that one detail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.25    | 2026-08-19 | Rapidglobe Ltd | **`D-021` deferred to post-launch and RT-04 changed from Fail to Pass (caveat) — WJ's decision.** The interface loads and every answer is captured, saved and exported; the defect is that a date question is rendered as a prose box with a word counter and an AI-assist button. ⚠️ **Not fixed by suppressing date extraction, which would re-open `GAP-39`/`GAP-40`** — the prompt clause that produces these cards exists to stop genuine project questions being silently dropped, and trading a cosmetic fault for a silent data-loss one is the wrong trade. **The correct fix needs a question-type field the extraction schema does not have**, which is the same contract change the supporting-document assessment identified — so the two are cheaper done together. **The Fail verdict and its reasoning are kept in the case notes rather than swapped out.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.24    | 2026-08-19 | Rapidglobe Ltd | ❌ **RT-04 fails on production; `D-021` logged.** Run against The Radcliffe Trust form — a fixture that **was not in the repository at all**, The Radcliffe Trust having never been added to any list; copied in during the run. **`Project Start Date` and `Project End Date` render as narrative textareas with word counters and AI-assist buttons.** ⚠️ **The model was obeying `lib/prompts.ts`, whose `GAP-39`/`GAP-40` clause states dates and durations must never be dropped and names "when it starts or finishes" as a real application question** — so the fault is the control the question is rendered with, not the extraction. `item_type = 'date'` exists in the enum and has never been used. **Not fixed: the choice between suppressing extraction and rendering a date input is a product decision, and making it while WJ was away would have been a guess against a rule that exists to prevent a fixed defect.** ✅ **Two good results from the same run:** the form's **"400 words minimum"** was not turned into a cap, and the six budget "Description / Notes" fields were not over-extracted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.23    | 2026-08-19 | Rapidglobe Ltd | **RT-03 passes on production, unblocked by the flagship runs.** It reads a dashboard carrying completed applications, and none existed on production until the A B Charitable Trust and MK Community Foundation flagships created them. **The dashboard was therefore read against genuinely varied real data — two completed applications and one Ineligible** (Walton Charity, from the same afternoon's eligibility run) — exercising the status labels and the summary bar's counts, including the `DR-EL-001` Ineligible count, on production. ⚠️ **Recorded because it is a property of this plan, not of this session: RT-03 sits third in a suite whose prerequisites are not produced until the flagships run, so the suite's own order does not hold on a fresh environment.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.22    | 2026-08-19 | Rapidglobe Ltd | **RT-02 passes on production; RT-03 BLOCKED, and the block is expected.** RT-03 reads a dashboard carrying at least one completed application, and **no production account has ever completed one** — production was not usable by a signed-in user until 2026-08-19 (`D-020`). **Deferred until a flagship end-to-end plan has been run against production, which creates that state**; RT-03 and any later case assuming existing applications are then re-run against real data. ⚠️ **Plan fix owed:** the header names a **dev** test account, while production testing uses `grantpathway+Prodtest@gmail.com`, so cases naming a specific user cannot be run verbatim against production — **the header should carry both.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.21    | 2026-08-19 | Rapidglobe Ltd | **`P5.5` restarted, and RT-00, RT-01a and RT-01b now pass against `grant-pathway-prod`.** RT-00 passed on its strengthened case — `npm run parity` clean and one real AI call returning both descriptions. **RT-01a and RT-01b's part-run of 2026-08-18 is void and superseded**, having taken place under an environment where no signed-in user could read or write (`D-020`) and no Bedrock call could succeed (`D-018`). **RT-01b is the case whose failure uncovered `D-016`, `D-017` and `D-018`; it now returns both AI-written descriptions with the provenance notice.** The charity profile save, which failed on 2026-08-18, succeeds. **These are the first clean smoke-test runs against production.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.20    | 2026-08-19 | Rapidglobe Ltd | **RT-00 strengthened, and its 2026-08-18 Pass withdrawn in effect.** That Pass was earned hours before `D-020` (no DML privileges for `authenticated` on five tables) and `D-018` (every Bedrock call failing on a signature mismatch) were found by ordinary testing — **it compared the set of applied migration versions, and a version row proves bookkeeping, not execution.** Two new steps: **step 5 runs `npm run parity`**, which compares grants, tables, RLS, functions and ACLs, policies, columns, enums, storage, constraints, triggers and indexes — this **replaces** the standalone `authenticated` grant check the case was going to grow, and is what would have caught `D-020`; **step 6 makes one real AI call** (the `/profile` lookup of 1194917), which is what would have caught `D-018`, where four checks said "configured" and none said "working". ⚠️ **Two limits recorded in the case itself:** the parity run is a _comparison_, so a migration unexecuted on both projects is invisible to it, and it sees the database only — credentials, Supabase Auth settings and Vercel configuration are outside it. **Re-run RT-00 in full against production before `P5.5` restarts.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2.19    | 2026-08-19 | Rapidglobe Ltd | **D-017 CLOSED — not a defect.** WJ read the Sentry Issues list filtered to `grant-pathway` / `production` / 30 days: **seven issues, including both of the previous day's real defects** (`D-020`'s `permission denied for table charity_profiles`, `D-018`'s AWS 403 signature error) and the deliberate `P5.4` test error. Sentry has been receiving and displaying events throughout. ⚠️ **The "no events in 14 days" observation of 2026-08-18 was wrong, and this repository already contradicted it** — `GAP-21` closed on 2026-08-16 by reading a tagged production event in Sentry, inside the window reported as empty. **A dashboard query returning nothing is evidence about the query until the query is verified.** Nothing was changed to close this. **`P5.5` is no longer gated on error visibility.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2.18    | 2026-08-19 | Rapidglobe Ltd | **D-020 updated with a second instance, found by the environment parity audit.** `20260528000001_add_qa_model_enum_values.sql` is recorded as applied on production and its two `alter type ... add value` statements had not executed, leaving `ai_request_type` with three labels against dev's five. **`refine_answer` and `assemble_draft` were both missing, which would have failed every per-question AI assist and every final assembly at the `ai_usage_log` write — after the Bedrock call was already paid for.** Repaired by WJ, and the re-snapshot diffs clean across all eleven sections. ⚠️ **Two migrations, five months apart, both recorded-and-unexecuted: the "later REVOKE" theory is now the less likely one.** The mechanism is still undiagnosed, so D-020 stays OPEN. **`RT-00` should call `npm run parity` rather than grow its own privilege check** — the script covers grants, tables, functions, policies, columns, enums, storage, constraints, triggers and indexes in one run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.17    | 2026-08-18 | Rapidglobe Ltd | **RT-00 re-run against `grant-pathway-prod` and passed — the first case in this suite executed against production.** All 5 RPC functions present; 32 migrations applied, latest `20260814000000`, matching the repo. **Step 4's `supabase migration list` was substituted with a direct count against `supabase_migrations.schema_migrations`**, which gives the same assurance without linking the CLI to prod or pulling the production database password onto the machine — recorded in the case notes rather than left as an undocumented deviation. Unblocks `P5.5` item 1 on the schema precondition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.16    | 2026-08-17 | Rapidglobe Ltd | **Two new cases, and the plan is no longer all-pass as a result.** **RT-16 (reuse a previous application)** closes a gap found while re-checking `P5.5` item 2's premise: P6.5's reuse path had **no positive test anywhere in the functional suite** — both flagships mention reuse only to assert the prompt does _not_ appear, and the path had been driven end to end exactly once, by `accessibility-test-plan.md`'s AC-14, on dev, under a WCAG criterion that could hold while the feature was functionally broken. Placed here rather than in a flagship (option A, WJ) because reuse is funder-agnostic mechanics and this plan already runs on a pre-seeded account. **RT-17 (two users at the same time)** was raised by WJ: **every result in this suite and every other plan was earned with exactly one user on the system.** `NFR-03` commits to ~10 concurrent users at launch with "no cross-user interference" and that has never been tested above one. RT-17 tests two — the smallest number that can reveal a cross-user fault. **A 10-user load test remains uncovered and is deliberately out of scope** (`GAP-113`). ⚠️ **The plan moves 🟢 → 🟡 on the dashboard.** It was "all 15 cases pass"; it is now 17 cases with two never run. Recording that honestly is the point — a plan that gains a case it has not run is not still all-pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2.15    | 2026-08-14 | Rapidglobe Ltd | **RT-09's outstanding manual re-run is complete — D-015/`GAP-41` closed, RT-09 returns to a clean Pass.** WJ opened a real exported `.docx` in Word after completing the Stony Stratford revised-form retest, using genuinely multi-line, multi-paragraph narrative answers (the form's own structure produces this naturally). Paragraph breaks, blank lines and bulleted-line layout all reproduced correctly. This was the one open item this plan carried since 2026-08-06 — no automated test opens a finished document in Word, so only a human re-run could close it. `TEST-DASHBOARD.md` row moves 🟡 → 🟢.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.14    | 2026-08-06 | Rapidglobe Ltd | **RT-14 gains an optional Storage-deletion add-on (`GAP-47`).** Account deletion now removes the user's `guidelines-temp` objects as well as their database rows; steps 1–8 cannot see that, because `/api/upload/process` empties the bucket seconds after extraction, so the add-on describes how to catch a file mid-flight (abandon Step 3 while the summary generates) and re-check the bucket immediately after deleting. Two cautions recorded with it: the Storage step deliberately does not fail the deletion, so a genuine Storage error is indistinguishable from a missing step in the UI — check Sentry before filing a regression; and `cleanup-guidelines` would remove the object within 30 minutes regardless, so the check is only meaningful immediately. The add-on also states that objects are **flat at the bucket root** with no per-user folders, since the folder layout previously documented in `data-model.md` never existed (`GAP-48`). No change to the mandatory steps, and RT-14's existing Pass stands. **Note on numbering:** this work was first written up as `GAP-39`/`GAP-40` and as v2.12 of this plan; both collided with a parallel session the same day, which had already taken those GAP numbers and v2.12. Renumbered here to `GAP-47`/`GAP-48` and v2.14.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2.13    | 2026-08-06 | Rapidglobe Ltd | **`GAP-41` fixed — D-015 closed in code, but RT-09 deliberately stays Pass (caveat) until a human re-runs it.** The Word export now emits one `TextRun` per line with `break: 1`. **The automated coverage goes all the way to the file**, which is the point: a test generates a real `.docx`, unzips it and counts `<w:br/>`, and the old single-`TextRun` code was confirmed to produce **zero** — the raw newline sat inside one `<w:t xml:space="preserve">`, which WordprocessingML treats as whitespace rather than a break. Any assertion stopping short of the packed file would have passed against the broken code, **which is exactly how this case passed over the bug on 2026-07-28**. RT-09 is not flipped to a clean Pass because no automated test opens the finished document in Word, and the original defect was found by a person reading her own download. A second defect was found and fixed in the same pass — `GAP-46`, the AI refine prompt actively instructing the model to reflow the applicant's layout, which would have flattened the answer one step earlier and made this fix look unreliable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.12    | 2026-08-06 | Rapidglobe Ltd | **RT-09 and RT-10 gained an explicit multi-line fidelity check, and RT-09's Pass was downgraded to Pass (caveat) — it passed straight over a live defect.** `GAP-41`/D-015: the Word export puts each answer into a single `docx` `TextRun`, which discards every `\n`, so blank lines and hyphen bullets an applicant typed arrive as one continuous block. Found 2026-08-06 by WJ reading a **real downloaded document** from his wife's genuine Stony Stratford application — not by running this plan, which had passed the same code on 2026-07-28. The reason it passed is instructive and is now written into the case: the verification list said "no corrupted or missing content", and single-paragraph test answers satisfy that while every line break is being silently dropped. RT-09's new step now **requires** a multi-line answer with blank lines and a bulleted list; RT-10 gained the same check as the comparison case, since its `format=txt` path joins an array of lines and was never affected — a divergence between the two formats is the fastest signal of a regression in either. Neither defect fixed; logged only, per WJ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.11    | 2026-07-30 | Rapidglobe Ltd | **RT-14's step 4 no longer hardcodes the author's full local OneDrive path.** Found while sweeping for the retired "AI Grant Accelerator" working title (Opus audit L1) — the path contained the old folder name, which is how it surfaced. `AGENTS.md` §4 forbids exposing the full OneDrive path in documentation precisely because it is machine-specific: any other contributor following this step literally would `cd` into a directory that does not exist on their machine, and `DR-BM-002`'s succession assumption depends on these plans being followable by someone who is not WJ. Replaced with "the repository root — the directory containing `supabase/` and `package.json`". No change to what the test actually does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.10    | 2026-07-28 | Rapidglobe Ltd | RT-15 closed — WJ confirmed both D-013 and D-014 fixed with the shortened diagnostic timer (modal stays open and clickable; wording correct). `WARNING_MS`/`TIMEOUT_MS` reverted to 55/60 minutes in `components/session-timeout-provider.tsx`, no diagnostic-only code remains. 15 of 15 test cases in this plan now Pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.9     | 2026-07-28 | Rapidglobe Ltd | With the diagnostic shortened timer running, two real defects found and fixed same day: **D-013** (Blocking) — the warning modal dismissed itself the instant the mouse moved toward its buttons (activity listener didn't check whether the modal was already open), meaning it was never actually clickable; likely present since the modal was first built. **D-014** (Cosmetic) — warning text rendered "1 minutedue to inactivity" due to a JSX line-wrap whitespace-trimming gotcha, fixed by switching to a template literal. Both fixed in `components/session-timeout-provider.tsx` / `session-timeout-modal.tsx`. Result remains Investigating — the shortened timer stays in place pending a clean re-test confirming both fixes and the underlying sign-out logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.8     | 2026-07-28 | Rapidglobe Ltd | RT-15 fresh scripted re-run (Chrome) failed to reproduce either the warning modal or the sign-out. Leading theory is Chrome throttling/discarding background-tab timers, unconfirmed. `WARNING_MS`/`TIMEOUT_MS` in `components/session-timeout-provider.tsx` temporarily shortened to 1/2 minutes (diagnostic only, to be reverted) so the sign-out logic can be verified independently of the throttling theory. Result changed from Pass to Investigating pending re-test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.7     | 2026-07-28 | Rapidglobe Ltd | RT-01 through RT-14 all executed and passed in one full session. Two plan-text staleness issues found and corrected: RT-03's step 1 element list was missing the "Ineligible" dashboard counter (added with `DR-EL-001`); RT-11's step 2 said "Click View" but the app now shows "Re-open" for an approved/exported application (matches the already-documented `FR-17` rename). RT-14 (delete account) is the first fresh confirmation since the 2026-07-23 `service_role` grant fix — passed cleanly on `grant-pathway-dev`; `grant-pathway-prod` remains unpatched, same outstanding gap. Only RT-15 (session timeout) remains — a fresh re-run was in progress at the time of this update; its existing 2026-07-04 Pass stands independently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2.6     | 2026-07-28 | Rapidglobe Ltd | RT-01b executed and passed — confirmed as a byproduct of `eligibility-check-test-plan.md`'s EL-01/EL-03 testing rather than a dedicated run. Both the found path (valid registration number pre-fills) and not-found path (invalid number falls back to manual entry, no crash) confirmed working.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 1.0     | 2026-06-15 | Rapidglobe Ltd | Initial regression test plan. 10 test cases across 2 tiers. Derived from Alan Knox Automated Testing audit and cross-referenced with user guide v1.14.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.1     | 2026-07-01 | Rapidglobe Ltd | Added Tier 0 (RT-00 environment/schema verification) after discovering `grant-pathway-dev` and `grant-pathway-prod` were both missing the AI-cap RPC schema (and prod was also missing the approve/reopen RPC) for weeks — invisible from inside the app, only found by querying the database directly. Added RT-11 (dashboard reopen), the only test covering `reopen_application`. Annotated RT-05 and RT-09 with their RPC dependencies. Noted that this plan has zero recorded executions and that every historical funder test result predates the 2026-06-22/06-29 RPC introductions, so none of them are valid evidence the current codebase works end-to-end. 11 test cases across 3 tiers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2.2     | 2026-07-03 | Rapidglobe Ltd | RT-00 executed for the first time — Pass, confirmed against `grant-pathway-dev`, run ahead of MKCF Oak Grants testing. Updated Status line and zero-executions note accordingly; RT-01–11 remain unrun.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2.3     | 2026-07-04 | Rapidglobe Ltd | Added RT-12 (Change Password), RT-13 (Sign Out), and RT-14 (Delete Account) — account housekeeping had no regression coverage at all (guide Section 15, "Managing Your Account"). RT-12 mutates the shared regression account's password and includes an explicit revert step. RT-14 requires a disposable throwaway account, never the shared regression account or an in-use funder test account, since deletion is permanent. 14 test cases across 3 tiers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2.4     | 2026-07-04 | Rapidglobe Ltd | Added RT-15 (Session Timeout — Inactivity), covering the documented 60-minute inactivity timeout (ADR-SEC-003, NFR, FR-06) that had no test coverage. Recorded as Pass based on a genuine live occurrence during Clothworkers testing — WJ was away over an hour, was signed out as expected, and on signing back in the in-progress application was exactly as left. The 55-minute warning modal itself was not directly observed this time (WJ was away from the screen) — flagged as unconfirmed in RT-15's notes, only the 60-minute sign-out and state preservation are confirmed. Also noted the user guide has no session-timeout section — a documentation gap, not a product defect. 15 test cases across 3 tiers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2.5     | 2026-07-24 | Rapidglobe Ltd | Added RT-01b (Charity Commission lookup — found and not-found paths), per `DR-TEST-001`'s capability-based restructuring — this account/profile mechanic had no dedicated coverage anywhere in the suite; RT-01a only exercised the found path incidentally. 16 test cases across 3 tiers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
