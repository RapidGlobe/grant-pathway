# docs/overview/assets — images for the external Business Overview

**Tier:** 3 — Stable
**Volatility:** Low
**Update when:** The process diagram or either logo changes

## Files

| File                                    | What it is                                                                                                                                                                |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `grant-pathway-process-overview.svg`    | **Source of truth for the process diagram.** Hand-authored, editable text, logos embedded as base64 so the file stands alone. Edit this, then re-export the PNG below.    |
| `grant-pathway-process-overview-v2.png` | 3200 × 1660 export of the SVG at 2×. **In use** by the live overview (v1.19), in both the `.md` and the `.docx`.                                                          |
| `grant-pathway-process-overview.png`    | The **old** AI-generated diagram. Superseded and **no longer referenced by the live overview** — retained only pending a repository-wide check for other uses. See below. |
| `grant-pathway-logo.png`                | Grant Pathway logo, 1562 × 560, transparent background — **the full-resolution master.** See below.                                                                       |
| `rapidglobe-logo.png`                   | RapidGlobe Ltd logo, 1600 × 896, opaque navy plate                                                                                                                        |

## The two Grant Pathway logo files, and why both exist

There are two copies of the same artwork in the repository, and the distinction matters:

| File                                          | Size       | For                                                                                                                           |
| --------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `docs/overview/assets/grant-pathway-logo.png` | 1562 × 560 | **Master.** Documents and print — the overview's `.docx` title page renders it about 900 px wide, so the resolution is needed |
| `public/images/logo.png`                      | 625 × 224  | **Web derivative.** The app nav, which renders it at 156 × 56 (`components/logo.tsx`)                                         |

They were briefly identical, and on 2026-07-31 deleting the docs copy as a duplicate was considered. It was kept instead, because the web copy was optimised down to 625 × 224 / 12 KB in the same pass — after which the docs copy is the only full-resolution logo in the repository outside binary `.docx` files. **If the brand artwork is ever replaced, update both**, and re-embed it in `grant-pathway-process-overview.svg` (which carries its own base64 copy so the SVG stands alone).

## Why the old PNG is still here

`grant-pathway-process-overview.png` was generated by an image model and cannot be edited — its text is pixels. That is how it came to carry **"(User confirms accuracy, the funder funders accuracy..)"** through several versions of a document intended for external distribution, along with a dangling "AI", two unfinished ellipses, a "Free for small UK charities" line that contradicted the document's own audience definition, an imitation of the Microsoft Word logo, and no depiction of the eligibility hard stop. The live overview no longer references it as of v1.19 (2026-07-31); it is kept only until a repository-wide check confirms nothing else does.

The lesson worth keeping: **do not use generated raster artwork for anything containing text that has to be correct.** Nobody proof-reads an image.

## Re-exporting the PNG after editing the SVG

There is no `rsvg-convert` or Inkscape on this machine, and ImageMagick's SVG delegate renders text poorly. Headless Chrome is the reliable route, but it will not write a screenshot from a `file://` URL, so serve the folder first:

```bash
python -m http.server 8931
```

Then, from PowerShell (adjust the output path):

```
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --disable-gpu --no-sandbox --hide-scrollbars --user-data-dir=%TEMP%\chromeprofile --force-device-scale-factor=2 --window-size=1600,830 "--screenshot=grant-pathway-process-overview-v2.png" "http://localhost:8931/grant-pathway-process-overview.svg"
```

`--force-device-scale-factor=2` is what produces the 3200 × 1660 output; the SVG's own viewBox is 1600 × 830. A `--user-data-dir` is required — without it Chrome fails silently with exit code 0 and writes nothing.

## Editing the SVG

Plain XML, commented, using the `BR-02` palette from `docs/app-name-and-branding.md`. Two things to know:

- **SVG has no text wrapping.** Each line of body copy is its own `<tspan>` with an explicit `dy`. If you lengthen a line, re-break it by hand and check the render — text will happily overflow its card.
- **XML comments cannot contain `--`.** Separator lines use `=` for that reason.
