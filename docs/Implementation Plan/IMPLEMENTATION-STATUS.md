# Grant Pathway v1 — Implementation Status

**Tier:** 1 — Always check after every task
**Volatility:** High
**Update when:** After every task — mark complete, update counts, add notes for deviations

**Phases 0–5 and Phase 6 task P6.1 are complete and archived in [IMPLEMENTATION-STATUS-ARCHIVE.md](IMPLEMENTATION-STATUS-ARCHIVE.md). This file tracks Phase 6 (P6.2a onward) and all future work.**

**Known accepted state (2026-07-25):** `master`'s `audit` CI check is currently red — a freshly-disclosed `brace-expansion` advisory (via `eslint`/`eslint-config-next`, both devDependencies) has no fix available short of the ESLint 10 upgrade already deferred in PR #70. WJ confirmed (2026-07-25) leaving it red is fine for now — he's ignoring the resulting GitHub vulnerability-alert emails — and this will be tidied up alongside other pre-launch cleanup around the prod push next week, not before. Not a live incident; do not re-investigate unless it changes.

**Last updated:** 2026-08-07 (**AC-01's results banked, two days after they were gathered, and `GAP-49` raised — this project's first WCAG **Level A** failure.** `accessibility-test-plan.md` **v1.2 → v1.3**; AC-01 marked **Fail**; the plan **🟡 → 🔴** on `TEST-DASHBOARD.md` (**v2.24**), leaving the suite at 3 🟢 / 1 🟡 / 3 🔴. **No task status changed and no code was written today** — this was a recording pass. AC-01's first run swept rows 1–20 of 24 on 2026-08-05 and **nothing had been written into the repository**: the results table was empty checkboxes and the findings sat in a session scratchpad under the OS temp directory, which is cleared without warning. They survived by luck. **Four defects logged.** `DEF-01` — one design token, `#94A3B8`, failing AA contrast on all five backgrounds it is used on (2.44 / 2.56 / 2.47, plus two status pills at 2.86 and 4.41); **that is one decision for WJ, not five code fixes, and is deliberately left open.** `DEF-02` — one component, six instances: the 16×16 show/hide-password eye button on sign-in, `/register` and `/account`. **`DEF-03` — `scrollable-region-focusable`, WCAG 2.1.1 Level A, now `GAP-49`**: the Step 4 guidelines citation pop-up (`.max-h-[60vh]` in `application-step4-draft.tsx`) has no `tabIndex`, so a keyboard-only user can open it, read the first screenful, and reach nothing below the fold. **The dialog exists solely to show the applicant the funder's original wording — for that user it does not degrade, it fails at the one thing it is for.** Filed 🟡 against `P5.3`'s existing axe bullet; likely fix is `tabIndex={0}` plus `role="region"` and an accessible name, to be done alongside **AC-05**, the focus-management case for the same dialog and the register's only 🔵 `ADR-OPS-006` row. `DEF-04` — the `@axe-core/react` auto-reporting that has never worked, which feeds `GAP-15`. **Coverage is partial and that only cuts one way**: twelve rows unswept and two `incomplete` findings still needing a human verdict, so run 2 can add defects and cannot remove any. **Five wrong instructions in AC-01 itself corrected**, each of which cost real time on the day: `Ctrl`+`Enter` does not run a DevTools snippet once focus has left the editor and fails silently; the snippet's `copy()` never fired, because Console Utilities do not exist inside a snippet; row 9 sent the tester hunting for an application-row menu that does not exist; row 17 asked for two sweeps of a Step 2 whose halves are visible together; row 5 needed a note that the sweep prints `/verify-email` for `/verify-email/confirm`. Incognito is now a prerequisite (extensions inject into password fields), and the snippet finishes all CSS animations before measuring, after one contrast reading turned out to be a button caught mid-fade in a background tab. **The process finding, not the defect, is why this is recorded at length:** a Level A failure was known on 2026-08-05 and had no number, no register row and no task until today, while both `ADR-TRACEABILITY.md` and `TEST-DASHBOARD.md` read as current — the dashboard actively described the plan as "not yet executed" on 2026-08-06, a day after it had failed.)

**Previously (2026-08-06):** (**`GAP-48` fixed — the `guidelines-temp` storage RLS policies had denied every request since 2026-05-19, gating on a folder layout the app never wrote; repointed onto the flat `{user_id}_` prefix (decision: WJ), 🔵 code-complete but never observed to grant in either form, and the migration still needs applying to both Supabase projects. Nothing was exposed — service-role access was always the real control. See the Notes entry below.** **Earlier, from a separate session the same day: `GAP-47` built and `GAP-48` raised — account deletion now removes the user's `guidelines-temp` Storage objects, which `PDR-DH-002` has claimed since 2026-04-16 while neither deletion path did it; and the bucket's three storage RLS policies turn out to have never granted anything. ⚠️ Both were first numbered `GAP-39`/`GAP-40` and collide with the two rewritten in package C below — that session worked in a worktree branched from `53ede57`, where GAP-38 was genuinely the highest number visible. Renumbered to 47/48; see the Notes entry below and `ADR-TRACEABILITY.md` v2.29 for the process lesson.** **Then, separately — nine items from one real, live grant application. Seven built and verified, two code-complete but unverified.** 🔵 **Package C — `GAP-39` and `GAP-40` rules rewritten, deliberately NOT signed off.** `GAP-39` is fixed at the level of the principle rather than with a third exception: the exclusion list was about **administrative identity and contact details** but read as though it were about **answer length**, which is how three questions were dropped across three funders. The first two were each closed with a budget-shaped exception — **being the sole exception is exactly why the third had nothing to appeal to.** Budget/cost is now a **case** of the rule, and the TABLE FORMAT skip-list, which carried its own copy of the flaw, is subordinated to it. `GAP-40` supplies what the existing "DO NOT MERGE" rule lacked: neither of §10’s sub-parts reads as a complete question alone, so merging looked like the only coherent option — the model is now told to **combine the shared stem with each sub-part** instead. **9 new prompt tests, suite 176 → 185 — every one asserts a rule is _present_, not what the model _does_ with it.** ⚠️ **A live extraction was attempted and could not run: `.env.local`’s AWS credentials are rejected by Bedrock (403 signature mismatch, correctly shaped, almost certainly rotated since 2026-08-04). This blocks all local AI testing and is worth fixing on its own account.** GCM-06 stays **Fail**, the capability matrix stays **🔴**, and a re-run must also confirm GCM-01–05 still pass — a rule loosened to stop dropping short answers is exactly what might start extracting contact details instead. ⚠️ **`ADR-TRACEABILITY.md` was briefly truncated to empty** by a scripted edit (opened with `'w'`, raised before writing; a follow-up script then read the empty file and wrote it back), and the empty version reached commit `fd80101`. Caught at the start of package C and restored from `77dc577`; no other document affected. **Earlier today, all built:** `GAP-41`, `GAP-46`, `GAP-42`, `GAP-43`, `GAP-44`, `GAP-45`. Original entry follows. **Nine items, ✅ **BUILT: `GAP-41` and `GAP-46`.** The Word export was passing each whole answer to the `docx` library as a **single `TextRun`**, and that library ignores `\n` inside a run — so every blank line and hyphen bullet an applicant typed was silently discarded. Now one run per line with `break: 1`. **The tests go all the way to the file**: they generate a real `.docx`, unzip it and count `<w:br/>`, because the defect was invisible at the level of "did we pass the right string" — the string was always right. The old path was confirmed to produce **zero** breaks, the newlines sitting inside one `<w:t xml:space="preserve">` where WordprocessingML treats them as whitespace. Any assertion stopping short of the packed file would have passed against the broken code, **which is exactly how RT-09 passed over this on 2026-07-28**. **`GAP-46` was found by the check `GAP-41`’s task note demanded**: `buildRefinePrompt()` told the model to "Improve the **structure**, flow, and clarity" and never mentioned layout, so "Help me improve this" was licensed to reflow a bulleted answer into prose — which would have destroyed the layout one step earlier, in the database, and made the export fix look broken. Fixed in the same pass. **Suite 163 → 176.** ⚠️ **RT-09 still needs a human re-run** — no automated test opens the document in Word. **Still open: `GAP-39` and `GAP-40` (extraction rules, package C) plus the `FP-11` deferral.** Earlier today: **`GAP-42`, `GAP-43`, `GAP-44`, `GAP-45`** ✅ **BUILT: `GAP-42`, `GAP-43`, `GAP-44`, `GAP-45`.** Step 4's sub-heading now tells the user their answers are saved and they can close the page and continue from their dashboard, dropping the false "as you type"; every successful save shows a per-answer **Saved** tick that clears itself, governance `£` fields included; and the nav Help button deep-links to the current screen's help page, finally calling the `helpCentreUrl(path)` helper written for it and never once invoked with a path. **18 new tests, suite 145 → 163**; type-check, lint and `prettier --check` clean. **A lint failure caught a real bug here, not a style nit** — extracting the "Saved" logic to a helper made `exhaustive-deps` demand `doSave` as a dependency of the 60-second sweep effect, which would have **restarted the interval on every render**, quietly breaking the very background save this package exists to reassure users about. **All eight GitBook targets were fetched live and title-checked**, not inferred — a wrong path 404s where the old root landing at least worked. ⚠️ **Not verified in a browser: Step 4 and the nav sit behind sign-in and the author does not enter credentials** — the public nav and footer links were confirmed live as still opening the root, but **HT-06 and a look at Step 4 are WJ's to run.** **Still open from today: `GAP-39`, `GAP-40` (extraction rules) and `GAP-41` (Word export line breaks), plus the `FP-11` deferral.** Original entry follows. **Eight items, ⚠️ **`GAP-45` — the nav Help button always lands on the help centre root, whatever screen you are on.** Pressing Help from Step 4 should open "Writing and editing an answer". **The plumbing has been there all along and was never connected** — `lib/help-centre.ts`'s `helpCentreUrl(path)` was written for this and its header comment names the use case almost verbatim, but `nav-authenticated.tsx` links to the bare base-URL constant, so the deep-linking helper is unused at the one place that most needs it. **Slugs verified, not guessed** — the live GitBook sitemap read (21 pages) and the Step 4 target fetched to confirm it resolves, since a wrong path 404s where the current root landing at least works. Full route map recorded; two calls left for WJ (`/dashboard` has no clean match; whether public routes deep-link too). **⚠️ Maintenance risk owned rather than implied:** the help centre is an external GitBook, a rename silently 404s one Help button, **nothing in CI can catch it**, and no client-side fallback exists. `help-and-tooltips-test-plan.md` → **v2.1** gains **HT-06**, written now and marked **not runnable**, so the coverage cannot be lost between logging and building. `AC-FR-49-01` needs amending at build time. `ADR-TRACEABILITY.md` → **v2.25**. **Earlier the same day, all from the Stony Stratford application:** `GAP-39`–`GAP-44` and the `FP-11` deferral. **Seven items, ⚠️ **`GAP-44` unblocked — WJ withdrew `AC-FR-18-02`'s no-visible-indicator rule.** Offered two options he took the broader: **every** successful save is visibly confirmed, background sweep included. **His reasoning overturns the assumption the old rule rested on** — silence existed to avoid nagging the user, but against a real applicant's hesitancy to close a tab holding hours of writing that is the lesser concern: **silence reads as "nothing is happening", not "everything is fine"**, and the user who steps away mid-sentence _without_ moving focus is precisely the nervous user this is for, so the narrower option leaves exactly the wrong person with no signal. The old intent survives as a constraint on new **`AC-FR-18-05`**: unobtrusive, no focus theft, nothing to dismiss, visually distinct from the "Not saved." alert. **Amended as a class:** the rule appeared in **six statements across four live documents** and all six changed together — `acceptance-criteria.md` ×2, `PRD-Grant-Pathway.md` ×2 (→ **v0.67**), `design-requirements.md` §7.8, `ui-inventory-and-data-contracts.md`. Changing one would have been the **eighth** recorded instance of this project's fix-the-instance-not-the-class failure; the seventh was `GAP-43`, hours earlier. `ADR-TRACEABILITY.md` → **v2.24**. **Earlier the same day: `GAP-42`/`GAP-43`/`GAP-44` raised, `GAP-41`, `GAP-39`/`GAP-40`, and the `FP-11` deferral.** Original entry follows. **Six items, and no code changed on any of them — WJ is reviewing his wife's completed Stony Stratford submission and asked for everything to be logged first.** ⚠️ **`GAP-42`/`GAP-43`/`GAP-44` — Step 4 never tells a user they can stop and come back.** The Approve buttons and the "N of N questions approved" bar read as "finish this in one sitting". **The capability works and was simply never stated** — the dashboard's Continue link goes to `/applications/[id]/step/[currentStep]` and Step 4 rehydrates every answer and its approved state. Agreed wording (WJ): _"Answer each question below. Your answers are saved automatically. You can close this page at any time and continue from your dashboard."_ — "from your dashboard" deliberately, since return is to the _step_, not the question, and with 17 questions the user still scrolls. **`GAP-43`: the string being replaced was false and the PRD contradicted itself.** "Saved automatically **as you type**" is not the behaviour — writes are on **blur** plus a **60-second sweep**, no debounce, no `beforeunload` guard. The behaviour is correct (`AC-FR-18-03` accepts "at most 60 seconds of edits potentially lost") and the PRD's own FR-18 notes describe it accurately, **including a 2026-07-13 correction that disproved a debounce claim, fixed the implementation note, and never checked the user-facing string in §7 of the same file** — the **seventh** recorded instance of fixing an instance rather than the class. **`GAP-44`: nothing ever confirms a save succeeded**, so the new reassurance would be an assertion the screen never demonstrates; the only feedback is the "Not saved." failure path. WJ approved a visible confirmation, **but `AC-FR-18-02` mandates silence on background saves** — two resolutions written as **draft `AC-FR-18-05`** for his ruling, recommendation being to scope the indicator to blur/explicit saves so no criterion needs amending. **Do not build `GAP-44` until that is closed.** `PRD-Grant-Pathway.md` → **v0.66**, `ADR-TRACEABILITY.md` → **v2.23**. **Earlier the same day: `GAP-41`, plus `GAP-39`/`GAP-40` and the `FP-11` deferral.** ⚠️ **`GAP-41` (High) — the Word export destroys every line break in an answer.** No code changed on any of them — WJ is still reviewing and asked for everything to be logged first.** ⚠️ **`GAP-41` (High) — the Word export destroys every line break in an answer.** WJ's wife laid out her §4b answer (215 words) with blank lines, a hyphen-bulleted list, a "Funding to Date" block and a worked calculation; the downloaded `.docx` rendered all of it as one continuous run of prose. **Confined to the Word generator, which is what makes it cheap:** the `<Textarea>` stores `\n` intact, Step 5's preview renders correctly via `whitespace-pre-wrap`, and `format=txt` is unaffected because it joins an array of lines — but `app/api/export/[applicationId]/route.ts` puts the whole answer into a **single** `TextRun` in both its branches, and the `docx` library ignores `\n` inside a run. Agreed fix: one `TextRun` per line with `break: 1` on all but the first — a ~ten-line helper in two places, no schema change, no new dependency. **A test passed straight over it, which is its own finding:** `regression-test-plan.md` RT-09 ran against this exact code on 2026-07-28 and passed on "no corrupted or missing content" — a bar single-paragraph answers clear while every line break is being discarded. It was found by a user reading her own downloaded file, not by the plan whose job it was. RT-09/RT-10 gained an explicit multi-line fidelity step (**v2.12**, D-015), RT-09 downgraded to **Pass (caveat)**, re-run required once fixed. **`FP-11` — rich-text formatting considered and deferred by WJ**, on the reasoning that most charity workers paste into funder web portals which strip formatting regardless, so it only pays where a downloadable Word/PDF form is still required; fixing `GAP-41` already delivers line breaks, blank lines and hyphen bullets, which do survive a paste. Full implementation cost recorded in `future-phases.md`; **no DR raised**, since nothing in any ADR, PDR or requirement promises rich text. `ADR-TRACEABILITY.md` → **v2.22**. **Earlier the same day: `GAP-39` (High) and `GAP-40` (Medium).** No code changed, on WJ's instruction; both are logged and covered by a new test case, pending further review of the same application.** This is the first defect report on this project drawn from genuine user work rather than a scripted test: WJ's wife completed a real Stony Stratford Town Council application for Stony Stratford Community Larder through the live service, and reconciling her 17 completed answers against the source form found the app produced **2 governance facts + 15 questions** where the form has **17 narrative asks**. **`GAP-39`: §4e ("an accurate figure for the number of people in the parish the project will serve") and §4f ("for how long will the project run?") were never extracted at all** — dropped by `lib/prompts.ts`'s data-entry/numeric exclusion, whose only carve-out is the BUDGET/COST exception, so a question is being discarded for the sole reason that its answer is short. For a parish grant, beneficiary reach is close to the whole decision. **This is the third instance of that failure mode**, after MK Community Foundation and Idlewild on 2026-07-27 — and each of those was closed by adding its own narrowly-scoped exception rather than correcting the rule, which is precisely why a third flavour got through; the fix should be to the rule, not a third exception. **`GAP-40`: §10 a) and b) (six- and twelve-month achievements) merged into a single card**, against the prompt's own verbatim "DO NOT MERGE ADJACENT QUESTIONS" rule — Medium, because nothing is lost to this applicant, but a per-question word limit would make it material. The two share a file but need different remedies: one is an inadequate exception, the other an adequate rule not being followed. **Four things were checked and found correct, recorded so they are not re-investigated:** §1 "Purpose of application" is a bare heading with no ask beneath it and is legitimately absorbed by §2; §7's reserves-justification prose is present as the Reserves governance card's helper text (`PDR-AI-008`); every non-narrative item was excluded correctly (tick-list, budget table, address, contact block, documents checklist, both signature blocks); and the absent "Finances of Your Group" catch-all is out of scope rather than regressed, since `PDR-AI-010` governs `free_form` **sections** mode and this document extracted in **questions** mode. **Test coverage added the same day:** new **GCM-06** in `guideline-capability-matrix-test-plan.md` (**v1.5**) — a docx **application form** rather than guidance about one, the only fixture in the corpus exercising lettered sub-question flattening and short-answer narrative asks — recorded **Fail** and staying that way until both defects are fixed. `TEST-DASHBOARD.md` → **v2.18**, moving that plan 🟢 → 🔴, the **first red on the dashboard since its 2026-07-16 reset**. `ADR-TRACEABILITY.md` → **v2.21**, both GAPs ⚠️ open with no covering task. **The cost to the user, which is the whole point:** §4e and §4f will be blank when she transcribes onto the funder's real form, discoverable only by reading the original document — the exact work the product exists to spare her. No task status changed in any phase. **Also today, answering WJ's question about whether Phase 5 audits the gaps register: it does not, and neither did the phase gate — a retrospective and deliberately unsigned Phase 5 → Phase 6 row was added to `ADR-TRACEABILITY.md` (v2.31), and a new `P5.5` item 4 makes the audit a real pre-go-live task with its own line on the Go-Live gate (`IMPLEMENTATION-PLAN.md` v3.25). 8 📋 and 4 partial rows outstanding, plus four register defects recorded — `GAP-32` was never added to the register at all. **All four then fixed the same day (v2.32), and the register sorted numerically and given a colour-coded Status column. `GAP-32` was then scoped properly and split three ways (v2.33) — its own description turned out to describe work finished four weeks earlier, and the real work was six other documents. Register now: 48 rows, 31 🟢 · 8 🟡 · 5 🔵 · 4 ⚪ · 0 🔴.**)

**Previously (2026-08-05):** (**Six things today. (1) `GAP-25` built — `P5.3b` item 1, ordered first because it is a security gap, not polish.** `ADR-ARCH-003` requires Zod on **all** Server Actions; only `charity.ts` had it. Now: **all 15 input-taking actions in `applications.ts` and all 7 in `auth.ts`** (`createApplication` and `signOut` take no input), shared schemas in new **`lib/validation.ts`**, **26 new unit tests** — suite 101 → **127**. **The password policy had four separate definitions** (three client components plus NFR-04 prose) and the PRD records a live client/server divergence being fixed once already; it now has one, with a test asserting the exact message so a future drift fails CI. The three client copies are deliberately left as a follow-up rather than mixing a UI refactor into a security fix. **⚠️ An ownership gap was found and closed in the same pass, and it is the more interesting half.** The mandatory `AGENTS.md` §1 doc check surfaced the Next.js warning that schema validation "only checks the shape of the input" — so entitlement was checked across all 24 actions too. 22 were already sound (`.eq('user_id', user.id)` throughout). **Two were not:** `saveManualAnswer` and `addManualGovernanceItems` **insert** `application_items` rows using a caller-supplied `application_id`, and that table's RLS keys only on `user_id`, so a row could be attached to another charity's application. **Precisely: integrity and availability, not disclosure** — SELECT/UPDATE policies both filter on `user_id` so a victim's rows stay invisible and untouchable, but `unique (application_id, item_order)` is a table constraint enforced independently of RLS, so a caller could occupy a victim's item slot and break their own upsert. New `assertOwnsApplication` closes it; **`GAP-17` in P5.2 now has a specific case to verify.** Also caught by auditing rather than assuming: `saveApplicationStep1` validated its input then wrote the **raw** values — which matters because P6.5's reuse lookup matches on a _trimmed_ funder name, so an untrimmed save would silently stop that application ever being offered for reuse. **Verified:** type-check, lint, 127 tests and `prettier --check` all clean, and **sign-in re-checked live** against the running dev server (valid input still reaches Supabase and returns the correct generic anti-enumeration message, no console errors). `ADR-TRACEABILITY.md` → **v2.19**, `GAP-25` 📋 → ✅. **(2) The legal effective dates are set — 5 August 2026 — closing audit finding S2a.** Both documents had read `[TO BE CONFIRMED]` since first publication, rated **Severe** by the Opus audit. Eight replacements across four files: the two internal copies and the two `-external.md` mirrors, the mirrors being what `/privacy` and `/terms` actually render, so the internal files alone would have changed nothing a user sees. Privacy policy → **v1.6**, terms → **v1.5**. **Dated from when the current text takes effect, not from an unscheduled go-live** — the service is already live and in use by external testers, so a future date would have claimed no policy was in force while people were using it. **S2b (the independent solicitor review) remains open and is now the only thing holding `P5.1`.** ⚠️ **The four generated PDFs/Word files in `docs/legal/pdf/` are now stale** (still `[TO BE CONFIRMED]`, one version behind) and must be regenerated before they go to a reviewer, or the reviewer receives the exact defect just fixed. **(3) `P5.0` requirements reconciliation executed — the blocking Phase 5 task. Register: `pre-launch-reconciliation-2026-08-05.md`. 23 divergences, none undispositioned: 15 fixed/amended the same day, 5 held for WJ's decision, 3 accepted and recorded.** `P5.0` is **not yet complete** — it cannot close until the five decisions are taken. Documents changed: `non-functional-requirements.md` → v1.2 (gained its **first-ever revision history**), `PRD-Grant-Pathway.md` → v0.65, `BRD-Grant-Pathway.md` → v0.55, `technical-design.md` → v1.25. **The completion flags now exist** — both the PRD and BRD had been fully reviewed against live code and neither said so, the PRD's only status words being two `(in progress)` phrases 36 versions stale, which is exactly how Phase 5 work came to be believed outstanding when it was done. **⚠️ The finding that needs acting on before `GAP-05` is built: `NFR-05`/PRD §12.5 promise a 320px minimum and mobile usability, while `ADR-ARCH-005` sets 1024px and `GAP-05` will block the UI below 768px** — and the sweep found this exact conflict was formally resolved once in the retired plan (OI-07, "ADR takes precedence") and **the resolution never reached the live document set**, making it the sixth and worst instance of fixing the instance rather than the class. Also: **a third meaning for ✅ was found — "present but inert"** (`GAP-14`'s axe harness), the **£100 budget means three different things and is already exceeded by committed `P5.4` spend**, and the **tooltip count is 10 in code against 9 in the test trail**. **Three decisions taken the same day (WJ) and applied: (1) viewport — block below 768px, 768–1023px functional but not optimised, mobile not supported in v1**, `ADR-ARCH-005` amended to state all three bands (resolving its own 1024px-vs-768px contradiction), `GAP-05` unblocked, **eight** live documents corrected rather than the five first identified; **(2) ten tooltips not nine** — a miscount, HT-02's Pass stands, corrected across five documents; **(3) R-09 withdrawn as an error in the register**, not a defect in `PDR-UI-008`, which was right throughout. **The sweep's sharpest find came after the decision: `IMPLEMENTATION-PLAN.md`'s own discrepancy row D24 had already recorded this exact viewport conflict and resolved it the _wrong_ way** — "must be usable on mobile browsers per C16 and NFR-05 (minimum 320px)", overriding the ADR it cited, while the retired plan's OI-07 resolved it the opposite way. It was not that nobody noticed; **it was noticed twice and settled inconsistently, and the live record was the wrong one.** **(4) Budget — `C1` raised from £100 to £150/month and confirmed as a _total_**, covering the ~£136 committed with ~£14 unallocated; the $127 AWS Budget is unchanged, so ≈£100 is now part of the budget rather than all of it. Applied across **13 statements in 8 documents**, including PRD §10's "total API spend" and §14's "AI API spend" — the two readings that were the actual divergence. **✅ `P5.0` is COMPLETE: all 23 divergences dispositioned and applied, and `P5.5` is no longer blocked by it.** **(4) `GAP-31` built — `P5.3b` item 2, sequenced here because it needs a migration and `P5.4` pushes migrations to production.** Both halves of the 2026-06-08 Knox findings, and **both were worse than the register described.** **The inactivity warning was sending about thirty emails, not two.** The register said "if Vercel fires the cron twice users receive the warning email twice" — but the eligibility test is a month-wide _range_ (`>=23 months ago && <24 months ago`) checked by a **daily** cron, so no double-fire was needed: a charity in that window got _"your account will be deleted in 30 days"_ on roughly **thirty consecutive mornings**, and the "30 days" was only true on the first, since the deletion date shown is computed from their last sign-in and kept drawing closer while the subject line did not change. **`email-notifications.md` already said "only one inactivity warning email is sent per inactivity cycle"** and `PDR-DH-002` says it singular — so this was a plain spec violation, not an edge case. Guard: new nullable `user_profiles.last_inactivity_warned_at` (migration `20260805000000`), compared against `auth.users.last_sign_in_at` so it self-heals when a user signs in and **adds nothing to the sign-in path**; stamped **after** the send, so the failure mode is one repeat rather than a silent skip — a duplicate email is recoverable, a deletion with no warning is not. **The second half: the deletion cron could delete in complete silence.** `lib/emails/send.ts` returns **without throwing** when `RESEND_API_KEY` is unset, so in the one configuration where every Email 4 fails, the route's `try/catch` never fired at all — the audit's stated worst case (deleted with zero communication) was one unset environment variable, not a coincidence. Now a **503 preflight that aborts before deleting anything**, matching `/api/account/delete`, plus Sentry on send, auth-delete and no-address-on-file failures in both crons and in the user-initiated route — whose header comment had claimed Sentry logging that did not exist. Both eligibility tests extracted to new `lib/inactivity.ts` (`subMonths` was duplicated verbatim, and the property that matters belongs to the _pair_: the two crons run an hour apart against the same user list and **no user may ever be claimed by both**, now asserted across 800 days of sign-in dates). **18 new tests, suite 127 → 145**; type-check, lint and `prettier --check` clean. `ADR-TRACEABILITY.md` → **v2.20**, `GAP-31` → ✅, and **its ADR column corrected** — it cited `ADR-AI-010` (summary pre-processing) and `ADR-OPS-001` (Vercel plan tier), neither of which addresses cron reliability. **⚠️ One thing left for WJ: the migration is not yet applied to `grant-pathway-dev`.** `supabase link --project-ref` needs an interactive database-password prompt, the same constraint recorded on 2026-06-29 — see the Notes row below for the command. Until it runs, the warning cron returns 500 with a Sentry event against dev, which is the loud failure it was built to have. Phase 5 now stands at **9 tasks, 2 done** (`P5.PERF1`, `P5.0`), plus GAP-24, GAP-25 and GAP-31 built out of `P5.3b` — leaving GAP-22, GAP-21 and the deferrable GAP-23. No Phase 6 task status changed. **(5) The four legal PDF/docx files in `docs/legal/pdf/` were found not to open — reported by WJ, needed urgently to send to a solicitor — and were regenerated.** All four passed structural validation on inspection (valid PDF xref/trailer, `pypdf` extracted text from every page, `docx` zip integrity clean) — the corruption was not reproducible from the repository side, so the cause is somewhere downstream of git (viewer, transfer, or the copy WJ had open) and was not chased further given the time pressure. Regenerated via the established pandoc → `.docx` → Word COM → PDF pipeline from the current external source markdown (no content change), verified page-by-page after conversion, committed as `54459de`. **(6) Terms of Service restructured to v1.6, at WJ's request, ahead of solicitor review.** The no-guarantee-of-funding-success sentence in Section 5 — _"We make no representation that using Grant Pathway will result in a successful grant application"_ — got its own `### No Guarantee of Funding Success` sub-heading, to stop it reading as incidental to two unrelated paragraphs either side of it. The AI-provider/data-processing paragraph moved earlier in the section so it does not end up nested under the new heading. No wording changed. Applied to `terms-of-service.md` (new v1.6 changelog blockquote) and `terms-of-service-external.md` (what `/terms` renders — verified live: heading confirmed directly above the guarantee paragraph), PDF/docx regenerated (`Grant-Pathway-Terms-of-Service-v1.6.{pdf,docx}`, 7 pages, superseded v1.5 files deleted), and `CHANGELOG.md` gained its own entry. Privacy Policy untouched — out of scope for this request, and it has no equivalent statement to relocate.)

**Previously (2026-08-04):** (**Pre-AC-01 environment verification at WJ's request. No Phase 5 or Phase 6 task status changed.** The development service is intact — Next 16.2.12 boots clean, all eight secrets present, `/api/health` green, auth round-trip reaching Supabase. **Two findings, see the 2026-08-04 Notes entry: (1) `@axe-core/react` is silently not reporting** — a route with two real AA violations produced an empty console, because `axe-provider.tsx`'s `catch` for a React 19 incompatibility is silent. **(2) `external-tester-brief.md` had no URL** and both obvious guesses are wrong — the `.org.uk` domain is still parked and `grant-pathway.vercel.app` is an unrelated product; the working deployment is `grant-pathway-three.vercel.app`, now named in the brief. **Then, same day: `accessibility-test-plan.md` → v1.2, AC-01 rewritten twice — first into an executable procedure, then into one a non-developer can actually run** — Step 0 harness gate, a saved DevTools snippet that drives `axe.run()` explicitly, a 24-row route table naming the conditional UI to render before each run (axe sees only the current DOM, so a passively-visited route reports clean on UI that never appeared — a second, independent route to a false Pass), seven separate Step 4 runs including the never-swept M8 banner, and the two known violations written in as a **reproduction check** rather than as examples. **Vercel production confirmed pointing at `grant-pathway-dev`** (WJ), closing the day's open question — recorded because the same question recurs at P5.4, when the answer is supposed to change.)

**Previously (2026-08-03):** (First session from the relocated working copy. **Three things. No Phase 5 or Phase 6 task status changed today** — P5.3 gained its output artefact but has not been executed, and P5.4 is no closer to done. **(3) `accessibility-test-plan.md` was created** — the seventh test layer under `DR-TEST-001`, 15 cases, with the NVDA setup section that was the actual blocker on the screen-reader pass; see the 2026-08-03 Notes entry for what it covers and the two findings that arose from writing it. The first two: **(1) The move was verified rather than assumed.** No `grant-pathway` clone remains under the user profile or either OneDrive root; the one surviving OneDrive folder is the parent deliberately kept for non-repo material and holds no `.git`, `node_modules`, `.next` or `.vercel`. Husky is still wired via `core.hooksPath` — a relocation can sever that silently — the tree is level with `origin/master`, and all four CI gates pass locally (`type-check`, `lint`, 101 tests, `format:check`). Both cleanups the 2026-07-31 sixth-pass entry deferred "for when the desktop app is closed" are closed: the stale OneDrive project entries are gone from the user-level config, and the single OneDrive path left in the gitignored local settings is not stale, since it points at that kept parent folder. **(2) `Schema Drift Check` now skips prod on scheduled runs until P5.4, at WJ's decision — see the 2026-08-03 Notes entry below for the reasoning and the M2 parallel.** The point to carry forward is the second-order one: the workflow goes green from now on **while production remains exactly as unprovisioned as it was**, and P5.4's exit test — "the `Schema Drift Check` workflow green and staying green" — would have been satisfied vacuously by that. It was tightened in the same pass to require the prod leg switched back on, and re-enabling it is now its own tasked P5.4 checklist item. **Do not read the green badge as prod progress.**)

**Previously (2026-07-31):** (WJ asked for the external Business Overview in `docs/overview/` to be reviewed before distributing it, given the recent gaps. **Reissued as v1.17** — `Grant-Pathway-Business-Overview-v1_17.md` plus the Word version, with v1.16 and the older v1.4 export moved to `docs/overview/archive/` behind a new README stating plainly that nothing in there is current, following the `docs/legal/archive/` pattern set the day before. **Six findings, four of them substantive.** **(1) The data-residency claim was an overclaim in a document about to go to charities and funders:** "All data is stored in UK-based infrastructure" full stop, when `privacy-policy.md` v1.5 Section 5 names **Vercel and Resend in the United States** and **Sentry in the EU**. Charity content genuinely is UK-only (Supabase London; Bedrock `eu-west-2`, never outside the EEA) — so the fix was to state that precisely and name the three supporting services that sit outside the UK under IDTA-equivalent safeguards, rather than to soften a sentence that was defensible for the part that matters. The external overview and the internal `business-overview.md` both had it. **(2) Grant discovery was still listed as a future Grant Pathway idea** — "helping charities find grants that match their work" — six weeks after the 2026-07-11 decision that discovery becomes a **separate, chargeable RapidGlobe service** and not a Grant Pathway feature (`BRD-Grant-Pathway.md` Section 8). Distributing that would have advertised a free future feature that is now a paid separate product. Removed, replaced with an explicit "Grant Pathway is, and will remain, a writing tool." **`FP-01` in `future-phases.md` still frames discovery as a possible Grant Pathway phase and has not been reconciled** — flagged in `business-overview.md`, left for WJ rather than rewritten unasked. **(3) Three built, user-visible behaviours were described nowhere:** the Charity Commission register lookup that pre-fills the organisation profile (disclosed in the Privacy Policy the day before, but absent from the product description), the P6.5 "start from one of your own earlier applications" path, and — the omission that matters most to a reader deciding whether to use the service — **FR-47's eligibility hard stop**, where the tool tells the charity it is not eligible and refuses to take the application further. All three added. **(4) The review-and-approve paragraph appeared twice**, near-verbatim, one copy carrying a stray comma ("figures, dates, and facts, correct?") — an editorial defect visible on page 3 of the distributable. Merged into one. Also: the fair-use paragraph now points at the Terms for the current figure instead of leaving it unquantified (the Terms' own figure was wrong until 2026-07-30); deletion wording gained the seven-day backup-rotation qualifier so "completely and permanently" is not read against `privacy-policy.md` Section 7; and "the Privacy Policy **will** set all of this out" became the present tense, both documents having existed since 2026-06-10. **Method:** the Word version was produced by editing the existing `.docx` XML in place rather than regenerating it, deliberately — WJ hand-finished v1.16 in Word with the logo, title page and footer, and a pandoc regeneration would have silently dropped all of it. Schema-validated against the original, rendered via Word COM and checked page by page. An orphaned 1.2 MB image left in the file by Word's own editing was dropped in passing, taking it from 3.1 MB to 1.9 MB. **Two things left for WJ, neither actioned:** the process diagram (`assets/grant-pathway-process-overview.png`) asserts "DATA STORED IN UK, NEVER SOLD OR USED FOR TRAINING" as a key principle — defensible for charity content, but it is the same sentence the text has just been made more careful about, and it is an image, so it needs redrawing rather than editing; and the overview reads throughout as though the service is live, which it is not until P5.4/P5.6. **Nothing about Phase 5 or Phase 6 status changed today.** **Second pass, same day, now v1.18:** WJ read the reissued document and asked whether one paragraph — present since day one — was authentic and accurate: the jargon examples ("theory of change", "additionality", "outcomes framework") and "Two to three days of her life, every time." **Both failed checking.** The three terms appear in **none of the 23 real funder documents** in `docs/Grant Org Guidelines` (all extracted, normalised for line-wrapping, every extraction verified non-empty so the zeros are real), while "impact" and "safeguarding" appear in 13 each, "outcomes" 9, "core costs" 8, "unrestricted" and "sustainability" 6. Genuine sector jargon, but not the jargon our users' funders actually use — replaced with four verified terms that are still opaque to a non-fundraiser, with the counts recorded beside the paragraph so they are not reintroduced. The day count had **no source anywhere in the repository** — it traces to Persona 1, a composite with no research basis, and `DR-PS-001`'s rationale asserts the same premise with nothing behind it — and it drifted three ways ("a full weekend" in `tone-and-voice-guide.md`, "2-3 days" in the PRD, and the preceding sentence's "clears her diary for the weekend"). This matters because the funding model depends on approaching technology funders and digital inclusion programmes, who fund on evidence. **A properly sourced statistic was added in its place:** applying for grants costs UK charities at least £900m a year, and at least **17.5% of what the smallest charities raise from foundations** against ~4% for those above £1m — Giving Evidence (Fiennes, Bull, Sandford, 2022), funded by the **Law Family Commission on Civil Society**, cited in a footnote in the `.md` and a source note on the last page of the `.docx`. Two alternatives were rejected with reasons recorded: the Commission's own briefing says "more than a third" for small and medium charities, which cannot be reconciled with the underlying 17.5%/15% and looks like the brackets added together; and Brevio's much-quoted 2020 survey is a pandemic-period snapshot from a commercial platform. The narrative framing was deliberately kept — an illustration needs no citation, a number stated as fact does. **v1.17 was not archived**: it lived about an hour, went to nobody, and the archive is for versions that actually reached an external audience; superseded in place, wording preserved in commit `987c8fb`. "A twenty-page PDF" was checked too and left alone (real corpus: 4–54 pages, median 10). **Third pass, same day: the process diagram was rebuilt as an editable SVG.** WJ asked whether the infographic needed updating — it did, and worse than the "DATA STORED IN UK" line already flagged: the artwork carried **actual gibberish** ("(User confirms accuracy, the funder funders accuracy..)"), a sentence ending in a dangling "AI", two unfinished ellipses, a bracket repeating its own heading, "FREE FOR SMALL UK CHARITIES" contradicting the document's own audience definition, an imitation of the **Microsoft Word logo**, and a straight-line flow with **no exit** despite `FR-47` being able to terminate an application at Step 3. Root cause: AI-generated raster artwork — its text is pixels, so nothing could proof-read it and no line could be fixed without regenerating the image. **Durable lesson: never use generated raster artwork for anything whose text has to be correct.** Replaced by `docs/overview/assets/grant-pathway-process-overview.svg`, hand-authored on the `BR-02` palette, with the two real logos WJ supplied embedded, five step cards **matching the app's own Step 1–5 numbering** (the old four contradicted the UI), an amber eligibility exit off Step 3, and geometric icons that imitate no one's trademark. A fresh overclaim was caught in it before shipping — a draft said "every point linked back to the guidelines" when citations are optional per `ADR-DATA-007`; now "with links back to the funder's own wording". The suspected "RapidGiobe" misspelling proved to be a **scaling artefact, not a typo** — the supplied logo is correct. `grant-pathway-process-overview-v2.png` (3200 × 1660) is exported and ready but **deliberately unused**: WJ asked for no new overview version in this pass, having spotted further changes he wants, so the old PNG stays in place until the Word file is rebuilt. Both real logos are now saved as reusable assets. **Correction, same day:** this note originally added that the transparent-background PNG was what the outstanding logo-replacement item had been waiting for, because `components/logo.tsx` "still carries a placeholder" — **that was wrong and the error was mine**, repeating a stale session note without checking the code. The placeholder went on **2026-06-13** in commit `7626a9d`; the component has rendered the real asset (`/images/logo.png`, the same 1562 × 560 artwork) ever since, and nothing is outstanding. Found only because WJ asked what the swap would actually involve. **Fourth pass, same day: `public/images` tidied at WJ's go-ahead.** Three unused logo files deleted — the original Canva export (376 KB, no alpha), the white-wordmark variant (190 KB, added 2026-06-12 "for dark/teal contexts such as email templates"), and a `removebg` intermediate (49 KB) — all confirmed unreferenced by any code, config or template, and all previously served publicly at guessable URLs. The white wordmark was checked rather than assumed before deleting: **both navs are `bg-white`**, the teal appears only on buttons, and the emails contain no images at all, so the dark logo is right everywhere it is actually used. **`public/images/logo.png` cut 655 KB → 12 KB (98%)** — it was the 1562 × 560 master being served for a 156 × 56 render; now 625 × 224 and palette-reduced to 58 colours, chosen by comparing 16/32/64-colour variants on RMSE and confirming they are indistinguishable at 2× display size, with transparency verified intact. **The docs copy was kept, reversing what was offered:** once the web copy is optimised the two are no longer duplicates, and the docs one becomes the only full-resolution logo in the repository outside binary `.docx` files (the overview title page needs ~900 px). Both are now documented in `docs/overview/assets/README.md`. Type-check and lint clean, **101 tests pass**; the asset was rendered at nav size on white and on teal, but **the running app was not checked** — the dev server needs the eight Sensitive Vercel secrets this environment cannot supply. Tooling for future sessions is recorded in `assets/README.md`: headless Chrome is the only reliable SVG rasteriser here, it will not screenshot a `file://` URL, and without `--user-data-dir` it fails silently with exit code 0. **Fifth pass, same day: the new diagram is now in the overview — v1.19 is the live pair.** `.docx` edited in place again to preserve the hand-finished title page; `word/media/image4.png` swapped for the 3200 × 1660 export and **both `wp:extent` and `a:ext` heights recalculated** (2443941 → 2988374 EMU) because the new diagram's aspect ratio differs — leaving them would have stretched it vertically. The diagram now heads **page 4** rather than being squeezed at the foot of page 3; still 6 pages; file size 1.9 MB → 1.1 MB. **v1.18 not archived**, same basis as v1.17: hours old, distributed to nobody, superseded in place (`d455186`). **The old `grant-pathway-process-overview.png` was deliberately NOT deleted** — the live overview no longer references it, but a repository-wide check for other uses was still running, and since a `.docx` embeds its own copy of an image, a file can depend on the diagram without naming it anywhere (`archive/…v1_16.docx` is known to). Deleting on the assumption that nothing else uses it is the same partial-sweep pattern the audit kept finding. One cosmetic point left for WJ: the diagram's own logo and the page header logo both land on page 4 a few centimetres apart — correct for standalone use of the SVG, redundant inside this document.)

**Previously (2026-07-30):** (Second session of the Opus audit walkthrough, continuing from 2026-07-29. Agenda agreed with WJ: the Phase 5 restructure proposal first, section by section, then the ten Low findings one at a time. **Section-by-section review of `phase-5-restructure-proposal-2026-07-29.md` began, and every one of its claims was independently re-verified against the code rather than taken on trust** — §2.1's four accounting errors all confirmed (the live status doc still says "Phase 0–5 breakdown (all complete, 97 tasks)" while the archive itself marks Phase 5 "In progress" at 13 tasks/7 done; six of those seven are the funder-directory tasks reversed 2026-07-15, so real Phase 5 progress is one task, `P5.PERF1`; plan version 3.21 vs 3.17 recorded here), and §2.2's six unbuilt spec deviations all confirmed still unbuilt. **Two overstatements in the proposal itself were found and will be corrected when it is applied:** A3 says `grep P5.5b` across both status files "returns nothing" — it does appear, in a Notes row of this file (line 113), though the substance stands since it is in no status _task table_ and the gate does not name it; and §2.2 says "seven of its bullets are not accessibility work" while listing GAP-05 among them, when §3 then correctly keeps GAP-05 _in_ accessibility — six move to `P5.3b`, not seven. **Actioned immediately at WJ's request: GAP-24 built.** The export disclaimer had the service asserting, in the user's name and on a document sent to a funder, that _"All content has been checked for accuracy before submission."_ Nothing in the product checks accuracy. Now reads `PDR-DH-003`'s _"Please review carefully before submitting to the funder."_ The two sentences moved in opposite directions, deliberately: the second (the defect) moved code → spec, while the first kept the built _"This application was prepared…"_ and the **spec** was amended to match, because that wording was a considered 2026-06-01 change aligning it with the Step 5 checkbox the user ticks. One `disclaimer` constant serves both `.docx` and `.txt`, so both formats changed together; the Step 5 checkbox itself is untouched and was never at fault — there the user makes the statement about themselves. Type-check, lint and 101 tests pass; **not browser-verified**, as it needs a session through to Step 5 and a real export — both flagship test plans already carry a "disclaimer present and correctly worded" step, so it will be caught on the next run. **The register-wide problem this confirmed:** GAP-24's old status, `✅ 2026-06-16 — fix step added to P5.3`, meant _a task was written_, whereas GAP-26's `✅ Resolved (commit 372d95b)` means _the code exists_ — same column, opposite meanings. GAP-21, GAP-22, GAP-23, GAP-25 and GAP-31 all still carry the "tasked" ✅ and were each verified unbuilt today, and the **Phase 4 → Phase 5 gate sign-off** lists nine gaps as resolved on that basis, six of them unbuilt. Proposal item **C2** addresses this and is not yet approved, so the column is deliberately left alone and GAP-24's row now says **BUILT** in words instead. `ADR-TRACEABILITY.md` → v2.14, `PDR-DH-003` and the `P5.3` plan bullet updated. **Then approved and applied in full.** WJ's four decisions: restructure **approved with amendments**; **`P5.5b` post-launch** with the condition that day-one statistics must still be available; **Bedrock hard-stop IAM recorded as an accepted risk** (the per-user 50/month cap in `reserve_ai_slot` is the real control; an IAM revocation would cut AI off mid-application for every user at once); **all in one pass, C2 included**. `IMPLEMENTATION-PLAN.md` → **v3.22**, `ADR-TRACEABILITY.md` → **v2.15**, proposal → v0.2 and now a record of the review rather than the specification. **New blocking `P5.0`** at the narrowed scope described above. **New `P5.3b`** holding the six spec deviations, **ordered by risk instead of GAP number** — GAP-25 first because Zod is absent from two of the three files in `actions/`, which is a security gap rather than polish; GAP-31 second because it needs a migration and so must precede P5.4's production push or that push happens twice; GAP-23 flagged as the only one where nothing is actually wrong. **P5.3 narrowed** to real accessibility work, and it now states plainly that `@axe-core/react` cannot run against a deployed build — the thing that blocked HT-05 on 2026-07-25. **P5.4 gained an explicit exit test — the `Schema Drift Check` green and staying green** — plus the four items recorded elsewhere as belonging to it but never tasked, above all the **Supabase production redirect-URL allowlist**, which silently breaks email verification and therefore registration for every user on day one, plus audit **O11** (`SENTRY_AUTH_TOKEN` absent, so no releases and minified production traces) and **O12** (`onRouterTransitionStart`) — neither of which was actually in the proposal's §2.5 despite O11 claiming it was. **P5.5 rewritten** around the `TEST-DASHBOARD.md` layers executed against **production**, recording plainly that every 🟢 earned to date was earned against dev; two factual errors removed (a check on the `/api/generate-draft` route deleted 2026-07-01, and a 30-second summary target when NFR-01 says 45s for large documents, so as written P5.5 would have failed a run NFR-01 says passes); and **the proposed MKCF question-count re-baseline dropped** — that plan already carries 15, 16 and 19 in different places, so "12" would have been the next stale number. Counts become "observed N on <date>", with the assertion living in the pre-deploy prompt-change check added yesterday under M3. **P5.6** gained the real DNS values (`A` → `76.76.21.21`, add `www`), a verification step that the apex **serves the application rather than merely returning `200`** (a registrar parking page already returned `200` once and read as success), a `robots.txt` re-check, an effective-dates check, and the `v1.0.0` tag **moved here from P5.4**, which as written would have tagged the release before the service was live. **C1–C4 all applied:** the Phase 5 count corrected from "13 tasks, 7 done" to **9 tasks, 1 done** (six of the seven counted "done" were the funder-directory tasks reversed 2026-07-15 — struck through and recounted to zero, retained for the audit trail), `P5.5b` given a status row for the first time and marked deliberately excluded from the gate, the plan version synced, the stale archive line about the "empty" production Sentry DSN corrected in place with a note on the damage it did, the go-live gate restated as an explicit task list, and the **Phase 4 → Phase 5 gate row annotated rather than rewritten** — it lists nine gaps as resolved when only GAP-26 was built at the time; five remain unbuilt now that GAP-24 is done. **C2 implemented as ✅ = built / 📋 = tasked, with a symbol key**, chosen over splitting the column because the register already uses ➖ for accepted deviations: **sixteen rows reclassified**, and the six in the **ADR consequence tables** matter more than the ten in the register, because those are the rows a session reads during the mandatory `AGENTS.md` §2 check — a ✅ there actively asserted a consequence was discharged when no code existed. **GAP-20's status cell populated for the first time** (empty since 2026-05-20, the only such row, which is how a task nobody had started stayed invisible for ten weeks — audit L8). **New `supabase/queries/operator-statistics.sql`** meets WJ's P5.5b condition: ten read-only queries covering every dashboard panel plus feedback opt-ins and the application-status funnel, **all ten executed against `grant-pathway-dev` before being committed** — and testing them rather than transcribing them found **three of P5.5b's eight specified queries reference columns that do not exist** and would each have errored on first run (`last_sign_in_at` and `email` are on `auth.users`, not `user_profiles`; `ai_usage_log` has `request_type`, not `route`), all three corrected in the spec. **Also agreed: a separate `accessibility-test-plan.md`** as a seventh test layer and P5.3's artefact, to be built as its own task after the Low findings — `ADR-OPS-006` mandates a manual keyboard/focus/screen-reader/contrast pass before every release and no plan executed it for the product flow, the only accessibility case anywhere being HT-05, scoped to tooltips, whose single keyboard step is what found `GAP-38` (three of nine tooltips unreachable by keyboard, a real shipped WCAG 2.1.1 failure). **The NVDA/VoiceOver deferral was narrowed to formal sign-off only** — it had come to mean no screen-reader testing at all pending an engagement; WJ attempted NVDA this day and could not get it working as designed, retrying 2026-07-31. `ADR-OPS-006`'s manual list is also WCAG 2.1-shaped while P5.3 targets 2.2 AA, and the 2.2-specific criteria land on features already built and tested by nothing. **Eight errors were found in the proposal itself** by re-verifying every claim against the code, and are recorded in its new §8 — the material one being P5.0's original scope. **WJ also observed the documentation set may be outsized for the service; agreed, and deliberately left for its own decision once the audit is closed out rather than folded in here.** **Then all ten Low findings were worked through one at a time, each verified against the code before being actioned, and committed per finding — L1–L10 are now closed, and the audit's remaining open items are the observations O1–O12 (O8 already closed, O10 deliberately post-launch).** The recurring theme is worth stating once rather than ten times: **the audit undercounted almost every finding, because it reported the instance it found rather than the class.** L1 named one document with the retired "AI Grant Accelerator" working title; six had it. L2 named four locations claiming three AI routes; six were stale and a seventh had already been fixed by yesterday's M3 work. L5's "147 of 174 docs lack a Tier header" reads as 156 of 186 today and moves with every document added. Sweeping rather than patching is what found the rest, and is why `P5.0` now carries an explicit instruction to do exactly that — **a finding is closed when the class is fixed, not the instance** — with the six precedents listed in the task. **L1** — retitled six documents (`technology-stack.md`, `constraints-and-assumptions.md`, `DECISIONS-INDEX.md`, `user-personas-journeys-and-use-cases.md`, `vision-statement.md`, `v1-out-of-scope.md`), reordered `technology-stack.md`'s history, and deliberately kept the three correct references (the "Working title (retired)" branding row, the audit's own text, the new history entries). WJ's context: it was the pre-rename internal working title and the local folder could not be renamed afterwards, which is why it kept resurfacing. The sweep also found **`regression-test-plan.md` RT-14 hardcoding the full local OneDrive path** in a `cd` instruction — a direct violation of `AGENTS.md` §4 and unusable by anyone who is not WJ, relevant to `DR-BM-002`'s succession assumption. **WJ then asked whether `user-personas-journeys-and-use-cases.md` adds any value at all.** Answered with evidence: yes for the personas (`PDR-AI-008`, two weeks earlier, cites Persona 1's lack of fundraising training as the reason the governance-fact fallback behaves as it does, and `tone-and-voice-guide.md` §1 is written directly about Margaret and David) and **no for Part 2, the user journeys, which were deleted** — a journey map is a pre-build instrument, and once the product exists it becomes a competing description nobody updates, demonstrated by two journeys describing the funder picker for six weeks after `DR-FD-001` v1.4 removed it. UC-05 corrected in place, including its "cannot proceed with an unlisted funder" gate, which no longer exists in any form. That in turn exposed **31 stale `business/…` documentation paths across six documents, none of which resolved** — including five in Tier 1 `data-model.md` — fixed by hand rather than by find-and-replace because three targets had also been renamed or moved and a blind swap would have created two new broken paths; plus a second stale pair in `information-architecture-and-navigation.md` citing root-level legal files deleted on 2026-06-22 as "the authoritative source" when the routes read the `docs/legal/…-external.md` copies. **This class of error had been found and locally fixed twice before (2026-07-10, 2026-07-13) and swept neither time.** **L2** — corrected six places still asserting three AI routes; the two that mattered were `ADR-TRACEABILITY.md`'s `ADR-OPS-005` consequence row and `GAP-21`'s description, because those are what a session reads during the mandatory `AGENTS.md` §2 check, so whoever built GAP-21 would have been told to instrument `/api/generate-draft`, deleted a month earlier. Dated historical statements were deliberately left alone. Note the 2026-07-01 deletion **did** sweep the docs and recorded doing so — it searched for the route _path_, while these six described the route by _count_, so a sweep is only as good as the phrasings it looks for. **L3** — NFR-01's "investigate document pre-processing or streaming before go-live" recommendation resolved: pre-processing built 2026-06-05, streaming deliberately deferred as `FP-10`. The useful half is what it exposed — the performance evidence (Clothworkers 40–47s) was measured the day **before** pre-processing existed and has never been re-measured, so the document asserted a 45s target on pre-mitigation evidence while implying the mitigation was unbuilt. Now flagged as a baseline with the `P5.5` measured pass named (closes **O6**). Also checked and deliberately **not** "corrected": the 20,000 vs 50,000 character ceiling appears in different documents and both are right — code default versus production env override. **L4/L5** — done together, since L4's remainder was L5's actionable subset: `PRD-Grant-Pathway.md` → Tier 1, `DEPLOYMENT-CHECKLIST.md` → Tier 2. Verified the PRD was the **only** document named in either of `AGENTS.md`'s tier tables lacking a header. **The audit was wrong about the second and the truth is worse: `DEPLOYMENT-CHECKLIST.md` is not named in `AGENTS.md` at all**, so it was governed by nothing — in no tier table and with no self-governing header, meaning no end-of-task checklist has ever pointed at the document an operator reads immediately before deploying to production. `AGENTS.md` deliberately unmodified, per its own rule that a tier header makes a document self-governing. **L6** — the MKCF flagship cited `mkcf-oak-grants-criteria.pdf` in **two** places; no such file has ever existed. The risk was the opposite of the audit's framing and worse: the instruction said to save the criteria there "if not already present", so a tester would have downloaded the **current** edition and saved a second copy, when the repository holds the **November 2025** edition against which every recorded MKCF result — including a question count that has moved 10 → 12 → 16 → 19 — is measured. Both citations fixed, fresh downloads forbidden, and the refresh procedure stated. `TEST-DASHBOARD.md` had the correct filename all along. **L7** — `ADR-FILE-003` contradicted itself in its **binding** section: Consequences required two extraction utilities while its own Decision section and the code have only ever had one (`lib/extract-text.ts`). Third finding this session shaped like GAP-21 — the document a session is instructed to trust is the one that is wrong. **L8 — completed rather than deferred, at WJ's request: the dependency licence review now exists** (`docs/legal/dependency-licences-2026-07-30.md`) and **`GAP-20` is closed** after ten weeks as the only register row whose status cell had ever been completely empty. Conclusion: **nothing in the dependency tree prevents a closed-source, proprietary, hosted service.** All 25 direct production dependencies permissive; **no AGPL, SSPL or BUSL across 889 packages** — the finding that matters most, since AGPL treats network use as distribution; no unlicensed packages. Four production-reachable packages carry real conditions and each was assessed and cleared under the current model: `@sentry/cli` (FSL-1.1-MIT, build-time, non-compete dormant), `sharp`'s native binaries (**LGPL-3.0-or-later** — the item a solicitor would most want named, but obligations attach to distribution and nothing is distributed), `jszip` (dual-licensed, **MIT elected**, and it genuinely ships in Word export), `caniuse-lite` (CC-BY data). The conclusion rests on Grant Pathway remaining a hosted service; the artefact names the triggers for re-running it, records that `security-audit.yml` covers vulnerabilities **not** licences, and is explicit that it is a technical review and not legal advice or a substitute for P5.1's legal review. No licence-checking tool was installed, deliberately. **L9** — 24 broken relative links, all in archived documents, none live. They did not rot: **archiving broke them**, since moving a file into an `archive/` subfolder invalidates its relative paths, and 22 of 24 targets still existed at a different depth. All re-based; **zero broken relative links repository-wide**. The durable rule — archiving must include re-basing links — is now recorded, and matters for the deferred Implementation Plan tidy-up which may move these files again. **L10** — the 23 `format:check` failures were never real: `core.autocrlf=true` with no `.gitattributes` meant git checked out CRLF while Prettier expects LF, and git has **always** stored LF (verified: zero CR bytes in the stored blobs), so only the local checkout differed and CI on Linux never saw it. New `.gitattributes` (`* text=auto eol=lf`, `eol=crlf` for `.bat`/`.cmd`/`.ps1`, and an explicit `binary` list for PDFs, Word documents and images — deliberate rather than trusting auto-detection, since this repository stores funder PDFs and exported deliverables where corruption is easy to cause and hard to notice), plus `supabase/.temp/` in `.prettierignore` as the one genuine failure. **`prettier --check .` now passes cleanly repository-wide for the first time.** **Also actioned, prompted by a GitHub alert mid-session at 2,702 of 3,000 included Actions minutes (90%, resetting 2026-08-01): CI no longer validates migrations on documentation-only pushes.** Measured rather than estimated — GitHub bills the **sum** of parallel jobs, so a push cost ~4 billed minutes, and `validate-migrations` alone ran at **2.3, 4.3 and 9.1 minutes** on three consecutive runs while nine documentation-only pushes that day each started a Supabase instance to replay migrations that had not changed. It now checks whether anything under `supabase/` changed and skips its expensive steps if not, **but the job still runs and still reports success** — implemented as conditional _steps_ rather than `paths-ignore` or a job-level `if:`, because it is a required status check on `master` and a **skipped** required check never reports success, which is precisely the unsatisfiable-gate failure that **M2** and **M6** were about. It falls back to validating when the base commit is unavailable, so the safe default is to run. `lint-and-typecheck` was deliberately left unconditional, because `format:check` covers Markdown and is therefore the one job that genuinely applies to a documentation change. **Verified live: the next run's `validate-migrations` completed in 11 seconds and still reported success**, taking that push to ~2 billed minutes. **Two things to carry forward. (1) `M7`'s lockfile drift is a recurring condition, not a one-off** — ten packages were again installed at versions not matching `package.json` (`next@16.2.11` against `^16.2.12`, `@anthropic-ai/sdk@0.109.0` against `^0.115.0`, and eight more), because merging a Dependabot PR leaves the local tree stale by design. `npm ci` run; type-check and 101 tests pass on the resynced tree. A licence review run beforehand would have described a tree neither CI nor production uses. **(2) An incident, recorded because it is a real tooling risk:** a Python one-liner of the form `open(path, "w").write(text)` raised a `UnicodeEncodeError` **after** `"w"` had truncated the file, leaving `MK-Community-Foundation-test-plan.md` at zero bytes. Recovered immediately and completely with `git checkout --` on the uncommitted file and the edits redone; no committed content was ever at risk and nothing else was affected. Never chain `.write()` onto `open(…, "w")` when the payload can raise. **Documents updated across the Low findings:** `technology-stack.md` → v1.11, `ADR-TRACEABILITY.md` → v2.17, `IMPLEMENTATION-PLAN.md` → v3.23, `PRD-Grant-Pathway.md` → v0.64, `user-personas-journeys-and-use-cases.md` → v1.5, `regression-test-plan.md` → v2.11, `MK-Community-Foundation-test-plan.md` → v2.2, `DEPLOYMENT-CHECKLIST.md` → v1.4, `information-architecture-and-navigation.md` → v1.9, plus `ADR-FILE-003`, `non-functional-requirements.md`, `data-model.md`, `tone-and-voice-guide.md`, and new files `docs/legal/dependency-licences-2026-07-30.md`, `supabase/queries/operator-statistics.sql`, `.gitattributes`. **Outstanding from the audit: observations O1–O12 only** — O8 closed 2026-07-29, O10 (`middleware.ts` → `proxy.ts`) deliberately post-launch, O6 now covered by P5.5, O11/O12 now explicit P5.4 line items. **Also still open and unchanged, WJ's own list:** S2a (set the four legal effective dates), S2b (engage a reviewer from `docs/legal/legal-review-options-2026-07-29.md`), the GoDaddy DNS change (`A` → `76.76.21.21`, add `www`), the Sentry alert rule and PII-scrubbing confirmation, and M2's open decision on whether Vercel should refuse to deploy when CI fails. **Agreed as separate tasks, not started:** a new `accessibility-test-plan.md` as a seventh test layer and P5.3's output artefact, with NVDA setup notes (WJ could not get NVDA working on 2026-07-30 and is retrying 2026-07-31; the NVDA/VoiceOver deferral was narrowed to formal sign-off only), and a decision on whether the documentation set is outsized for the service. **Finally, at WJ's request, the two live legal documents were prepared for external review.** He ran the **ICO's own Privacy Notice Generator** — step one in `docs/legal/legal-review-options-2026-07-29.md` — and asked for a comparison. Our policy is substantially the better document; the ICO output arrived as an unfinished skeleton (`[Your additional purpose]` placeholders, instruction boxes, the company _number_ in the registered _name_ field, the wrong brand and domain, the personal `Wjokhia@` address dropped at v1.3, a retention section pointing circularly at the temporary `vercel.app` host, and one processing purpose). None of it was carried over; it is retained as a checklist and as evidence to a reviewer that the free ICO tools were used first. **It did expose three genuine gaps, all closed — privacy policy → v1.5:** first and last name were collected at registration but never disclosed; **consent was missing entirely as a legal basis** — the optional `feedback_consent` checkbox (FR-08) is textbook consent-based processing and appeared in neither Section 2 nor the Section 4 basis table, so Section 8 also omitted the **right to withdraw consent** that UK GDPR requires wherever consent is relied on; and the Charity Commission register lookup in `actions/charity.ts` was undisclosed, now covered by a new "Where we get your information from" subsection, flagged for the reviewer rather than asserted as settled since a small charity's registered address is often a trustee's home address. **Preparing the Terms then found two errors of its own — terms → v1.4 — one serious: Section 6 stated a fair-use limit of 20 AI-assisted requests per month when the real limit is 50.** A binding, published contractual term understating the service by 60%, wrong since the cap was raised on 2026-06-17 (`PDR-AI-005`) while `lib/prompts.ts` has enforced `MONTHLY_CAP = 50` throughout — six weeks live, and it would have gone to a solicitor uncorrected. **The single most consequential thing found this session.** The contact address was also `wjokhia@` in four places against the Privacy Policy's `admin@`, so **the two live legal documents gave different contact addresses for the same company**. Swept for the stale cap and found two more instances (`ADR-TRACEABILITY.md`'s `ADR-AI-008` consequence row, comments in `lib/ai-error-handler.ts`) — the 2026-07-10 pass that corrected this figure in three PRD/ADR files missed all three, the same partial-sweep pattern as everywhere else today. Both `-external.md` copies were **regenerated programmatically** from the internal files rather than hand-mirrored, with the diff checked line by line, removing the drift risk their own maintenance note warns about. **`docs/legal/archive/` created** holding only the two genuinely superseded v1.0 Word exports (`git mv`, references checked first per L9), with a README stating plainly that nothing in there is current; everything else deliberately left live. **`docs/legal/pdf/`** holds `Grant-Pathway-Privacy-Policy-v1.5.pdf` (8pp) and `Grant-Pathway-Terms-of-Service-v1.4.pdf` (7pp) generated from the external copies since those are the operative published text, each with `.docx` alongside for tracked-changes markup, both verified visually page by page. Method note: LaTeX stalls on this machine, the reliable route is pandoc → `.docx` → Word COM → PDF. **⚠️ Both still show `Effective date: [TO BE CONFIRMED]` — that is S2a, WJ's to set, needs no solicitor, and is the first thing a reviewer will ask.**)

**Previously (2026-07-29):** (An independent audit of the whole service and its documentation was commissioned from Claude Opus 5 and delivered as `docs/Opus Audit 290726.md` — 4 Severe, **8** Medium (M8 added during the walkthrough, see below), 10 Low findings plus **12** observations (O10–O12 added during the walkthrough), no service or documentation changes made during the audit itself. WJ is now working through it finding by finding to agree a plan of action; dispositions agreed so far: **S1** (no production database, drift alarm red 23+ days) reclassified as planned and tracked — prod DB to be built in the next few weeks under P5.4, with the Schema Drift Check going and staying green as its exit test; **S2** (both live legal pages published carrying `Effective date: [TO BE CONFIRMED]`) accepted as Severe and owned by WJ, blocking external-tester verification, split into **S2a** setting the two effective dates — not blocked, four files including the two `-external.md` mirrors — and **S2b** the independent legal review, blocked on sourcing an affordable reviewer, staying in P5.1, with a research task agreed to find named options and indicative costs; **S3** actioned immediately, see below; **S4** (Phase 5 not started) bulk reclassified as planned and tracked, but with the **empty production Sentry DSN pulled forward as an immediate action** since the external beta currently runs with no error visibility at all, and the prod redirect-URL allowlist to be made an explicit P5.4 line item rather than assumed knowledge, as missing it breaks day-one registration for every user. **Actioned this session: `app/mockup/` deleted** (audit finding S3) — a throwaway Step 4 design mock-up from 2026-06-05 that was still shipping to production, reachable with no authentication (it sat outside both route groups, and `middleware.ts` gates by allow-list so an absent route is open by default) and indexable (`app/robots.ts` disallows only `/api/`, `/account/`, `/dashboard/`). No data exposure — it was never connected to real data — but it published internal product-strategy notes publicly during an external tester beta, including the now-false claim "Does not handle narrative funders (Garfield Weston, City Bridge)" which Option B superseded. Deleted rather than relocated: the file header already authorised it and `docs/Business Design/mockup.html` holds the equivalent static artefact. Verified nothing referenced the route first. The audit's paired suggestion to add `disallow: '/'` to `robots.ts` while `grantpathway.org.uk` is pre-launch is **not** done — it belongs to finding M5, still to be reviewed. **Also actioned: two amendments to the audit report itself.** **(a)** S4's Sentry row was **wrong and has been withdrawn** — production error reporting works. `vercel env ls` shows both Sentry variables present in Production, the deployed bundle carries a live EU-region DSN (`o4511417358745600.ingest.de.sentry.io`, matching the org ID in `IMPLEMENTATION-PLAN.md:1587`), and WJ confirmed from the dashboard that `production` events are arriving. The false claim came from `IMPLEMENTATION-STATUS-ARCHIVE.md:830` — "production Vercel DSN is empty — to be set at P5.4" — never updated after the DSN was set; **that archive line is still stale and needs correcting, logged but not yet actioned.** What does remain unverified from the same P5.4 item: no Sentry alert rule appears to exist (seven issues accumulated unnoticed) and PII scrubbing is unconfirmed. **(b)** New finding **M8** added — deploying over an open tab breaks Server Actions silently. Sentry `GRANT-PATHWAY-6` (8 events, 3 weeks) diagnosed conclusively: a tab on release `5895146` sat 2h02m, then posted a Server Action that returned HTTP `200` with a body React could not parse, throwing via `onunhandledrejection`; commit `40453bf` had deployed in between, invalidating the old build's action IDs. **Vercel Skew Protection is disabled** (no `__vdpl` cookie in production responses) and 14 commits went to `master` that day, each a deploy invalidating every open tab. Compounded by the 2h02m gap exceeding the 60-minute session timeout while `D-013` (fixed 2026-07-28) stopped the client signing the user out. The worse half is handling, not cause: **88% of those events were on `.../step/4`**, the answer-writing screen, and the rejection surfaces nothing in the UI — a tester loses a submit with no error shown. Two-part fix: enable Skew Protection (Pro settings toggle, WJ actioning) then catch Server Action failures visibly, Step 4 first. **Also actioned: M1 — `AGENTS.md` §1's five Next.js documentation paths corrected.** All five pointed under a non-existent `app-router/` directory, making the project's most strongly worded pre-task check unenforceable since day one. Replaced with verified paths against the installed Next 16.2.11 tree (`01-app/…`), expanded from five rows to seven, plus a standing instruction to locate and correct a missing path rather than skip the check. **Discovered while doing so: as of Next.js 16, Middleware is renamed to Proxy and the `middleware` file convention is deprecated** — there is no middleware documentation left in the installed tree. This project's `middleware.ts` (route allow-list, per-request CSP nonce, session handling) has **not** been migrated; a codemod exists (`npx @next/codemod@canary middleware-to-proxy .`) but the rename is load-bearing for auth and security headers, so it needs its own change and its own testing rather than being bundled into a documentation fix. Logged in `AGENTS.md` §1 and `CHANGELOG.md` so no session treats the deprecated name as current. **Also actioned: M2 — the `audit` job moved out of `ci.yml` into a new `security-audit.yml`** (weekly Mondays + manual, deliberately not a required check), so a genuine failure in `lint-and-typecheck`/`test`/`validate-migrations` is visible again instead of being masked by the permanently-red dependency advisory that made `ci.yml` red on 11 of its last 12 runs. WJ chose this over allowing the advisory. Same finding: `DEPLOYMENT-CHECKLIST.md`'s claim that "on CI pass, Vercel builds and deploys" was **never true** — Vercel deploys on push independently of CI — now corrected with an explicit "check CI manually" instruction; whether to make the gate real via Vercel's Ignored Build Step is recorded as an **open decision**. CI job names reconciled across `README.md`, `technology-stack.md` TS-08 and `DEPLOYMENT-CHECKLIST.md`, which all used invented labels (Quality/Tests/Security/Migrations) matching no real job. **Outstanding:** `master`'s branch protection still requires a now-nonexistent `audit` context, so that requirement can never be met — needs the protection rule reduced to the three real contexts, awaiting WJ's go-ahead as a repo settings change; no practical change meanwhile since `enforce_admins` is `false` (M6). **Also actioned: S2b research filed and branch protection corrected.** New document `docs/legal/legal-review-options-2026-07-29.md` unblocks the S2b half of the legal-review problem (S2a — setting the two effective dates — needs no solicitor and remains outstanding). Ten providers, all with sourced prices: **qLegal (QMUL) £0** (only free scheme admitting a for-profit company under £100k turnover, reopens July/August 2026), **Singleton Solicitors £480 + VAT** (only published fixed fee covering both documents), **Lawhive ~£200–£400**. Recommended path is free ICO tools first (Privacy Notice Generator, self-assessment, SME helpline) then a paid review — c. £576 inc. VAT realistic, plausibly £0. LawWorks confirmed **ineligible** (not-for-profit entity types only); DPO-as-a-service (£695–£5,000/mo) flagged as the main trap since there is almost certainly no statutory DPO duty. Two prices unverified on the day (Singleton, LawBite — sites down) and flagged in place. Belongs to P5.1 alongside `GAP-20`. **Branch protection on `master`** also corrected with WJ's authorisation: the required-checks list still named the now-removed `audit` context, so it could never be satisfied; reduced to the three real contexts and verified — `strict` still true, force-push/deletions still blocked, `enforce_admins` still `false` (that is M6, still an open decision). **Also actioned: M3 — all three parts, WJ having asked for the value-adding option rather than mere compliance.** (1) The "feature flag convention" — which required every AI-prompt/eligibility/export change to be wrapped in its own env-var flag, and which at least four `lib/prompts.ts` changes had ignored — is **replaced** by a **recovery-path convention**: a named way to back the change out, recorded in the commit or changelog, with Vercel rollback, `AI_ENABLED=false` or a dedicated flag all qualifying. Replaced rather than reinstated because it guarded the wrong risk: every one of those four changes was itself a fix for a defect found in live testing, so the failure was undetected regressions, not un-revertable ones — a flag would not have helped. (2) **`AI_ENABLED` is now documented** in the feature-flag table, listed first, with what users actually see (both AI routes return 503 "AI service busy"; the charity paraphrase degrades silently so profile setup still completes) and the sharp edge that it must be exactly the string `false`. (3) **New pre-deploy prompt-change regression check** — any `lib/prompts.ts` change requires re-running one known guideline and confirming the extracted question count, with before/after recorded; this is the control that would have caught the 12→10 regression, and it targets the real gap (silent extraction regressions are invisible to CI and the unit suite — see observation O7). Also closed the first half of **L4** (stale "funder test plan" item, retired by `DR-TEST-001`); L4's missing Tier header remains for the doc sweep. `DEPLOYMENT-CHECKLIST.md` → v1.3. **Also actioned: M4 — CI moved from Node 20 to Node 24 to match production.** `vercel project inspect` confirmed the production project runs **Node 24.x** and local dev is v24.14.1, so CI on Node 20 was the only place in the setup running a different — and end-of-life since 30 April 2026 — runtime, i.e. the one place whose job is catching mistakes was not testing what users hit. That confirmation also settled the audit's open "Node 22 or 24?" question in favour of **24**; 22 would have left a smaller version of the same mismatch. Changes: `node-version` → `'24'` in `ci.yml` (both Node jobs) and `security-audit.yml`; **`"engines": { "node": ">=24" }` added to `package.json`** where no `engines` field existed at all — this also pins the Vercel runtime in version-controlled code, since Vercel reads that field; `README.md`'s "Node.js 18+" prerequisite corrected (it was below Next 16.2.11's own `>=20.9.0`, so following the README produced an unrunnable build — relevant to `DR-BM-002`'s succession assumption), with the same fix applied to `IMPLEMENTATION-PLAN.md` and `technical-design.md`. `ADR-OPS-008`'s snippet updated to `'24'` plus a correction note recording that it shows one job where three now gate and that its "before Vercel begins its build" claim was never true (M2). Type-check, lint and all 101 tests pass. **Also actioned: M5 — the public base URL is now environment-driven and indexing is opt-in.** Checking the domain directly found the position worse than "DNS not pointed yet": `grantpathway.org.uk` **is** attached to the Vercel project (apex and `www`), but nameservers are still the registrar's (`ns39`/`ns40.domaincontrol.com`), Vercel reports it **not configured properly** and wants `A 76.76.21.21`, the apex returns HTTP `200` from `76.223.67.189` serving a **registrar parking page** (no `APP_VERSION`, unlike `grant-pathway-three.vercel.app` which serves the current build), and `www` does not resolve at all. A misleading `200`. **Note this supersedes the 2026-07-28 entry claiming the domain "already points at `grant-pathway-dev`" — it does not.** Live consequence: the three transactional emails linking to the domain (inactivity warning, both deletion confirmations) have been sending users to a parking page, and `sitemap.xml` advertised five unreachable URLs. Fixed with a new `lib/site-url.ts` exporting `SITE_URL` (from `NEXT_PUBLIC_SITE_URL`, default the canonical domain, trailing slash stripped — truthiness check not `??`, since `vercel env pull` writes empty strings) now used by `sitemap.ts`, `robots.ts`, `layout.tsx` (`metadataBase` + OG `url`) and all three emails, and `ALLOW_INDEXING` (from `NEXT_PUBLIC_ALLOW_INDEXING`) which is **opt-in** — `robots.ts` now disallows everything unless it is exactly `'true'`, where previously it allowed `/` unconditionally and left the pre-launch host and every preview indexable. Both documented in `.env.example`; README gains a domain-status note. Deliberately left hardcoded: the Resend sender identity `noreply@grantpathway.org.uk` (DKIM-tied, not a link, works today) and the Word-export footer brand line. Type-check, lint, 101 tests pass. **Needs WJ:** the registrar DNS change, then `NEXT_PUBLIC_ALLOW_INDEXING=true` at launch; meanwhile consider `NEXT_PUBLIC_SITE_URL=https://grant-pathway-three.vercel.app` so email links reach the running app. **Also actioned: M6 — GAP-11 closed as an accepted deviation, dead ruleset deleted.** Admin enforcement was considered and **rejected on a mechanical ground**: required status checks are evaluated against the commit being pushed but only run _after_ the push, so a direct push can never satisfy them — `enforce_admins: true` would block all direct pushes to `master` and force a PR workflow where the sole developer approves their own PRs, contradicting `AGENTS.md` §5 for no added reviewer. (This is what the earlier bypass messages meant: "4 of 4 required status checks are expected" = not yet reported.) WJ's decision: accept and document. GAP-11's status was wrong twice — protection was never blocked on a GitHub Pro upgrade (it is live on `master`: three CI contexts required, `strict` on, force-push/deletion blocked) and its "team enforces PR review manually" workaround describes a process that neither happens nor is required; all three rows also said `main` rather than `master`. Now ➖ accepted deviation matching the GAP-15/16 convention, with residual risk stated (an unreviewed commit can reach production; Vercel deploys independently of CI) and the real mitigations named (pre-deploy checklist, Vercel instant rollback, `AI_ENABLED`, and CI now genuinely green). Revisit if a second contributor joins. The dead ruleset "Protect Master" (id `16804175`, `enforcement: disabled`, created 2026-05-24) was **deleted** — the repo now has no rulesets, so classic protection is the only mechanism and the ambiguity is gone. `ADR-OPS-002`/`ADR-STACK-005` rows updated to ✅; `ADR-TRACEABILITY.md` → v2.13. **Also actioned: M7 and O8 — and a second correction to the audit.** M7's headline claim of "twenty Dependabot PRs open, several superseded" was **wrong**: five are open (#79, #81, #82, #84, #85), none superseded, and `dependabot.yml` caps each ecosystem at five so twenty was never possible. The superseded PRs it described were real but had already been closed on 24–27 July, before the audit — Dependabot closes its own PRs when it opens a newer bump for the same package. Report corrected in place. The queue's real problem was different: `daily` with **no grouping** kept it pinned at the five-PR limit, one package per PR, so it stopped carrying information — the same desensitisation pattern as M2's permanently-red job. Now **weekly (Mondays) and grouped**: production minor/patch as one PR, devDependency minor/patch as another, with **majors deliberately ungrouped** so each keeps its own changelog — the deferred ESLint 10 upgrade (#79) stays visible as one identifiable PR rather than buried in a batch. GitHub Actions updates likewise weekly and grouped. **Lockfile drift resolved:** `npm ls react` had reported `19.2.7 deduped invalid: "19.2.8" from the root project`, so local runs were not testing pinned versions (CI unaffected — it uses `npm ci`); `npm ci` run, `react@19.2.8` now consistent throughout. **O8 closed:** the two stale local branches (`prettier-3.8.4`, `typescript-6.0.3`), both superseded by what `package.json` declares, deleted — `master` is now the only local branch. No PRs closed, since none is superseded. **Also actioned: M8's second half — Step 4 now tells the user when a save or action could not reach the server.** The first half (Vercel Skew Protection) closed earlier today; this was the more important half, because **the failure was silent**. Step 4's Server Actions already return `{ok, error}` objects for expected errors and those surface fine; the gap was a transport-level failure where no result comes back at all (version skew, or session expiry redirecting to sign-in so React receives unparseable HTML). `handleApprove`, `handleAddManualGovernanceItems`, `handleReadyToAssemble` and `handleManualContinue` all awaited without `try`/`catch`, so the rejection escaped to `window.onunhandledrejection` — Sentry logged it (`GRANT-PATHWAY-6`, 8 events/3 weeks, **88% on Step 4**) but the user saw nothing. Worst was **`doSave`**, whose catch was empty and commented "silent — blur will retry on next edit": sound for a transient failure, wrong for these causes where every retry also fails, so a user could keep writing while nothing persisted. Fixed with a new `lib/action-error.ts` (two messages, reasoning and Sentry evidence recorded alongside), a sticky `role="alert"` "**Not saved.**" banner in the progress bar with a **Reload now** button — placed there so it cannot be scrolled past while typing, and self-clearing on the next successful save — and `try`/`catch` on the other four handlers routing to their existing inline error slots. Tier 1 docs updated: `PRD-Grant-Pathway.md` → **v0.62** (Section 7 Step 4 error-state row, kept distinct from the existing "API failure" row) and `acceptance-criteria.md` gains **AC-FR-18-04**, which records why this is a deliberate exception to AC-FR-18-02's silent-background-save rule (silence is right for saves that succeed, never for failures). Type-check, lint, 101 tests pass. **Not browser-verified** — needs an authenticated Step 4 session plus a forced transport failure; quickest check is to type an answer, go offline in devtools, blur the field, and confirm the banner appears, then go back online and confirm a successful save clears it. Scope: Step 4 only, per the audit and where 88% of events occurred; the same pattern exists in other components calling Server Actions without a catch and is worth a post-launch sweep. **All eight Medium findings are now closed; the ten Low findings and the observations remain, and WJ has scheduled them for the next session (2026-07-30) together with the Phase 5 restructure proposal below. Session closed at that point — no Low finding was actioned.** **Also actioned: a review of Phase 5 at WJ's request, which found it does not hang together — findings and a proposed restructure are in `docs/Implementation Plan/phase-5-restructure-proposal-2026-07-29.md`, awaiting his approval, with nothing yet changed in `IMPLEMENTATION-PLAN.md` or either status document.** Three structural problems, all verified rather than inferred: the status accounting misstates Phase 5 as complete when its real progress is one task (six of the seven counted "done" are the funder-directory tasks reversed on 2026-07-15); `P5.3` is headed "Accessibility" but seven of its bullets are unbuilt ADR/PDR code changes, of which **GAP-24 is the most consequential — every Word export still tells the funder "All content has been checked for accuracy before submission", a claim the app cannot make in the user's name**; and `P5.5` "Final Testing" predates Phase 6 entirely while competing with `TEST-DASHBOARD.md`'s layer structure. The proposal's headline addition is a new blocking **`P5.0` requirements reconciliation** — the doc-vs-build comparison WJ asked about, which no Phase 5 task performs today. It also flags that `✅` in `ADR-TRACEABILITY.md`'s resolution column means "a task was written" for GAP-21–25 but "the code exists" for GAP-26, and that the **Phase 4 → Phase 5 gate sign-off** consequently lists nine gaps as resolved when six are unbuilt. **Also actioned: dependency updates merged and two majors deferred.** #86 (production minor/patch, 8 updates incl. Next 16.2.10 → 16.2.12) and #87 (dev minor/patch, 4 updates) squash-merged with all required checks green — the first output of the morning's `dependabot.yml` grouping rewrite, working as intended. **#88 (`typescript` 6 → 7) and #79 (`eslint` 9 → 10) were deferred and then closed**, both blocked upstream by `eslint-config-next`/Next rather than by our code: TS 7 fails CI on `typescript-eslint does not support TS 7.0` and fails the Vercel build because Next 16.2.11's own TypeScript step cannot load it; ESLint 10 still fails on `contextOrFilename.getFilename is not a function` inside `eslint-plugin-react`. Closed rather than left open because Dependabot re-bases and rebuilds an open PR on every merge to `master` — WJ spotted two further red rows which turned out to be the same PR (#88) rebuilt twice — so leaving them open would have produced a red deployment row on every future merge, re-creating the exact desensitisation failure M2 and M7 were about. Dependabot will raise fresh PRs on the next releases. **Trap recorded on the closed PR and in `CHANGELOG.md`: the Vercel preview on the ESLint 10 branch is green, and that is misleading — `next build` does not run ESLint, so only CI's `lint-and-typecheck` exercises the upgrade.** A red Vercel preview on a deferred major-version PR is expected and is not a production signal. Branch protection refused the first merge (`strict: true`, `master` had moved) and the branch was rebased rather than forced through with `--admin` — its first live exercise. **Also actioned: two new observations added to the audit, plus a correction to one of my own.** **O11** — `SENTRY_AUTH_TOKEN` is absent from Vercel, so no Sentry release is created and **production stack traces are minified**; this directly weakens the diagnostics S4 and M8 rely on, and P5.4's Sentry line asks only for the DSN, so a sign-off today would leave it that way. **O12** — `onRouterTransitionStart` is not exported from `instrumentation-client.ts`, so client-side navigations are uninstrumented, which matters disproportionately here because the five-step flow is almost entirely client-side navigation. Both were read out of a Vercel build log while diagnosing #88 and have been present since the SDK was installed. **The correction:** O10 claimed the `middleware` deprecation was silent with no build warning — it is not; `⚠ The "middleware" file convention is deprecated` has appeared in every Vercel build log since Next 16 landed. The accurate version is worse for us: nobody reads build output on a green deployment, which is now the shared root cause of O10, O11 and O12. See `CHANGELOG.md` (2026-07-29) and `docs/Opus Audit 290726.md`.)

**Previously (2026-07-28):** (Three further items same day, most recent first: **(1)** `docs/legal/external-tester-brief.md` moved to `docs/Test Plans/` (it's tester-onboarding material, not a legal document) with `git mv`, no duplicate left behind; amended while there to add a "Need a hand while you're testing?" section linking the real GitBook help centre (`https://rapidglobe.gitbook.io/grant-pathway`). **(2)** A documentation freshness audit (WJ asked whether everything referenced in `AGENTS.md` was current) found six gaps — `IMPLEMENTATION-STATUS.md` itself stale by a day, `data-model.md`'s `lookup_source` values wrong plus missing `service_role` grant/`added_manually` documentation, `PRD-Grant-Pathway.md` Section 7/12.4 and its trailing version line all drifted, `acceptance-criteria.md` missing an AC for D-013's modal-open exception, `technical-design.md`'s session-timeout section missing `touchstart` and using an invented button label, and `moscow-feature-register.md`'s FR-29/FR-47 notes not mentioning already-built PDRs. All six fixed same day; see `CHANGELOG.md` (2026-07-28) for full detail. **(3)** WJ found sign-in failing on an iPhone ahead of an external demo — traced via `vercel logs` to a trailing space at the end of a pasted password, which neither `signIn()` nor `SignInForm` trimmed before use (**D-015**). Fixed in both places (server-side trim is the real fix; client-side trim avoids a spurious "invalid email" error on a trailing-space email); live-verified in the dev server that a trailing-space password now passes validation and is correctly rejected as wrong credentials rather than silently mismatched. Registration/reset/change-password not touched, same latent risk if it resurfaces there. See `CHANGELOG.md` (2026-07-28) for full detail.

**Previously (2026-07-28):** Both defects logged the night before were resolved and live-verified, `guideline-capability-matrix-test-plan.md` and `eligibility-check-test-plan.md` both fully executed and set to 🟢, `regression-test-plan.md` completed for the first time ever (all 15 cases Pass), external-tester prep done, and the AWS Bedrock cost backstop confirmed already built. Full day, most recent first: **(1)** `PDR-AI-011` — the previous night's non-deterministic eligibility verdict (National Opera Studio vs. Idlewild) root-caused as an inherent Bedrock/LLM limitation, not a missing `temperature: 0`; fixed with an asymmetric second confirming call before trusting an `eligibilityMismatch: true` verdict (biased toward not blocking, matching `DR-EL-001`'s cost asymmetry). **(2)** `PDR-AI-012` — the previous night's missing aggregate word-limit indicator (CPF Trust's 500-word cap spanning multiple AI-extracted sections) fixed with a new `overallWordLimit` field and a live combined-word counter across linked Step 4 sections; made more prominent (larger, bold) per WJ's live-test feedback. Both live-verified same day, `guideline-capability-matrix-test-plan.md` bumped to v1.4. **(3)** `eligibility-check-test-plan.md` EL-01 and EL-03 both executed and passed (EL-03 initially looked like a failure — "no eligibility mismatch warning" — but the test plan's own documented expected result is exactly that outcome on a partial fit; corrected WJ's read, he confirmed Pass), bumped to v1.3, 🟢. **(4)** `regression-test-plan.md` RT-01b (Charity Commission lookup) closed as a byproduct of EL-01/EL-03 testing; RT-01 through RT-14 then executed in one session, all Pass (RT-03/RT-11 had stale plan text, corrected; RT-14 confirmed the 2026-07-23 `service_role` fix still holds on dev, prod still unpatched). **(5)** RT-15 (session timeout) initially failed to reproduce on a fresh scripted re-run in Chrome — no warning modal, no sign-out, at the real 55/60-minute wait. Diagnosed by temporarily shortening `WARNING_MS`/`TIMEOUT_MS` to 1/2 minutes in `components/session-timeout-provider.tsx`, which surfaced two real, previously-invisible defects: **D-013** (Blocking) — the modal dismissed itself on any mouse movement toward its own buttons (the activity listener didn't check whether the modal was already open), meaning it was never actually clickable, likely since the modal was first built; **D-014** (Cosmetic) — warning text rendered "1 minutedue to inactivity" due to a JSX line-wrap whitespace-trimming gotcha in `components/session-timeout-modal.tsx`, fixed by switching to a template literal. Both fixed and live-confirmed by WJ; diagnostic timer reverted to the real 55/60 minutes, no test-only code remains. RT-15 closed Pass — **all 15 cases in `regression-test-plan.md` now Pass for the first time**, bumped to v2.10. **(6)** `TEST-DASHBOARD.md` bumped across the day to v2.16 — 5 of 6 test plans now 🟢 (only `help-and-tooltips-test-plan.md` remains 🟡, NVDA/VoiceOver deliberately deferred by WJ). **(7)** Split `docs/legal/terms-of-service.md`/`privacy-policy.md` into internal (with changelog) and new external (clean, no changelog blockquotes) versions; `/terms` and `/privacy` now render the external copies. **(8)** Drafted `docs/legal/external-tester-brief.md` ahead of opening the dev-hosted site to external testers in the coming weeks (WJ decided dev, not prod — no infrastructure change needed, `grantpathway.org.uk` already points at `grant-pathway-dev`); checked Dependabot status, unchanged since 2026-07-25, still parked per WJ. **(9)** WJ confirmed the AWS Bedrock spend-cap backstop (`PDR-AI-005`) is already built, not merely planned as the PDR previously implied — `grant-pathway-bedrock-cap`, a $127/month (≈£100) AWS Budget, alert-only at $70/$127 thresholds, correct recipient, Bedrock-only account; PDR corrected. See `CHANGELOG.md` (2026-07-28) for full detail on every item above.

**Previously (2026-07-27):** `guideline-capability-matrix-test-plan.md` fully executed by WJ: GCM-01 through GCM-05 all Pass, closing out the last untested layer of `DR-TEST-001`'s suite. The table-format budget-question fix below was live-verified (12→13 questions). Two findings logged as observations, not fixed that night: a non-deterministic eligibility verdict on identical input (National Opera Studio vs. Idlewild — fails once, passes on an unchanged retry, not yet root-caused), and no aggregate word-limit indicator when one funder limit (CPF Trust's 500-word cap) spans multiple AI-extracted sections — a live combined counter was agreed as the fix, deferred (both resolved the next day, see above). GCM-04 needed a different charity (Bridge Support MK, on a fresh account) since National Opera Studio doesn't fit Clothworkers' 10 disadvantage-focused programme areas; all 13 of its extracted questions were cross-checked directly against the source PDF with no discrepancies. `TEST-DASHBOARD.md` bumped to v2.7. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** Starting GCM-01, WJ found a project funding-amount question ("State the total amount of funding you are requesting...") silently missing from Step 4. Root cause: a separate TABLE FORMAT rule in `lib/prompts.ts`, used only for table-structured guideline documents, has its own independent numeric-type skip-list that the same-day MKCF budget-question fix never touched — it dropped the question outright since its source-table type was "Short numerical field". Fixed by adding the same budget-question exception to that skip-list. `tsc --noEmit`, 98/98 tests, and lint all pass. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** AB Charitable Trust flagship (v2.2, Asylum Justice) fully executed by WJ: ABC-01 through ABC-10 all Pass. The governance-dropdown fix below was live-verified. Two factual errors in the test plan itself were also corrected: the grant range was wrongly recorded as £10k–£40k/yr (guidelines actually state £10,000–£30,000 pa) and a fabricated "31 July 2026" deadline was removed (no deadline appears in the guidelines at all). ABC-04 stripped of eligibility-specific wording (that's `eligibility-check-test-plan.md`'s job). Also actioned: an ABC-01 mix-up mid-session overwrote `eligibility-check-test-plan.md` EL-02's fixture account profile before the flagship correctly switched to a new account — EL-02's recorded pass stands but that account can no longer reproduce it, flagged in that plan (v1.2). `TEST-DASHBOARD.md` bumped to v2.6. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** Live-testing the AB Charitable Trust flagship (ABC-08), WJ found a manually-added governance dropdown ("Are any bank signatories related...") left at its default "Not sure yet" showed no approve panel at all, with no way to remove the item — a genuine stuck state. Root cause: "Not sure yet" was stored as an empty string, indistinguishable from "untouched", and the approve gate treats a manually-added item's blank answer as unanswered by design. Fixed by giving "Not sure yet" a real value and treating a governance dropdown as never "empty" (a `<select>` always has an effectively selected option); also handles approving without ever touching the dropdown by persisting "Not sure yet" explicitly rather than leaving `answer_text` null. `tsc --noEmit`, 98/98 tests, and lint all pass. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** MK Community Foundation flagship paste-path retest completed and reported by WJ: 16 questions extracted, MKCF-01–09 all Pass, and — the point of the retest — citations on Q12/Q14/Q16/Q17 now open the guidelines viewer with the correct passage highlighted, live-verifying the `dewrapSoftLineBreaks()` fix below. No other issues reported. `MK-Community-Foundation-test-plan.md` and `TEST-DASHBOARD.md` updated accordingly. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** Citation highlight missing on soft-wrapped numbered questions that had soft-wrapped across two lines when pasted from Word — `lib/preprocess-text.ts`'s pasted-text section-tagging used only the first physical line as the heading, injecting the marker's closing bracket mid-sentence and breaking quote matching for the rest. Fixed with a new `dewrapSoftLineBreaks()` pass (merges a lowercase-starting continuation line into the previous one, conservative enough not to merge genuine short headings with separate body text) plus a longer heading-length cap for numbered questions specifically. 3 new tests, all 98 pass. See `CHANGELOG.md` (2026-07-27) for full detail.

**Previously (2026-07-27):** Live-testing the MK Community Foundation Oak Grants flagship, MKCF-06 found the AI's question extraction was silently dropping project-budget questions (e.g. "What is the total cost of your project?") and the narrative half of compound label+question lines (e.g. "Description of your project" from "Project Name & Description of your project:") — `lib/prompts.ts`'s exclusion list conflated organisational financial data-entry with project budget/cost questions, which should still surface as narrative cards per `is_budget_question`. Fixed by scoping the exclusion and adding explicit compound-line/budget-question rules; re-verified live at 15→19 questions after the fix, all confirmed genuine against the source PDF. Same session: AB Charitable Trust flagship's Test Data charity found not to genuinely match any of AB's four eligible themes, so ABC-04 (intended as the positive case) triggered a real eligibility mismatch instead. Actioned: `AB-Charitable-Trust-test-plan.md` bumped to v2.1, charity swapped to Asylum Justice (real charity, genuine unforced match on Access to Justice / Migrants and Refugees), on a new test account since charity profiles are one-per-account; the accidental mismatch run logged as `eligibility-check-test-plan.md` EL-02's completion (v1.1, caveat — dashboard badge step not independently checked). `TEST-DASHBOARD.md` bumped to v2.4. See `CHANGELOG.md` (2026-07-27) for full detail.)

**Previously (2026-07-25):** Live-testing HT-05's keyboard-only pass found 3 of 9 tooltips (`tt-budget-no-ai`, `tt-guidelines-choice`, `tt-summary-review`) completely unreachable by keyboard — each wraps a plain `div`/`p`/`h1` with no `tabIndex`, a real WCAG 2.1.1 failure (`GAP-38`). Fixed same session: `tabIndex={0}` + focus-visible ring added to all three. Bundled in the same pass: `tt-summary-review`'s factually-wrong copy (`GAP-37`) rewritten to describe the summary screen's actual purpose instead of a caveat that can't apply to it. `GAP-36` (`tt-charity-lookup`'s hover flash) remains open, needs live debugging. Separately found: `@axe-core/react` only runs in local `npm run dev`, not the deployed Vercel build WJ was testing against — HT-05's axe-core step is blocked until tested locally. Type-check/lint/95 tests all pass. See `CHANGELOG.md` (2026-07-25) for full detail.

**Previously (2026-07-25):** Tooltip persistence reversed — `PDR-UI-008` v3.0. Live-testing found a missed migration and, once fixed, no self-service way to bring back a dismissed tooltip (`GAP-35`); stepping back, WJ concluded the whole server-side dismiss/persist/reset mechanism was over-engineered for a pre-launch product and asked to simplify to a plain hover/focus tooltip with no memory. `components/contextual-tooltip.tsx` rewritten, `actions/tooltips.ts` deleted, the same-day Reset tooltips control removed, `user_tooltip_dismissals` table dropped (migration `20260725000000`, pushed to `grant-pathway-dev`). Also removed Step 2's stale "guidelines not saved" banner (`GAP-34`) per WJ's request, found in the same testing session. `GAP-34`/`GAP-35` both closed. Type-check/lint/95 tests all pass. See `CHANGELOG.md` (2026-07-25) for full detail; `docs/Test Plans/help-and-tooltips-test-plan.md` still needs its own rewrite pass, not done in this change.

**Previously (2026-07-25):** Live-testing `PDR-UI-008`'s help-and-tooltips-test-plan.md: HT-01 passed. HT-02 first failed to persist — traced to migration `20260724000000` (`user_tooltip_dismissals` table) never having been pushed to `grant-pathway-dev`, despite being committed 2026-07-24; pushed, then HT-02 re-tested and passed. WJ then asked how a user re-surfaces a dismissed page-load tooltip — no self-service path existed, checked `account-settings-form.tsx` (the natural home) and confirmed nothing was there. Logged as `GAP-35`, then built at WJ's request mid-session to unblock further testing: a "Reset tooltips" control in Account Settings calling a new `resetAllTooltips` Server Action (`actions/tooltips.ts`) that deletes all `user_tooltip_dismissals` rows for the current user. Type-check/lint/97 tests all pass. See `CHANGELOG.md` (2026-07-25) and `ADR-TRACEABILITY.md`'s Gaps register for full detail.

**Previously (2026-07-25):** Dependabot PR #80's react-dom bump was failing every check — react-dom@19.2.8 needs peer react@^19.2.8, which the PR left at 19.2.7. Fixed directly on master by bumping both together, matching this repo's existing practice of fixing dependency issues via a direct commit rather than pushing onto a Dependabot branch. Also patched a newly-disclosed postcss advisory found mid-verification (override floor raised 8.5.10→8.5.18); a second newly-disclosed brace-expansion advisory was left as-is — it's dev-tooling-only and part of the same already-deferred ESLint 10 chain as PR #70. Type-check/lint/97 tests/build all pass. See `CHANGELOG.md` (2026-07-25) for full detail.

**Previously (2026-07-24):** Help centre base URL corrected — an updated handoff spec confirmed the real GitBook address, `https://rapidglobe.gitbook.io/grant-pathway`, replacing the placeholder used when `PDR-UI-008` was first built. Single-constant fix in `lib/help-centre.ts`; no other file changed. Type-check/lint/97 tests all pass. See `CHANGELOG.md` (2026-07-24) for full detail.

**Previously (2026-07-24):** Help centre link + tooltips given their own test plan layer — `docs/Test Plans/help-and-tooltips-test-plan.md` new, 8 cases covering the help-centre link, all 5 tooltip trigger variants, cross-session persistence, and the `ADR-OPS-006` accessibility pass. Kept separate from the flagship/capability-matrix plans as a horizontal UI concern. `TEST-DASHBOARD.md` bumped to v2.1. Not yet executed. See `CHANGELOG.md` (2026-07-24) for full detail.

**Previously (2026-07-24):** Help centre link + contextual tooltips built — `PDR-UI-008`. Persistent "Help" link added to nav/footer/dashboard empty-state; 10 of 11 spec'd tooltips built via a new reusable `components/contextual-tooltip.tsx` (no new tour-library dependency), dismissed-state persisted server-side via new `user_tooltip_dismissals` table (migration `20260724000000`). Fixed a real AI-usage-counter bug along the way: two separate places were computing `current_usage` and discarding it before it reached the client. `tt-register-password` deliberately not built — already redundant with an existing permanent hint. New dev-only component-testing infrastructure added (`@testing-library/react`, `happy-dom`) since none existed. Type-check/lint/97 tests all green; live browser/accessibility verification outstanding, blocked by this environment's known dev-server credential limitation. See `CHANGELOG.md` (2026-07-24) and `PDR-UI-008` v2.0 for full detail.

**Previously (2026-07-24):** Test strategy restructured — `DR-TEST-001` moves `docs/Test Plans/` from funder-by-funder to capability/guideline-shape based, following `DR-FD-001` v1.4's removal of the funder picker. Two flagship end-to-end plans retained (A B Charitable Trust, MK Community Foundation), a new guideline-capability-matrix plan and a new eligibility-check plan (3 varied cases) added, 10 named-funder plans archived. See `CHANGELOG.md` (2026-07-24) and `TEST-DASHBOARD.md` v2.0 for full detail.

**Previously (2026-07-24):** Change-password fixed and live-verified — GoTrue requires a `current_password` field directly in the `updateUser` call when `GOTRUE_SECURITY_UPDATE_PASSWORD_REQUIRE_CURRENT_PASSWORD` is enabled on this project; confirmed via `vercel logs` (`code: 'current_password_required'`) and fixed in `actions/auth.ts`. Two false alarms during verification (suspected rate limit, ruled out via new `signIn` logging showing genuine `invalid_credentials`; suspected the fix itself, ruled out via a working password-reset confirming the Auth stack was healthy) both traced to mismatched password values between WJ's attempts, not app bugs. WJ confirmed a clean change-then-sign-in with a copy-pasted password works end-to-end. See `CHANGELOG.md` (2026-07-24) and the Notes table below for full detail.)

**Previously (2026-07-23):** Account deletion fixed — `service_role` was missing table grants on `application_items`/`application_guidelines`, causing every deletion attempt to fail on the very first cascade step with a generic "Deletion failed" error. New migration `20260723000000_grant_service_role_item_graph_tables.sql` applied to `grant-pathway-dev`. Not yet applied to `grant-pathway-prod`. See `CHANGELOG.md` (2026-07-23) for full detail.

**Previously (2026-07-23):** Citation-highlight fix: `findQuoteRange` now also tolerates a list-bullet glyph — WJ found Garfield Weston Q9 "Your finances" opened the "view original guidelines" panel with no highlight, unlike Q1, because the source's bulleted list line ("understand:\n■ that you have...") has a non-whitespace "■" sitting between two quoted words that the AI's "verbatim" quote drops. See `CHANGELOG.md` (2026-07-23) for full detail.

**Previously (2026-07-21):** `[ITEM N]` citation fallback marker built and live-verified — while generating user-guide screenshots, WJ found the Wolfson Foundation's guidelines produced zero citations; root cause was its docx having no real Word heading styles at all, so the tagging pipeline had no page or heading marker to anchor a citation to. A third citation type, `[ITEM N]`/`source_type: 'item'`, was built as a fallback for exactly this case — WJ's own proposal. First re-test looked like a fresh failure but was actually Step 3 skipping regeneration for an application that already had a cached summary; after explicitly clicking "Regenerate summary," WJ confirmed "Item N of the guidelines" badges render correctly. Two new findings logged, not yet actioned: no citation badge on the budget-flagged section, and section count varying (6 vs 7) between identical runs. See `CHANGELOG.md` (2026-07-21) and `ADR-DATA-007`'s 2026-07-21 amendment for full detail.)

**Previously (2026-07-17):** Long Stony Stratford Town Council live-testing session, WJ: "Job done, it looks much better." Full list of fixes/decisions that session, most recent first: PDR-AI-010 decided and built (financial-section citation duplication); all field types now reflect the approved-state tint; citation badges truncate long headings; the real Step 4 sync bug (silent failure for any funder with both governance facts and narrative items, latent since 2026-07-15) and the real citation heading_path bug both found via `vercel logs` and fixed; PDR-AI-009 compound-question false positive investigated, no change needed; both agreed morning items (site footer version id, PDR-AI-009 relevance-check) built and live-verified; export date fixed to one timestamp per application. One open finding logged, not yet actioned: a likely-hallucinated governance fact (trustee-relatedness) on this funder, to be covered by WJ's planned formal test rather than an ad-hoc fix now.
**Plan version:** 3.24 _(re-synced 2026-08-05 by `P5.0` — it had drifted again to 3.22 against the plan's 3.23. Finding A4 fixed this once on 2026-07-30, when it read 3.17 against 3.21, and it drifted on the very next plan revision. Same class as the PRD footer version line (register ref R-01), fixed twice and drifted twice: a figure duplicated in two files will diverge unless something checks it.)_
**Overall status:** In progress
**Target launch:** Not committed — was 31 July 2026; WJ decided 2026-07-05 that launch will not happen until Phase 6 (P6.1–P6.5) is complete. No commercial deadline forces the original date. Working estimate: August–September 2026. (Originally P6.1–P6.6; P6.6 retired 2026-07-14.)

Update this file as tasks are completed. Change `[ ]` to `[x]` for completed items and update the **Last updated** date above.

---

## Summary

| Phase                                                                        | Tasks | Done  | Status                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------- | ----- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Phase 6 — Application Item-Graph Rearchitecture**                          | **7** | **6** | **In progress — now required before launch (ADR-DATA-006, revised 2026-07-05); guideline source-reference feature blended in 2026-07-10; P6.6 retired 2026-07-14; P6.7 excepted, ongoing by design**             |
| &nbsp;&nbsp;P6.1 — Profile schema extension                                  | 1     | 1     | ✅ Complete (2026-07-05) — dev only, see note below                                                                                                                                                              |
| &nbsp;&nbsp;P6.2a — Guideline page/section reference extraction (groundwork) | 1     | 1     | ✅ Complete (2026-07-14) — see note below                                                                                                                                                                        |
| &nbsp;&nbsp;P6.2 — Item-graph data model (compatibility mode)                | 1     | 1     | ✅ Complete (2026-07-14) — dev only, proven against MK Community Foundation — Oak Grants, see note below                                                                                                         |
| &nbsp;&nbsp;P6.3 — Extraction rewrite                                        | 1     | 1     | ✅ Complete (2026-07-14), first milestone — live-verified against the real MK Community Foundation PDF; extended 2026-07-21 with an `[ITEM N]` fallback marker for headless guidelines (Wolfson), see note below |
| &nbsp;&nbsp;P6.4 — Step 4 rendering rework                                   | 1     | 1     | ✅ Complete (2026-07-14), first milestone — live-tested by WJ (found and fixed a quote-highlighting bug same day); citation badge extended 2026-07-21 to render the new `'item'` citation type, see note below   |
| &nbsp;&nbsp;P6.5 — Reuse previous application (private, per-charity)         | 1     | 1     | ✅ Complete (2026-07-14) — dev only; supersedes the originally-planned shared playbook design, see note below                                                                                                    |
| &nbsp;&nbsp;~~P6.6 — Transparency status~~                                   | —     | —     | 🚫 Retired (2026-07-14) — not built, will not be built; see note below                                                                                                                                           |
| &nbsp;&nbsp;P6.7 — Funder-by-funder capability extension (ongoing)           | 1     | 0     | Not started — no completion state; does not block launch                                                                                                                                                         |
| **Phase 6 → Go-Live Gate**                                                   | —     | —     | ⏳ Not yet reached — requires P5.1–P5.5 and P6.1–P6.5 complete                                                                                                                                                   |
| **Total (Phase 6 only)**                                                     | **7** | **6** |                                                                                                                                                                                                                  |

For the full Phase 0–5 breakdown see [IMPLEMENTATION-STATUS-ARCHIVE.md](IMPLEMENTATION-STATUS-ARCHIVE.md). **Phases 0–4 are complete. Phase 5 is _not_** — corrected 2026-07-30; this line previously read "(all complete, 97 tasks)", so the live status document read as though Phase 5 were finished. Phase 5 stands at **9 tasks, 2 done** (`P5.PERF1`, and `P5.0` completed 2026-08-05), plus GAP-24 built out of the new `P5.3b`. The old "13 tasks, 7 done" counted the six funder-directory tasks that `DR-FD-001` v1.4 reversed on 2026-07-15. See the recount note in the archive and `phase-5-restructure-proposal-2026-07-29.md` §2.1.

---

## Notes

| Date       | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-07 | **AC-01's results banked two days late, and `GAP-49` raised — the first WCAG Level A failure this project has recorded.** AC-01 swept rows 1–20 of 24 on 2026-08-05; **nothing was written into the repository** — the plan's results table was still empty checkboxes and the findings existed only in a session scratchpad under the OS temp directory, which is cleared without warning and belongs to no version control. Now banked in `accessibility-test-plan.md` **v1.3** with per-row detail, AC-01 marked **Fail**, and the plan flipped 🟡 → 🔴 on `TEST-DASHBOARD.md` **v2.24** (suite: 3 🟢 / 1 🟡 / 3 🔴). **Four defects, and they are four different kinds of thing.** `DEF-01` is **one design token** — `#94A3B8` fails AA on every background it is used on, so it reads as five findings and is one decision, **and that decision is WJ's**; recorded and left open rather than fixed by picking a darker grey. `DEF-02` is **one component, six instances** — the 16×16 show/hide-password eye button. **`DEF-03` is the one that matters: `GAP-49`**, `scrollable-region-focusable`, WCAG 2.1.1 **Level A** — the Step 4 guidelines citation pop-up has no `tabIndex` on its scroll container, so a keyboard-only user opens it, sees the first screenful and can reach nothing below the fold. **The dialog's entire purpose is showing the applicant the funder's original wording, so for that user it does not degrade — it fails at the single thing it exists for.** `DEF-04` is the broken `@axe-core/react` auto-reporting, which is the harness rather than the product and feeds `GAP-15`. `GAP-49` filed 🟡 against `P5.3`'s existing "run axe and fix all violations" bullet, naming the specific defect so a re-run that never opens the dialog cannot sign it off; do it with **AC-05**, the focus-management case for the same component. **Twelve rows remain unswept and two `incomplete` findings need a human verdict** — run 2 can only add defects. **Five wrong instructions in AC-01 corrected** (`Ctrl`+`Enter` does not run a snippet once focus leaves the editor; `copy()` does not exist inside a snippet; row 9's application-row menu does not exist; row 17's two Step 2 sweeps are one; row 5's sweep prints the pre-redirect path), plus Incognito made a prerequisite and an animation-freeze guard added to the sweep snippet. **The delay is the finding.** For two days a Level A failure had no number, no row and no task while the register and the dashboard both read as complete — the dashboard saying "not yet executed" on 2026-08-06, a day after the run had failed. Numbering was checked against a freshly fetched `origin/master` first, per the `GAP-39`/`GAP-40` collision of 2026-08-06.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-06 | **`GAP-32` scoped properly and split three ways — its own description turned out to be wrong.** WJ asked what tidying it up needed. The gap read "`FR-22` and its three acceptance criteria (`AC-FR-22-01/02/03`) reworded — not yet scheduled"; **all four had been done on 2026-07-10/14**, along with `moscow-feature-register.md`, and `ADR-DATA-002` already records it. The real work was six other documents, found by grepping the whole `docs/` tree rather than trusting the gap text — **a gap's own text is a claim, not evidence**, and anyone closing this by checking only the four things it named would have "closed" it wrongly. **A (built today):** `data-model.md` ×2 — the key-constraints bullet and §7's "Data Not Stored" table, which had contradicted **that same document's** §4a and ERD since 2026-07-14; `ui-inventory-and-data-contracts.md` Screen 2; and `AC-FR-22-04`, whose closing clause claimed the text "isn't surfaced back to the user anywhere yet (`P6.4` builds the viewer that will)" — written the same day `P6.4` shipped that viewer, with a stale `GAP-34` reference (closed 2026-07-25) alongside. **B (drafted, pending WJ):** `ADR-ARCH-004` + `ADR-FILE-004` as a pair, per `ADR-ARCH-004`'s own note; both promised a real update "once `P6.2a` ships" and it shipped 2026-07-14. Corrections stated in new dated notes rather than edited into the Context, per this repo's convention. Neither decision changes — `ADR-ARCH-004` chose the DB as primary state store _despite_ the guidelines being unstorable, so removing that weakness strengthens it. `ADR-FILE-004`'s note states the one real open question (should the `sessionStorage` round-trip go?) and deliberately leaves it to WJ, recommending no change before go-live. **C (tasked to `P5.4`):** the PRD `FR-22` row is the one place still carrying the old wording **legitimately** — "true of the product as it exists in production today" is accurate, since the retention migration is on `grant-pathway-dev` only. Fixing it now would make the PRD wrong the other way, so `P5.4` gains a trigger under its migration-push step saying explicitly not to fix it early. `GAP-32` moves 🔴 → 🔵. **Register: 48 rows — 31 🟢 · 8 🟡 · 5 🔵 · 4 ⚪ · 0 🔴.** `ADR-INDEX.md` unchanged (both ADRs remain ✅ Decided; only their Context was corrected). No code changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-06 | **All four register defects fixed; gaps register sorted numerically and colour-coded (WJ's request).** `ADR-TRACEABILITY.md` → **v2.32**. **`GAP-32` added to the register** — it had existed only in the `ADR-DATA-002` consequence table since 2026-07-10 and is now the register's **only 🔴**: `FR-22` and `AC-FR-22-01/02/03` still state the retired "never store guidelines" rule, four weeks after that ADR was reversed and guidelines began being retained in `application_guidelines`. It still has no task; that is what 🔴 means, and it is now visible. **`GAP-01`/`GAP-02`/`GAP-04` corrected to ✅** in the ADR consequence tables, each verified in the code first rather than on the register's word (`lib/prompts.ts`, whose header comment says "GAP-01 resolved"; the `p += (89 - p) * 0.04` progress bar in `components/application-step3-summary.tsx`; `httpStatusForError()`/`aiErrorBody()` in `lib/ai-error-handler.ts`). **Task column re-pointed** — `GAP-21`/`GAP-22`/`GAP-23` → `P5.3b`, `GAP-12` → `P5.6`, each recording what the cell used to say. **`GAP-27` given the 🔵 it never had** (its cell read the bare word "Partial", predating the 2026-07-30 key, so it was invisible to any scan for non-built rows). **Sorted numerically** — the table had drifted into insertion order (…31, 30, 35, 38, 37, 36, 33, 39…), a contributing condition to both `GAP-32` going unseen and the duplicate `GAP-39`/`GAP-40` issued by two parallel sessions. **New Status column**: 🔴 No task · 🟡 Tasked only · 🔵 Partial · ⚪ Accepted · 🟢 Built — the same five categories established 2026-07-30, with discs for contrast, not new meaning. **🟡 is the point of it**: 📋 is a beige clipboard, so the one category meaning _promised but does not exist_ looked the mildest thing in the table, which is how the Phase 4 → Phase 5 gate signed off nine unbuilt gaps as resolved. Discs are paired with words because colour alone is not an accessible signal (`NFR-06`). **Register now: 48 rows — 31 🟢 · 8 🟡 · 4 🔵 · 4 ⚪ · 1 🔴**, numbers 01–48 complete and each used once. The v2.31 gate row was annotated, not rewritten, so the finding and the fix stay legible as separate events. No code changed; no task status changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-06 | **Phase 5 does not audit the gaps register, and the phase gate that should have covered it stopped running.** WJ asked the question; all eleven Phase 5 tasks were checked and none touches `ADR-TRACEABILITY.md`'s register. `P5.0`'s reconciliation covered the PRD, BRD, NFRs, technical design, data model and acceptance criteria, naming that file only twice, both incidental corrections. The sign-off table's last row was **Phase 4 → Phase 5 (2026-06-17)** while Phase 6 has been running since 2026-07-05 with P6.1–P6.5 built, so the sweep `AGENTS.md` §2 mandates before a new phase never ran for Phase 6. Register walked row by row: **31 ✅ · 4 partial · 8 📋 · 4 ➖ · 0 ⚠️** across 47 rows. **The 8 📋 are unbuilt work still owed** — `GAP-03`, `GAP-05`, `GAP-12`, `GAP-17`, `GAP-21`, `GAP-22`, `GAP-23`, `GAP-28`. Four defects found: `GAP-32` is absent from the register despite its preamble claiming to consolidate every ⚠️ row, and is the only number between 01 and 48 with no row — which also makes the register unsafe as the next-free-number source, the hazard behind the duplicate `GAP-39`/`GAP-40` issued by two parallel sessions that day; `GAP-01`/`GAP-02`/`GAP-04` read ⚠️ in the ADR consequence tables and ✅ built in the register, and the ADR-table half is the one `AGENTS.md` §2 sends sessions to; the Task column still points at pre-restructure tasks (`GAP-21`/`GAP-22`/`GAP-23` → `P5.3` when they are `P5.3b`'s, `GAP-12` → `P5.4` when the `v1.0.0` tag moved to `P5.6`); and `GAP-27` carries no status symbol at all. **Two changes, both on WJ's instruction:** a retrospective Phase 5 → Phase 6 gate row in `ADR-TRACEABILITY.md` (**v2.31**), left unsigned on purpose because a gate written after the phase it guards must not be presented as one that ran on time; and **`P5.5` item 4** in `IMPLEMENTATION-PLAN.md` (**v3.25**), a real register audit before go-live with a dated artefact, given its own line on the Go-Live gate because it is the only gate item that checks whether the gate's own inputs are honest — **and the gate must not be signed with a 📋 row left unexplained**, which is exactly what happened at the last one. No task status changed in any phase; no work added to Phase 6. Incidental fix in the same pass: the v2.30 history row in `ADR-TRACEABILITY.md` had an unescaped `\|\|` in a code span that split it into six cells of a four-column table, which prettier then padded with 1,941 spaces so it looked correct — same failure class as the `\n` corruption found in this file the same day.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-06 | **`GAP-48` fixed — the `guidelines-temp` storage RLS policies now actually grant, for the first time since 2026-05-19.** All three gated on `(storage.foldername(name))[1]`, assuming a `<user_id>/` folder layout the app has never written; with no `/` in the flat `{user_id}_{timestamp}` names, that subscript is NULL and **every request was denied for eleven weeks**. Nothing was exposed and nothing broke, because no client relies on them — the bucket is private and all real access is service-role, which `ADR-FILE-001` accepts on its own. The defect was dead code reading as load-bearing. Migration `20260806000000_gap48_storage_rls_flat_prefix.sql` repoints them onto `starts_with(name, (select auth.uid())::text \|\| '_')`. **Repointed rather than dropped (decision: WJ)** — nothing depends on them today, but they are the tripwire for whoever later adds a client-side Storage call and assumes per-user scoping exists. `starts_with()`not`LIKE`, because `_`is a LIKE wildcard and` \|\| '_%'`would over-match`<uuid>Xanything`unless escaped; the trailing underscore is what stops one user id that prefixes another from matching, and the same rule now reads identically in the SQL,`lib/storage-guidelines.ts`and`app/api/upload/process/route.ts`. 🔵 **Not marked ✅ on purpose: the predicate has never been observed to grant in either form.** CI proves the migration replays, not that it works. Per `ADR-SEC-002`the live case is written into`GAP-17`/`P5.2`, naming two traps — use the anon key plus a user JWT, never the service role (it bypasses RLS and would make any test pass); and include a name starting with the user id but missing the underscore. **Also recorded: `20260622000003_rls_hardening.sql`swept every table's policies on 2026-06-22 and missed these**, because storage policies live on`storage.objects`rather than a`public` table — a table-by-table audit cannot see them. ⚠️ **Action for WJ: the migration is not applied to either Supabase project** (`supabase link` needs an interactive password prompt). Until then the inert originals stay live — the status quo, exposing nothing. No application code changed; suite unchanged at 205.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-06 | **`GAP-47` built — account deletion now deletes the Storage files it has always claimed to; and the path those files were documented at never existed (`GAP-48`).** Raised as a spec/code mismatch rather than a planned task: `PDR-DH-002` has listed "uploaded funder guideline files (Supabase Storage)" among the data deleted since 2026-04-16, and neither S8.2 (`/api/account/delete`) nor S8.3 (`cron/inactivity-deletion`) touched Storage — both deleted database rows only. `data-model.md` §6 had been carrying a note recording the mismatch rather than resolving it. Nothing was ever actually retained, because `cleanup-guidelines` removes anything older than an hour every 30 minutes; hence a mismatch, not an incident. **Decision: WJ, on the author's recommendation — fix the code rather than amend the PDR.** The argument: every other `cleanup-guidelines` failure is self-healing because the owning account still exists to re-derive the object-to-person link, but **once the account is deleted it does not** — the object name still embeds the user id, so it remains personal data with no user left to ask and nothing to join it to. That is the one window the fallback cannot recover from, and it is entered by an explicit erasure request. Amending a document carrying a UK GDPR commitment, to match code fixable in twenty-five lines, was the worse direction of travel. Applied to **both** paths even though the cron's is a near-certain no-op (24 months idle against a bucket holding only sub-hour files), because the two routes are deliberately maintained as mirrors and **both** bugs previously found in this cascade came from them drifting apart — `GAP-33`'s missing table, and the missing `service_role` grants that made every deletion fail on 2026-07-23. New `lib/storage-guidelines.ts`; called **before** the table cascade, so nothing is yet destroyed if Storage fails; failure goes to Sentry as a warning but **deliberately does not fail the deletion**, since the sweep remains the backstop and letting a defence-in-depth step abort an erasure request would make it a new single point of failure (the same posture `/api/upload/process` already takes). **⚠️ The most useful finding is that the documented object path does not exist.** The brief for this task, `data-model.md` §6 **and** §7, and the schema migration's own comment all described objects as living at `guidelines-temp/<user_id>/<filename>`. They are **flat at the bucket root** — `{user_id}_{timestamp}`, written that way deliberately so `cleanup-guidelines`' `list('')` needs no recursive traversal, and it is the identical prefix `/api/upload/process` uses as its IDOR guard (`__tests__/upload-idor.test.ts`). **A fix written against the documentation would have matched nothing, deleted nothing, and returned success** — replacing the mismatch with code that looks compliant and does nothing, which is strictly worse than the gap it closed. All three places corrected, and a test asserts the folder form is rejected so the assumption cannot quietly return. **`GAP-48` raised, not fixed:** the same assumption is baked into the schema, where all three `guidelines-temp` RLS policies gate on `(storage.foldername(name))[1] = auth.uid()::text` and therefore **have never granted anything** — with no `/` in the name that subscript is NULL. No live exposure: the bucket is private, service-role-only access is the control actually in force, and `ADR-FILE-001`'s consequence explicitly accepts "a storage RLS policy **or** service-role-only access". The defect is dead code reading as load-bearing, plus §7's positive claim of "per-user RLS-scoped folders restricting each user to their own uploads", wrong on both counts. **Left for its own task deliberately** — altering a security control should not ride along on a documentation correction, which is how the wrong comment arrived in the first place; recommendation recorded is to repoint the policies at the flat prefix rather than drop them, as a tripwire for whoever later adds a client-side Storage call. The applied migration was corrected by **comment only**, no SQL touched; a real fix needs a new migration. **14 new tests (suite 191 → 205, measured after merging the parallel session's work);** type-check, lint and `prettier --check` clean. **No user-facing change, and none intended:** `AC-FR-41-03` deliberately omits uploaded guidelines from the deleted-data list on the confirmation screen (corrected 2026-07-13, consistent with `FR-22`) and that stays right — these files exist for seconds during extraction and listing them would imply a persistence the product does not have; recorded in `PDR-DH-002` so nobody reconciles the two lists by editing the screen. `regression-test-plan.md` → v2.12 (RT-14 optional Storage add-on, describing how to catch a file mid-flight since the bucket is empty by the time the normal steps run), `TEST-DASHBOARD.md` → v2.18, `AGENTS.md` §1 gains a worktree note (worktrees have no `node_modules`, so the Next.js docs are read from the main checkout rather than presumed missing — the §1 paths themselves are correct). No task status changed: `GAP-47` sat outside the Phase 5 list, and `GAP-48` is open and unscheduled.                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-06 | **Package C: `GAP-39` and `GAP-40` rules rewritten — code complete, 🔵 not ✅, and the distinction is the point.** **`GAP-39` is fixed at the level of the principle, not with a third exception.** The exclusion list was about **administrative identity and contact details**, but read as though it were about **answer length** — which is how three questions were dropped across three funders (MK Community Foundation’s funding amount, Idlewild’s table row, and now Stony Stratford’s beneficiary count and project duration). The first two were each closed with a budget-shaped exception; **being the sole exception is exactly why the third had nothing to appeal to.** The rule now states that the test is what a question is _about_, never how long its answer is, names the lost cases, forbids dropping on answer shape alone, and ends "when in doubt, extract". Budget/cost is recast as a **case** of the rule. The TABLE FORMAT skip-list — which carried its own copy of the flaw, the reason Idlewild survived the same-day MKCF fix — is now subordinated to it. **`GAP-40`: the existing "DO NOT MERGE" rule did not bind for an understandable reason** — neither of §10’s sub-parts reads as a complete question alone (the stem "Please state what you hope to have achieved:" sits above both), so merging looked like the only coherent option. The fix supplies the missing option: **combine the stem with each sub-part**. It also states a bare section heading is not a question, keeping §1’s correct non-extraction correct rather than inviting an over-correction. **9 new prompt tests, suite 176 → 185 — and every one asserts a rule is _present_, not what the model _does_ with it.** ⚠️ **A live extraction was attempted and could not run: the AWS credentials in `.env.local` are rejected by Bedrock with a 403 signature mismatch** (correctly shaped at 20/40 chars, so almost certainly rotated since they were entered by hand on 2026-08-04); `AWS_REGION` is empty there too, though harmless. **This blocks all local AI testing, not just this check, and is worth fixing on its own account.** So GCM-06 stays **Fail** and the capability matrix stays **🔴**. A re-run must confirm §4 gives six cards including §4e/§4f and §10 gives two — **and that GCM-01–05 still pass**, since a rule loosened to stop dropping short answers is exactly what might start extracting contact details instead. **Also recorded: `ADR-TRACEABILITY.md` was briefly truncated to empty** by a scripted edit that opened it with `'w'` (truncates on open) then raised before writing, with a follow-up script reading the empty file and writing it back; the empty version reached commit `fd80101`. Caught at the start of package C, restored from `77dc577`, lost v2.27 content re-applied, and scripted rewrites of that register now guard on line count and output size. All eleven documents touched today were checked; no other was affected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-08-06 | **BUILT: `GAP-41` (Word export line breaks) — and the check its task note demanded found a second defect, new `GAP-46`, fixed in the same pass. Suite 163 → 176.** The export passed each whole answer to the `docx` library as a **single `TextRun`**, and that library ignores `\n` inside a run, so every paragraph break, blank line and hyphen bullet was silently discarded. Now one run per line with`break: 1` on all but the first, CRLF normalised first. Helper moved to **`lib/docx-text.ts`** — not tidiness: it lived inside the route handler where nothing could test it, which is part of why this shipped and then survived a passing RT-09. **The verification is the interesting part.** The tests generate a **real `.docx`**, unzip it and count `<w:br/>`, because the defect was invisible at the level of "did we pass the right string" — the string was always right, the bug only existed in the file. Running the old path the same way confirms it: **zero `<w:br/>`**, with the answer in one node as `<w:t xml:space="preserve">one\ntwo\nthree</w:t>`. The newlines were in the XML all along; WordprocessingML treats a literal newline inside `<w:t>` as whitespace. Any assertion stopping at the run level would have passed against the broken code — **which is exactly how RT-09 passed over this on 2026-07-28**. **`GAP-46`:** `buildRefinePrompt()`opens by asking the model to "Improve the **structure**, flow, and clarity" and said nothing about preserving layout, so "Help me improve this" was licensed to reflow a bulleted answer into prose. **Fixing only the export would have made the export fix look broken** — the layout would have been destroyed one step earlier, in the database, and the export would have faithfully reproduced the flattened version, sending the next investigation back to code that was by then correct. Fixed with an explicit`PRESERVE THE APPLICANT'S LAYOUT`instruction that also **disambiguates the word that caused it**: "structure" means argument and sentence construction, never visual layout. 13 new tests; type-check, lint and`prettier --check`clean. ⚠️ **RT-09 stays Pass (caveat) and still needs a human** — no automated test opens the finished document in Word, and this defect was originally found by a person reading her own download.`regression-test-plan.md`→ **v2.13**; it stays amber on the dashboard until WJ re-runs RT-09 with a genuinely multi-line answer.`ADR-TRACEABILITY.md` → **v2.27**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-06 | **BUILT: `GAP-42`, `GAP-43`, `GAP-44`, `GAP-45` — the first code to land from the Stony Stratford review. 18 new tests, suite 145 → 163.** Step 4's sub-heading now reads _"Answer each question below. Your answers are saved automatically. You can close this page at any time and continue from your dashboard."_ (free-form equivalent alongside), dropping the false "as you type" and adding the resumability sentence that was never there. Every successful save — blur, the 60-second sweep, the flush before approve — shows a per-answer **Saved** tick that clears itself after 2.5s with nothing to dismiss; it sits on governance `£` fields too, which carry no word counter, because those are answers as much as narrative ones. The nav Help button deep-links to the current screen's help page via `usePathname()` and a new `helpPathForRoute()`, finally calling the `helpCentreUrl(path)` helper written for this and never once invoked with a path; footer and empty-state links still open the root deliberately. **Two things from the build worth keeping.** (1) **A lint failure caught a real bug, not a style nit:** extracting the "Saved" logic to a `markSaved()` helper made `react-hooks/exhaustive-deps` demand `doSave` as a dependency of the 60-second sweep effect — the rule does not propagate stability through a component-scope function — and adding it would have **restarted the interval on every render**, quietly breaking the background save this package exists to reassure users about. Inlined into `doSave` instead. Lint was clean beforehand, so it was introduced and caught, and `--max-warnings 0` is why it was visible. (2) **All eight GitBook targets were fetched live and title-checked**, not inferred from the sitemap — a slug that parses is not a page that exists, and a wrong path 404s where the old root landing at least worked. Type-check, lint and `prettier --check` clean. ⚠️ **Not verified in a browser: Step 4 and the nav are behind sign-in and the author does not enter credentials.** Confirmed live instead: the public nav and footer Help links still open the root, as intended. **HT-06 (now "not yet executed") and a look at Step 4 are WJ's to run.** Docs: `ADR-TRACEABILITY.md` → **v2.26** (all four ✅), `PRD-Grant-Pathway.md` → **v0.68** (earlier "agreed, not yet built" flags cleared), `acceptance-criteria.md` (`AC-FR-18-05` built; **`AC-FR-49-01` amended** for the deep-links), `design-requirements.md` §7.8, `ui-inventory-and-data-contracts.md`, `help-and-tooltips-test-plan.md` → **v2.2**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-06 | **`GAP-45` logged — the nav Help button always lands on the help centre root, whatever screen you are on. Slugs fetched and verified; not built.** WJ's point: pressing Help from Step 4 should open "Writing and editing an answer", not a front page the user then navigates from. **The plumbing has been there all along and was never connected** — `lib/help-centre.ts` exposes `helpCentreUrl(path)` for exactly this and its header comment names the use case almost verbatim ("linking Step 2's upload screen straight to the relevant help page"), but `nav-authenticated.tsx` links to the bare `HELP_CENTRE_BASE_URL` constant, so **the deep-linking helper is unused at the one place that most needs it**; the only caller anywhere is `dashboard-empty.tsx`, with no path. `nav-authenticated.tsx` is already `'use client'`, so `usePathname()` supplies the route with no prop drilling — a route map in `lib/help-centre.ts` plus one lookup. **Slugs verified, not guessed:** the live GitBook sitemap was read (21 pages) and the Step 4 target fetched to confirm it resolves, because a wrong path 404s where today's root landing at least works. Map: Step 1/new → `applications/choosing-your-funder-and-grant`; Step 2 → `applications/uploading-funder-guidelines`; Step 3 → `applications/reviewing-the-ai-summary`; **Step 4 → `writing-answers/writing-and-editing-an-answer`**; Step 5 → `finishing-up/final-review`; `/profile` → `getting-started/setting-up-your-charity-profile`; `/account` → `account-settings/changing-your-password`; `/account/delete` → `account-settings/deleting-your-account`. **Two calls left for WJ:** `/dashboard` has no clean match (nearest `reference-and-faqs/application-status-labels`; default root), and whether the public routes deep-link too. Footer and empty-state links stay on the root deliberately — general-purpose, whereas the nav button means "help me with _this_ screen". **Six of the 21 help pages cover Step 4 alone**, so landing users in the right section and letting GitBook's sidebar finish the job is the intended outcome, not a shortfall. **⚠️ Maintenance risk owned rather than implied:** the help centre is an external GitBook, so a rename silently 404s one route's Help button, **nothing in CI can catch it**, and no client-side fallback exists because a GitBook 404 is invisible to the app. Mitigations now in place: the whole map lives in one file, and **`help-and-tooltips-test-plan.md` → v2.1 gains HT-06** — click Help on every screen, re-run whenever the help centre is restructured — written now and marked **not runnable**, so coverage cannot be lost between logging and building. **`AC-FR-49-01` needs amending at build time** ("opens the help centre" → _the relevant page of_ it); left unchanged for now because it is accurate today. `ADR-TRACEABILITY.md` → **v2.25**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-06 | **`GAP-44` unblocked — WJ withdrew `AC-FR-18-02`'s no-visible-indicator rule, and all six statements of it across four live documents were amended in one pass. Still not built.** Offered two options, WJ took the broader: **every** successful save is visibly confirmed, background sweep included, not just blur and explicit saves. **His reasoning overturns the assumption the original rule rested on** — silence was chosen to avoid nagging the user with a periodic indicator, but weighed against a real applicant's hesitancy to close a tab containing hours of writing that is the lesser concern: **silence reads as "nothing is happening", not "everything is fine"**, and the user who steps away mid-sentence _without_ moving focus is precisely the nervous user this is for, so the narrower option leaves exactly the wrong person with no signal. **The withdrawn rule's intent survives as a constraint on `AC-FR-18-05`:** unobtrusive, no interruption, no focus theft, nothing to dismiss, visually distinct from `AC-FR-18-04`'s "Not saved." alert — only the assumption that silence achieves it is dropped. **Amended as a class, not an instance:** `acceptance-criteria.md` (AC-FR-18-02 rewritten with a full amendment note; AC-FR-18-04's "deliberate exception" note rewritten, since it described an exception to a rule that no longer exists; **AC-FR-18-05 promoted from draft to criterion**), `PRD-Grant-Pathway.md` → **v0.67** (FR-18 implementation notes and §7's auto-save line), `design-requirements.md` §7.8, `ui-inventory-and-data-contracts.md`. Changing one and leaving five contradicting it would have been the **eighth** recorded instance of this project's fix-the-instance-not-the-class failure — the seventh was `GAP-43`, hours earlier. `ADR-TRACEABILITY.md` → **v2.24**. `GAP-42`/`GAP-43`/`GAP-44` all still open, no covering task; test coverage to be added when built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-06 | **`GAP-42`, `GAP-43`, `GAP-44` logged — Step 4 never tells a user they can stop and come back. All three approved by WJ, none built, and `GAP-44` is blocked on a spec conflict.** WJ raised the narrow point after watching his wife work: the per-card Approve buttons and the "N of N questions approved" bar read as "finish this in one sitting", and nothing says a part-written application can be abandoned and resumed. **`GAP-42` — the capability works and was simply never stated:** the dashboard's Continue link goes to `/applications/[id]/step/[currentStep]` and Step 4 rehydrates every answer and its approved state. Agreed wording: _"Answer each question below. Your answers are saved automatically. You can close this page at any time and continue from your dashboard."_ **"Continue from your dashboard" is deliberate** — return is to the _step_, not the question, so with 17 questions the user still scrolls to find their place; "return to the exact point" would be a promise the product does not keep. **`GAP-43` — the string being replaced was false, and the PRD contradicted itself.** "Saved automatically **as you type**" is not the behaviour: `handleAnswerChange` only sets local state and marks dirty; writes are on **blur** plus a **60-second sweep**, with no debounce and no `beforeunload` guard anywhere. The behaviour is correct — `AC-FR-18-03` explicitly accepts "at most 60 seconds of edits potentially lost" — and the PRD's own FR-18 notes describe blur + sweep accurately, **including a 2026-07-13 correction that specifically disproved a debounce claim, fixed the implementation note, and never checked the user-facing string specified in §7 of the same file.** PRD-versus-PRD, not code-versus-copy, and the **seventh** recorded instance of fixing an instance rather than the class. The one replacement string closes both gaps. **`GAP-44` — nothing ever confirms a save succeeded**, so `GAP-42`'s reassurance is an assertion the screen never demonstrates; the only save feedback in the component is the "Not saved." failure path (`AC-FR-18-04`, 2026-07-29). WJ approved a visible confirmation, **but `AC-FR-18-02` requires background saves to show no visible indicator.** Two resolutions written into `acceptance-criteria.md` as **draft `AC-FR-18-05`** for WJ to choose: (1) scope the indicator to blur/explicit saves and leave the sweep silent — satisfies every existing criterion unamended, **recommended**; or (2) amend `AC-FR-18-02`, which reverses a deliberate decision and reintroduces periodic noise. **Do not build `GAP-44` until that is closed.** `PRD-Grant-Pathway.md` → **v0.66** (§7 respecified, agreed-not-built, live string recorded alongside), `ADR-TRACEABILITY.md` → **v2.23**. Test coverage to be added when built, not before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-06 | **`GAP-41` (High) logged — the Word export destroys every line break in an answer — and rich-text editing considered and deferred as `FP-11`. No code changed, on WJ's instruction.** Third defect from the same real Stony Stratford application. His wife laid out her §4b answer (215 words) with blank lines, a hyphen-bulleted list of facilities, a "Funding to Date" block and a worked calculation; the downloaded `.docx` rendered all of it as one continuous run of prose. **Confined to the Word generator, which is what makes it cheap:** the `<Textarea>` stores `\n` intact, Step 5's preview renders correctly via `whitespace-pre-wrap`, and `format=txt` is unaffected because it joins an array of lines — but `app/api/export/[applicationId]/route.ts` puts the whole answer body into a **single** `TextRun` in both its branches, and the `docx` library ignores `\n` inside a run. Agreed fix (not built): one `TextRun` per line with `break: 1` on all but the first — a ~ten-line helper in two places, no schema change, no new dependency, covering the budget narrative field too. **Check in the same pass whether "Help me improve this" preserves line structure**, since the refine prompt takes and returns plain text with no layout instruction. **A test passed straight over this and that is its own finding:** `regression-test-plan.md` RT-09 ran against this exact code on 2026-07-28 and passed, verifying "no corrupted or missing content" — a bar single-paragraph answers clear while every line break is discarded. It was found by a user reading her own file, not by the plan whose job it was. RT-09 and RT-10 gained an explicit multi-line fidelity step (**v2.12**, Defect Log **D-015**); RT-09 downgraded to **Pass (caveat)** and must be re-run once fixed. `ADR-TRACEABILITY.md` → **v2.22**. **`FP-11` — rich-text formatting deferred by WJ**, on the reasoning that most charity workers paste into funder web portals which strip formatting anyway, so it is only valuable where a downloadable Word/PDF form is still required — not the common path and not a demonstrated demand, while fixing `GAP-41` already delivers line breaks, blank lines and hyphen bullets, which do survive a portal paste. Full implementation cost written into `future-phases.md` so it is not re-derived. **No DR raised** — nothing in any ADR, PDR or requirement promises rich text, so no decision is being reversed. Scope note recorded: "all fields including budget" means all _narrative_ fields; the budget narrative (§5a) is an ordinary `Textarea` and in scope, the two governance `£` cards are numeric inputs where formatting has no meaning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-06 | **`GAP-39` (High) and `GAP-40` (Medium) logged from a real, live grant application — the first defects on this project found in genuine user work rather than a scripted test. No code changed, on WJ's instruction.** WJ's wife completed a real Stony Stratford Town Council application (Stony Stratford Community Larder) through the live service, uploading `Stony Stratford Grant-Application-Form-2026.docx`. Reconciling her 17 completed answers against the source form: the app produced **2 governance facts + 15 questions** where the form has 17 narrative asks. **`GAP-39`:** §4e ("an accurate figure for the number of people in the parish the project will serve") and §4f ("for how long will the project run?") were **never extracted** — dropped by `lib/prompts.ts`'s data-entry/numeric exclusion, whose only carve-out is the BUDGET/COST exception. For a parish grant, beneficiary reach is close to the whole decision. **This is the third instance of the same failure mode** — a substantive question dropped solely because its answer is short — after MK Community Foundation and Idlewild on 2026-07-27, and each of those was closed with its own narrowly-scoped exception rather than by fixing the rule, which is why a third flavour got through. Any fix should change the rule, not add a third exception. **`GAP-40`:** §10 a) and b) (six- and twelve-month achievements) merged into one card, against the prompt's own verbatim "DO NOT MERGE ADJACENT QUESTIONS" rule — Medium because nothing is lost to this applicant, but a per-question word limit would make it material. Note the two need different remedies despite sharing a file: one is an inadequate exception, the other an adequate rule not being followed. **Checked and correct, recorded so they are not re-investigated:** §1 "Purpose of application" is a heading with no ask beneath it and is legitimately absorbed by §2; §7's reserves-justification prose is present as the Reserves governance card's helper text (`PDR-AI-008`); all non-narrative material (tick-list, budget table, address, contact block, documents checklist, signature blocks) was excluded correctly; and the absent "Finances of Your Group" catch-all is out of scope, not regressed, since `PDR-AI-010` governs `free_form` sections mode and this extracted in questions mode. **Test coverage added same day:** new **GCM-06** in `guideline-capability-matrix-test-plan.md` (v1.5) — a docx **application form** rather than guidance about one, the only fixture exercising lettered sub-question flattening and short-answer narrative asks; recorded **Fail**. `TEST-DASHBOARD.md` (v2.18) moves that plan 🟢 → 🔴, its first red since the 2026-07-16 reset. **Cost to the user, which is the point:** §4e and §4f will be blank when she transcribes to the funder's real form, discoverable only by reading the original — the thing the product exists to spare her.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-05 | **Terms of Service → v1.6, at WJ's request, ahead of solicitor review.** Section 5's no-guarantee-of-funding-success sentence — _"We make no representation that using Grant Pathway will result in a successful grant application"_ — was sitting mid-paragraph between two unrelated statements. Given its own `### No Guarantee of Funding Success` sub-heading, unchanged wording; the AI-provider/data-processing paragraph moved earlier in the section so it isn't left nested under the new heading. Applied to `terms-of-service.md` and `terms-of-service-external.md` (the file `/terms` renders — checked live, heading confirmed directly above the guarantee text). PDF/docx regenerated (`Grant-Pathway-Terms-of-Service-v1.6.{pdf,docx}`, 7 pages), superseded v1.5 files deleted. Privacy Policy not touched — out of scope for this request.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-05 | **All four legal PDF/docx files in `docs/legal/pdf/` reported unopenable by WJ, needed urgently for a solicitor — regenerated.** Structural validation on the committed files found nothing wrong (valid PDF xref/trailer, `pypdf` extracted text from every page, `docx` zip integrity clean), so whatever WJ hit could not be reproduced from the repository side — the cause sits somewhere downstream of git (viewer, transfer, or the local copy open at the time) and was not chased further given the urgency. Regenerated anyway via the established pandoc → `.docx` → Word COM → PDF pipeline from the current external source markdown, no content change, verified page-by-page. Committed as `54459de`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-05 | **`GAP-31` built — `P5.3b` item 2.** The inactivity-warning cron had no record of what it had sent, and its eligibility test is a month-wide range checked daily, so an affected charity received the same account-deletion warning about **thirty** times — not the double-fire the 2026-06-08 idempotency audit described, and against `email-notifications.md`'s explicit "only one inactivity warning email is sent per inactivity cycle". Guarded by new `user_profiles.last_inactivity_warned_at`, compared against `last_sign_in_at` so it self-heals on sign-in; stamped after the send so the failure mode is a repeat, not a silence. The deletion cron now aborts with 503 before deleting anything when `RESEND_API_KEY` is unset — `lib/emails/send.ts` returns without throwing in that case, so its `try/catch` could never have caught the one configuration where every notification fails. Sentry now receives email, auth-delete and missing-address failures from both crons and from `/api/account/delete`, whose header comment had claimed exactly that since it was written. `lib/inactivity.ts` (new) holds the shared date arithmetic and both eligibility tests; 18 new tests (suite 127 → 145). **Action for WJ — apply the migration to `grant-pathway-dev`:** `supabase link --project-ref [dev-ref]` then `supabase db push`. This cannot be issued from a session because `supabase link` prompts interactively for the database password, the same constraint recorded on 2026-06-29 when the production migration repair was run from a real terminal. **Sequencing note:** this migration is now part of what `P5.4` must push to `grant-pathway-prod`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-05 | **`P5.0` requirements reconciliation executed — the blocking Phase 5 task, and the first time anything in Phase 5 compared the built service against the requirement documents.** Register: `pre-launch-reconciliation-2026-08-05.md`. **23 divergences, none undispositioned** — 15 fixed or amended the same day, **5 held for WJ's decision**, 3 accepted and recorded. `P5.0` remains **open** until those five are taken. **The completion flags now exist, which was the point of the task:** the PRD had been reviewed section-by-section against live code across versions 0.27–0.60 and the BRD across 0.8–0.49, and **neither document said so** — the PRD's only words about its own review status were two `(in progress)` phrases in its 0.27/0.28 rows, accurate on 2026-07-13 and 36 versions stale after. Both now carry a **Review Status — ✅ COMPLETE** block; the historical rows were deliberately left as written, being accurate as history. **⚠️ Act on this before `GAP-05` is built:** `NFR-05` and PRD §12.5 promise a **320px** minimum and usability on Chrome Android and Safari iOS, while `ADR-ARCH-005` sets **1024px** and `GAP-05` will add a banner below 768px that **blocks** the UI — and `P5.5`'s cross-browser step asks a tester to confirm mobile usability the banner makes impossible. Five live instances. **The sweep then found this exact conflict had already been formally resolved — in the retired plan** (`docs/Old/Imp Plan/IMPLEMENTATION-STATUS.md` OI-07: "ADR takes precedence… No action required") **and the resolution never reached the live document set.** Sixth recorded instance of fixing the instance rather than the class, and the worst, because the class fix already existed in writing. A second question falls out of the same ADR: it sets a 1024px minimum but places the banner at 768px, so the **768–1023px band** (iPad portrait, small laptops) is below the stated minimum with no banner and no supported layout. `NFR-05` carries a visible conflict block pending the decision rather than a silent correction. **A third meaning for ✅ was found, after "tasked" and "built": "present but inert."** `NFR-06` requires automated scanning on every new screen and `GAP-14` is marked ✅ "installed; wired into `app/layout.tsx`" — both true, and the control does nothing, because `axe-provider.tsx` swallows a React 19 incompatibility in a silent `catch`. Automated scanning has therefore not run on any screen built since the React 19 move. This is also the false half of `GAP-15`'s justification. **The £100 budget means three different things and is already exceeded:** `C1` says total infrastructure **and** API; the PRD variously says total API spend, AI API spend, and monthly running cost — and committed `P5.4` spend is Vercel Pro (~~£16) + Supabase Pro (~~£20) + a Bedrock ceiling of ≈£100 ≈ **£136** before Upstash, Sentry, Resend or Axiom. `A3` and `A13` rest on the same figure. **Three NFRs understated the service** rather than overstating it — the same shape as the Terms understating the AI cap: `NFR-04`'s password row omitted the leaked-password check, secure password change and current-password requirement; its session-timeout row omitted the 55-minute warning dialog and `D-013`; `NFR-01` had no target for the **charity-objects paraphrase**, the third user-facing AI operation. `NFR-03` measured concurrency when at ~100 users the binding constraint is **spend**. **Two smaller sweeps, both held for WJ:** the contextual-tooltip count is **10 in code against 9 in the test and audit trail**, and `help-and-tooltips-test-plan.md` is internally inconsistent (table lists ten, HT-02's Pass note says "all 9 confirmed"), so either one tooltip was never exercised or the note miscounts the table above it; and `PDR-UI-008` names three tooltip ids that exist nowhere in the codebase. **Items 2 and 4 came back clean**, validating the 2026-07-30 decision to narrow this task rather than run six full passes — the item graph, `application_guidelines`, citations and `first_exported_at` are correctly recorded across all three documents, `user_tooltip_dismissals` correctly logged as created then dropped, and `last_inactivity_warned_at` correctly absent everywhere (matching `GAP-31` unbuilt). Two exceptions recorded rather than dropped: `lib/site-url.ts` and `lib/action-error.ts` (both 2026-07-29, audit **M5**/**M8**, both architecture-level, neither in `technical-design.md` despite landing the day it was last updated), and `technical-design.md` stamped v1.24 with **no v1.24 row** in its history. **Documents changed:** `non-functional-requirements.md` → **v1.2** (gained its **first-ever revision history** — its absence is why the document had no review entry to point at), `PRD-Grant-Pathway.md` → **v0.65** (footer version line **deleted** rather than corrected a third time, having drifted after both the 0.7 and 0.23 fixes; revision rows reordered chronologically), `BRD-Grant-Pathway.md` → **v0.55**, `technical-design.md` → **v1.25**. No code changed; no Phase 6 task status changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-04 | **Pre-AC-01 environment verification, at WJ's request — the service is intact, but the axe harness does not report and AC-01 must not be run until it does.** Local `npm run dev` boots on Next 16.2.12 in 2.7s with all eight Sensitive secrets present, `/api/health` returns `{"status":"ok"}`, every route returns 200, and the auth round-trip reaches Supabase (an invalid credential returned the correct anti-enumeration message, so GoTrue is live rather than merely a page rendering). **Finding 1 — `@axe-core/react` is silently not reporting, which makes AC-01's pass condition unfalsifiable.** The dev console printed no axe output on `/`; axe-core run directly against the same page found **two serious violations** — `color-contrast` on the version string (2.44:1 against 4.5:1, SC 1.4.3) and `target-size` on the "Show password" button (16×16 against 24×24, also failing the spacing exception, SC 2.5.8). axe-core 4.12.1 loads fine (`window.axe` present and initialised); the React integration is what fails, and `components/axe-provider.tsx` wraps it in a **silent** `catch` for a known React 19 incompatibility, so a clean console is indistinguishable from axe never running. "Check the console, expect zero" would have returned a false Pass on a route with two real AA failures — and this retroactively explains HT-05's verbal "zero violations across eight routes" (2026-07-25) that AC-01's own scope note already refused to carry forward. **Not fixed:** needs a decision between making the failure loud plus explicit per-route `axe.run()`, or moving to `@axe-core/playwright`/the axe CLI, which would also remove AC-01's dependence on hand-walking twenty routes. **Finding 2 — `external-tester-brief.md` named no URL, and both obvious guesses are wrong:** `grantpathway.org.uk` is still a GoDaddy parking lander (confirmed live — the two A records outstanding since 2026-07-02 still need registrar access) and `grant-pathway.vercel.app` belongs to an unrelated product ("Find Grants for Your Canadian Business"). The working deployment is **`grant-pathway-three.vercel.app`**, verified serving this app with health green and no deployment-protection wall; added to the brief as a "Where to find it" section including the name-collision note. **Raised and closed the same day: Vercel production points at `grant-pathway-dev`** (confirmed by WJ). Unreadable from the repo (auth is entirely server-side) and it mattered because prod's Resend SMTP was never configured and holds none of Phase 6's migrations — had it pointed there, the tester could not have registered at all. Recorded because the same question recurs at P5.4, when the answer is supposed to change. **AC-01 was then rewritten (`accessibility-test-plan.md` → v1.1)** into an executable procedure: Step 0 harness gate (`window.axe && window.axe.version`; `undefined` = Blocked and a bigger finding than any violation), a **saved DevTools snippet** driving `axe.run()` explicitly — chosen over a pasted one-liner because it survives navigation and reloads across all 24 routes — and a **route table naming the conditional UI to render before each run**, which is the second and independent route to a false Pass: axe sees only the DOM as it stands, so a passively-visited route reports clean on UI that never rendered. Step 4 alone needs **seven runs** because its states do not coexist, including the M8 "Not saved" banner (forced via DevTools → Offline; it postdates HT-05 and has never been swept by anything). Also specified: forcing the session-timeout modal without waiting out its real 55-minute threshold, a `⚠️ do not confirm` on `/account/delete`, and `app/global-error.tsx` added to a route table that had missed it. **The two known violations are written in as a reproduction check, not examples** — a sign-in-page run that does not surface both means the tooling is broken, not the page clean, catching the original inversion one layer up. **Then rewritten a second time the same day (→ v1.2), for a reader with no developer-tools experience, and that is the more important revision:** v1.1 was precise but unusable, written in vocabulary that assumes the reader already knows the tool — **repeating, three cases later, the exact mistake this plan's NVDA section exists to prevent.** v1.2 opens Chrome, presses `F12`, names the tabs, warns about Chrome's `allow pasting` refusal on first console paste (an unexplained dead end for a first-timer), has the snippet copy its own result to the clipboard so recording is a paste not a transcription, and puts a **full worked example with the exact expected output before the route list** — with `0 problem(s)` there labelled a setup failure rather than a clean page. Snippet run against the live dev server before publishing; output matches the documented example exactly. **A correction, recorded because it is the mirror of the `GAP-15` one:** I flagged the GitBook guide's "four stages" as contradicting the app's five steps, citing the 2026-07-31 process-diagram fix. WJ corrected it — stages and steps are distinct items. The guide's four stages are its **chapter structure** (Getting started / Applications / Writing answers / Finishing up), deliberately not 1:1 with the steps, and "stage" is a live term elsewhere (§5.10 loading stages; funder-side Stage 1→2). Where `GAP-15` was a precedent missed by not checking the register, this was a precedent **over-applied** to a structure never checked. The guide was spot-checked and is current. **No Phase 5 or Phase 6 task status changed.** |
| 2026-08-03 | **`accessibility-test-plan.md` created — the seventh test layer and P5.3's output artefact (agreed 2026-07-30, written today at WJ's request).** v1.0, **15 cases (AC-01–15), not yet executed.** Covers all five of `ADR-OPS-006`'s manual pre-release items, the two guideline-viewer items in its Consequences, the five WCAG 2.2-specific criteria that land on built-but-untested features (Consistent Help, Accessible Authentication, Redundant Entry, Focus Appearance, Target Size), and `help-and-tooltips-test-plan.md` HT-05 step 4 — **absorbed as AC-08 rather than left deferred in two places**, so that plan's 🟡 now clears when AC-08 does and one screen-reader session closes both. **The NVDA setup-and-operation section is the point of the exercise:** the screen-reader step was Blocked on 2026-07-25 and attempted unsuccessfully on 2026-07-30 because NVDA was hard to operate, not because the testing was hard — so the plan carries install guidance, the browse-mode/focus-mode distinction, quick-nav keys, a symptom-to-cause table, and above all the **Speech Viewer**, which renders announcements as text so they can be screenshotted as evidence and read with the audio off. **Two findings arose from writing it:** `ADR-TRACEABILITY.md`'s only 🔵 ADR-OPS-006 row (the P6.4 guideline viewer, "built but never manually tested" since 2026-07-14, with no plan that would ever test it) is now assigned to **AC-05** — status deliberately left 🔵, since a test being written is not a consequence discharged (→ v2.18); and `design-requirements` §8.6 forbids `px` text sizing while the components use it throughout, so AC-15 records a likely real failure with its reasoning, unfixed, because it needs its own decision. **A correction to my own first draft, recorded because it is the failure mode this project keeps finding:** I flagged `ADR-OPS-006`'s absent Lighthouse CI as an uncovered consequence and proposed tasking it. It is `GAP-15`, an accepted deviation with WJ's 2026-06-16 decision recorded in two places. Checking the code without checking the register that records why. **Cannot reach 🟢 until `GAP-05`'s viewport banner is built** — that is P5.3's own development work. `TEST-DASHBOARD.md` → v2.17 (7 plans, 5 🟢). **No Phase 5 or Phase 6 task completed; P5.3 has its artefact but has not been executed.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-03 | **`Schema Drift Check` now skips prod on scheduled runs, until P5.4 (decision: WJ) — and P5.4's exit test was tightened in the same pass so the change cannot be mistaken for progress.** The prod leg failed all ten scheduled runs from 2026-07-24 to 2026-08-02, correctly, because Phase 6's migrations exist only on `grant-pathway-dev`; since a workflow's status is the worst of its jobs, the whole check read red daily and a genuine unrelated drift on **dev** would have been indistinguishable from the known prod failure — the audit's **M2** masking pattern one layer down. Skipping was available here only because this workflow is **not** a required status check (confirmed against the branch protection API: `lint-and-typecheck`, `test`, `validate-migrations`), so unlike M2/M6 no gate is left waiting on a check that never reports. The matrix now defaults to `["dev"]` via a `workflow_dispatch` choice input; prod stays one manual run away (`["prod"]` or `["dev","prod"]`) with no commit needed, which is what P5.4 will want while applying migrations. **The hazard is that the badge goes green while production is exactly as unprovisioned as before** — and P5.4's exit test read "the `Schema Drift Check` workflow green and staying green", which this would have satisfied vacuously. Tightened to require the prod leg switched back on, with re-enabling it added as its own P5.4 checklist item, placed after the migrations. Nothing about Phase 5 or Phase 6 progress changed today; **P5.4 is no closer to done.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-24 | **Change-password fixed: GoTrue requires `current_password` in the `updateUser` payload itself.** WJ tried to change the password on `grantpathway+walton1@gmail.com` via Account Settings and got a generic "Something went wrong." The current-password field showed no error, meaning `changePassword` (`actions/auth.ts`) passed `signInWithPassword` (current password correct) and failed later at `supabase.auth.updateUser({password})`. First pass added error logging only (`changePassword` had none — `vercel logs` had nothing for the attempt already made, and this session couldn't reproduce it directly: `SUPABASE_SERVICE_ROLE_KEY`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` read as empty strings here, secret redaction) and asked WJ to retry. **Root cause, confirmed on retry via `vercel logs --query "change-password"`:** `code: 'current_password_required'`, `status: 400` — this project has `GOTRUE_SECURITY_UPDATE_PASSWORD_REQUIRE_CURRENT_PASSWORD` enabled, which rejects any `updateUser` password change missing a `current_password` field in that same call, regardless of the separate `signInWithPassword` re-verification `changePassword` already does (GoTrue has no visibility into that earlier call). Confirmed via `@supabase/auth-js`'s own `UserAttributes` type comment. The initial hypothesis (a "secure password change" reauthentication-nonce setting) was wrong — this is a different, simpler GoTrue setting. **Fix:** `changePassword` now passes `current_password: currentPassword` alongside `password: newPassword`. `tsc --noEmit`/`eslint --max-warnings 0` clean, all 91 tests pass (no unit coverage existed for this action; not added here). **Two false alarms during verification, both ruled out:** WJ changed the password successfully (`auth.users.updated_at` confirmed bumped in the DB) but then couldn't sign in — suspected a rate limit at first, since `changePassword`'s internal `signInWithPassword` re-verification consumes a sign-in attempt every try, and `signIn`'s error handling deliberately shows the same generic "incorrect" message for a rate limit as for a real wrong password (AC-FR-04-03, anti-enumeration). Added the same logging to `signIn`; the real error was `invalid_credentials`, ruling out a rate limit. A password reset via "Forgot password" (a separate code path, no `current_password` involved) then confirmed sign-in worked, proving the Auth stack was healthy — the two failed attempts were simply mismatched password values between WJ's own attempts, not a code defect. **Live-verified 2026-07-24, same day:** WJ changed the password again, copy-pasted the exact value from notepad, and signed in successfully first try.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-23 | **Account deletion fixed: `service_role` was missing table grants on the two newest item-graph tables.** WJ tried to delete a test account (`grantpathway+lloyds1@gmail.com`, 2 applications, 20 items, 1 guidelines row, 10 `ai_usage_log` rows — all counted beforehand via `supabase db query --linked`) via the "Permanently delete my account" flow and got "Deletion failed. Please try again." His own hypothesis (clicked delete before typing `DELETE`) was ruled out — the client-side confirmation check runs before any network call and shows a different message, so reaching the server error means confirmation had already passed. `vercel logs --environment production --query "delete-account"` showed the real error: `42501: permission denied for table application_items`, on the very first step of the deletion cascade (`app/api/account/delete/route.ts`) — a recount afterward confirmed nothing had been deleted at all, consistent with failing on step 1. Root cause, confirmed via `information_schema.role_table_grants`: `application_items` and `application_guidelines` (both added 2026-07-14) only ever granted privileges to `authenticated`, never to `service_role` — unlike the four original tables, which carry full `service_role` privileges granted ad hoc outside any tracked migration. Fixed with new migration `20260723000000_grant_service_role_item_graph_tables.sql`, applied to `grant-pathway-dev` and verified. **Not yet applied to `grant-pathway-prod`** — same outstanding gap as the existing schema-drift tracking. Purely additive (a `GRANT`, no data/schema change); no user data was lost or affected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-23 | **Citation-highlight fix: `findQuoteRange` now tolerates a list-bullet glyph, not just whitespace/punctuation.** WJ, continuing a Garfield Weston test application on Step 4, noticed Q1 ("Total annual expenditure")'s citation opened the "view original guidelines" panel with the quote correctly highlighted and scrolled to, but Q9 ("Your finances")'s citation opened the panel with no highlight, landing on Page 1. Traced with `supabase db query --linked` against `grant-pathway-dev`, comparing the stored `guideline_reference.quote` for both items against the retained `application_guidelines.guideline_text`: Q1's quote is a clean unbroken sentence in the source, but Q9's source text is a bulleted list ("We need to understand:\n■ that you have a robust plan to fund your work") and the AI's quote drops the "■" bullet glyph as decorative formatting when quoting across it, leaving a non-whitespace character sitting between two quoted words that `findQuoteRange`'s whitespace-only separator couldn't match through. Fixed by folding a small set of list-bullet glyphs (`■ • ● ▪ ◦ ‣ ·`) into the same separator already used for whitespace between quote words (`lib/guideline-citations.ts`) — same shape of fix as the 2026-07-15 punctuation-tolerance and 2026-07-17 heading-path fixes below. General fix, not scoped to Garfield Weston or budget questions — benefits any funder's guidelines with bulleted list items. No stored citation data needed correcting, only the client-side matching logic. `tsc --noEmit`, `eslint --max-warnings 0`, all 27 tests in `guideline-citations.test.ts` pass (1 new, the exact live case).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-17 | **PDR-AI-009 false-positive investigated: compound questions, no change made.** Continuing Stony Stratford testing, WJ hit the "does not appear to address the question" warning repeatedly on §5 "Application Background" (5 sub-asks bundled into one guidance paragraph) and §8/9 "General Activities of the Group" (3 sub-asks), and asked whether the check needed loosening. Both flagged test answers reused an identical boilerplate opening sentence copy-pasted from a different question's answer -- itself a plausible independent trigger the prompt already names ("boilerplate unrelated to the question") -- so the compound-question structure alone wasn't confirmed as the cause. WJ isolated it with a clean test: §11 "Publicity and Marketing" (single, non-compound question) with a fresh, non-reused, on-topic answer got no warning at all, confirming the check works correctly for ordinary single-ask questions. **WJ's call: no change** -- flagging an answer that only covers some of a multi-part question's asks is a defensible reading of "does not address the question," distinct from the wholly-irrelevant-filler-text case this PDR was built to fix. Recorded in `PDR-AI-009`'s Implementation status section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-17 | **Citation heading_path fix, corrected: assembled from two markers, not one (third attempt, same gap).** With the sync bug above fixed, `vercel logs` gave direct visibility into the actual raw citations the AI returned. Both failing citations shared one real cause, different from either earlier hypothesis: the model constructed a two-element `heading_path` by joining two separate, consecutive `[SECTION: ...]` marker lines that merely _read_ like parent/child in the prose -- but running the real extraction pipeline locally against the actual `.docx` confirmed every marker in this document is flat, single-element, with no genuine nesting anywhere. A heading_path assembled from two flat markers can never match a real one. Fixed: `buildSummaryPrompt()`'s citation rule now explicitly says heading_path must come from one marker line, never assembled from two. Corrected the misleading "Fixed 2026-07-17" doc comment in `lib/prompts.ts`, which had claimed the earlier tick-list/narrative fix resolved this on its own. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged). Not verifiable locally -- WJ's next regeneration is the outstanding live-verification step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-17 | **Session wrap-up: PDR-AI-010 decided and built, live-testing confirmed, one open finding logged.** Reviewed a fresh Stony Stratford export as an end-of-session check -- all 12 items (3 governance facts + 9 narrative sections) present, correctly sequenced, and persisted, confirming the Step 4 sync fix holds across a full assemble/export cycle. WJ: "Job done, it looks much better." **PDR-AI-010 decided and built same day**: `buildSummaryPrompt()`'s "sections" rule now handles a themed financial section whose only content is numeric fields already captured by the governance facts -- still creates the section but reframes its guidance as a "tell us in your own words" invitation and omits its citation rather than duplicating one a governance fact already owns. Chosen over WJ's initially-proposed unconditional catch-all specifically to avoid repeating the anti-pattern `PDR-AI-008`'s original always-on governance block was corrected away from. **New finding, not yet actioned**: the fresh export's item 3 ("Are any of your trustees related...") looks like a hallucinated governance fact -- neither Stony Stratford source document mentions trustee relatedness at all, contradicting `PDR-AI-008`'s extraction rule and `TEST-DASHBOARD.md`'s existing 2-of-5-signal review. WJ: covered by planned formal testing rather than an ad-hoc fix now. **Debug logging kept deliberately** -- WJ wants it retained during ongoing testing but was explicit it must not reach production; added an explicit Go-Live Gate checklist item (`IMPLEMENTATION-PLAN.md` v3.18) rather than relying on it being remembered. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged). PDR-AI-010's prompt change not independently verifiable locally -- a future guideline upload for a wholly-numeric financial section is the outstanding live-verification step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-17 | **Budget fields now also reflect the approved-state tint (extends the Yes/No dropdown fix).** WJ confirmed the dropdown fix, then pointed out the same white-regardless-of-approval problem on every budget-flagged field -- the two £ governance inputs (total expenditure, reserves) and any budget-flagged narrative `Textarea`. The row below had assumed the budget fields' hardcoded `bg-white` was deliberate; WJ's ask makes clear that assumption was wrong -- he wants every field, budget or not, to behave consistently once approved. Fixed: removed the `bg-white` override from both the £-prefixed governance `Input` and the narrative `Textarea`'s budget conditional -- both now fall through to the shared components' own `bg-transparent` default, same as every non-budget field already had. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged). Not verified visually in this environment (same constraint as the dropdown fix) -- WJ's next look at Step 4 is the verification step for all three field types together.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-17 | **Governance Yes/No dropdown now reflects the approved-state tint like every other field.** WJ noticed approved cards turn green throughout Step 4 except the Yes/No `<select>` used for 2 of the 5 governance facts (trustee-relatedness, bank-signatories-relatedness), which stayed flat white regardless of approval -- compared side by side against an ordinary approved `<Textarea>`, which correctly turned green. Root cause: `components/ui/input.tsx`/`textarea.tsx` both default to `bg-transparent`, letting the parent card's tint show through -- that's how every ordinary field already reflects approval. The governance Yes/No field is a plain native `<select>`, not built on either shared component, hardcoded to `bg-white` instead -- an oversight, not a deliberate choice (unlike the budget fields' deliberate always-white background, which keeps manually-entered figures looking distinct). Fixed: changed to `bg-transparent`, matching the existing pattern exactly. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged, no existing coverage of this component). Not verified visually in this environment (needs real approved application data; preview tool unreliable for this project) -- WJ's next look at Step 4 is the verification step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-17 | **Citation badges now truncate long headings.** WJ confirmed item 4's citation fix worked ("brilliant") but flagged item 6's as much worse -- its badge rendered an entire multi-sentence instructional paragraph verbatim ("a) Give details of expenditure required for your project e.g. materials, equipment, professional fees..."), because Stony Stratford's source document applies a Heading style to that whole paragraph rather than a short title. Genuinely valid citation, just an unbounded badge. Fixed: `citationLabel()` (`components/application-step4-draft.tsx`) truncates at a word boundary past 90 characters -- chosen so item 4's own 86-character label, which WJ was happy with, renders completely unchanged. Full text remains available via the badge's `title` attribute and the "view original guidelines" panel. General fix, not specific to this funder. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged, no existing coverage of this component). Verified via a standalone script: item 4 unaffected, item 6 truncates cleanly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-17 | **Step 4 sync silently broken for any funder with both governance facts and narrative items (regression since 2026-07-15).** A `vercel logs` pull (requested to diagnose the citation gap) surfaced something more serious: `[step4] free_form upsert failed: null value in column "added_manually"... violates not-null constraint`. Root cause: `resolveGovernanceInserts()` (`lib/governance-items.ts`) always sets `added_manually` explicitly on governance rows; the narrative section/question insert objects in `step/4/page.tsx` (both free_form and structured branches) never set it at all. Merging both into one `.upsert()` call makes PostgREST fill the missing key with `NULL` for narrative rows rather than the column default, tripping the constraint on every affected row -- the whole upsert fails atomically and silently (server-log only), so Step 4 keeps showing stale data regardless of what's regenerated. Latent since the `added_manually` migration (2026-07-15) -- **any funder with governance signal regenerated since then may have silently failed to sync.** Fixed: `added_manually: false` added explicitly to both narrative insert branches. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged, no existing coverage of this sync code).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-17 | **Per-citation debug logging added -- Stony Stratford gap persists after two fixes.** WJ regenerated Stony Stratford again after the punctuation-tolerance fix (below) and found item 4 still had no citation badge, plus a new gap on item 6 ("Budget for this Project"), which _had_ a citation in the very first test round. Item 6's real marker (`5. BUDGET FOR THIS PROJECT.`) has no apostrophe/dash, so the punctuation fix can't explain its loss -- most likely ordinary run-to-run variance in the model's citation-reporting (temperature is already 0 for this route, but this project has hit temperature-0 non-determinism before, e.g. the 2026-07-15 Step 3 extraction-determinism fix). Rather than propose a third speculative fix, `app/api/generate-summary/route.ts`'s citation-reconciliation loop now logs the raw `citation` object whenever one is offered but fails validation -- previously only visible in aggregate (a >50%-invalid warning a single stray miss never trips). Surfaces the actual heading_path/quote via `vercel logs` without needing a reproducible local Bedrock call. Temporary/diagnostic -- remove once citation reliability is no longer under active investigation. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (unchanged). WJ's next regeneration plus a `vercel logs` pull is the next diagnostic step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-17 | **Citation validation made punctuation-tolerant for heading paths (second fix, same Stony Stratford gap).** WJ regenerated the summary after the tick-list-plus-narrative-follow-up fix (below) and confirmed item 4 still had no citation badge. Ran the real `mammoth`/`tagSectionsFromHtml` extraction pipeline locally against the actual Stony Stratford `.docx` (no Bedrock needed -- pure local library code) and confirmed a real marker exists for this heading, using the source's curly apostrophe in "Council's" -- the only apostrophe anywhere in the document's headings. `validateCitation()` compared heading_path with a byte-exact `Set.has()`, no punctuation tolerance, while its sibling `findQuoteRange()` (quote highlighting) already tolerates exactly this curly-vs-straight variation (found live once before, 2026-07-15, MK Community Foundation) -- a highly plausible explanation for why only this one heading kept failing. Fixed by applying the same `EQUIVALENT_PUNCTUATION_CLASSES` tolerance to heading-path matching in both `extractValidMarkers()` and `validateCitation()` (`lib/guideline-citations.ts`). General fix, not specific to this form -- any funder heading with an apostrophe/curly quote/dash was equally exposed. New regression test reproduces the exact heading and apostrophe variant. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 76 tests pass (75 existing + 1 new). Not verifiable against a real Bedrock call locally -- WJ's next regeneration is the outstanding live-verification step, alongside the earlier fix (both may have been needed together).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-17 | **Citation extraction fixed for tick-list-plus-narrative-follow-up sections.** WJ live-tested Stony Stratford Town Council's guideline form ("a worst case form") and found 10 of 11 extracted items carried a citation badge, but "Alignment with Council's Overarching Principles" -- built from a tick-list ("3.1 tick all that apply") plus a separate narrative follow-up on the same topic ("3.2 Please outline how...") -- had none. `validateCitation()` (`lib/guideline-citations.ts`) only checks that the AI's reported marker exists, never the quote's content, so the missing badge meant the model itself returned `citation: null` -- most likely because the existing citation rule already says never to quote a "formatting instruction" next to a question, and a tick-list intro reads exactly like one. Fixed with a new rule in `buildSummaryPrompt()`'s citation instruction (`lib/prompts.ts`): the narrative follow-up's own wording is the item's own text for citation purposes in this pattern, not an incidental detail to skip. Targets the general pattern, not just this one form. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). Not verifiable against a real Bedrock call locally (`dotenvx` redacts AWS credentials) -- WJ's next guideline upload for this funder is the outstanding live-verification step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-17 | **Export date fixed to one timestamp per application.** WJ live-tested the site footer version fix by checking a real Henry Smith export's `.txt` and `.docx` files, and spotted a 2-minute gap between their "Date:" lines -- `app/api/export/[applicationId]/route.ts` computed `exportDate` as `new Date()` live on every request, so exporting each format separately (or re-downloading either weeks later) showed a fresh timestamp each time. New nullable `applications.first_exported_at` column (migration `20260717000000`, `grant-pathway-dev` only), set once on an application's first export in either format and never overwritten again; the route now reads this column instead. `applications.last_exported_at` (drives the separate re-export warning) is unaffected. `lib/database.types.ts` regenerated -- also required manually restoring a previously-lost type refinement for the `reserve_ai_slot` RPC's `Returns` shape, which the CLI still can't introspect (same limitation as 2026-06-22). `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). New `acceptance-criteria.md` AC-FR-37-03A; `PDR-DH-003` revision history updated; `data-model.md` bumped 1.13 -> 1.14; `technical-design.md` bumped 1.18 -> 1.19. Not yet live-verified against a fresh export -- WJ's next test (export both formats for the same application, confirm identical dates) is the outstanding step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-17 | **Item (1) below built: site footer now shows the app version.** `components/site-footer.tsx` now calls `lib/version.ts`'s `getAppVersion()` (the same helper already used in the export document's footer) and renders "Grant Pathway v\<version\>" under the existing tagline. No new decision needed -- see `CHANGELOG.md` for detail. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged, no existing coverage of this component). **Live-verified by WJ same day** -- screenshot confirmed "Grant Pathway v2026.07.17-9d8d30c" rendering correctly in the deployed footer. Item (2), AI refine relevance-check consistency, remains open -- see the row below; a design conversation with WJ is needed before that one is built.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-17 | **`PDR-AI-009` built same day it was decided: AI refine relevance-check consistency.** `buildRefinePrompt()` (`lib/prompts.ts`) now always attempts its structure/clarity pass regardless of word-limit status, but also judges whether the answer plausibly addresses the question -- if not, it prepends a clearly-flagged warning line (new `REFINE_IRRELEVANT_WARNING` constant) instead of declining outright. One risk found and fixed during implementation: "Use this improved version" previously saved `refinedText` verbatim, so the warning sentence itself could have ended up saved as part of the charity's answer if clicked through without reading -- fixed with a `stripRefineWarning()` helper in `components/application-step4-draft.tsx`, applied both at adoption and before computing `PDR-AI-006`'s over-limit word/character count (which the raw warning text would otherwise have inflated). `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged, no existing coverage of this component). **Live-verified by WJ same day:** retested a real Henry Smith safeguarding question (300-word limit) with the same irrelevant filler text from the original screenshots -- the warning line appeared correctly over the limit (503 words), followed by a suggestion compressed to within 300 words, no shortfall message shown. WJ confirmed the mid-text compression is expected, pre-existing `buildRefinePrompt()` behaviour (unrelated to this fix). WJ: "we can safely say this has passed testing."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-17 | **Item (2) below now designed: `PDR-AI-009` decided.** `buildRefinePrompt()` (`lib/prompts.ts`) has no relevance-check instruction at all, which is why "Help me improve this" declined clearly irrelevant filler text when over its word limit but passed the same content through unchanged when under limit (screenshots supplied 2026-07-16) -- an emergent inconsistency, not a designed safeguard. Presented three options to WJ: always decline outright (needs a new API/UI shape, not a prompt-only fix -- rejected), always polish silently (removes the model's only existing signal, opposite of "tighter" -- rejected), or always polish but prepend a clearly-flagged warning when the content doesn't address the question, regardless of word-limit status (chosen). WJ also confirmed this needs its own PDR, per precedent (`PDR-AI-006`/`007`/`008`) -- `PDR-AI-009` recorded, `PRD-DECISIONS-INDEX.md` updated (21 -> 22 decisions). **Not yet built** -- next step is the prompt change itself in `lib/prompts.ts`, verified against WJ's original two scenarios plus a genuine on-topic answer to check for false positives.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-17 | **Two items agreed last night as "pick up in the morning" -- tracked here so they are not lost.** This project has repeatedly found decided-but-untracked items rediscovered days later (`PDR-AI-006`, `PDR-AI-007`, `DR-FD-001`'s free-text fallback all sat unbuilt 6-12 days, found only when raised again in passing) -- per that precedent, both items below are logged now, before code changes, so today's session starts from documentation rather than chat memory. **(1) Site footer version id.** `components/site-footer.tsx` shows no version at all. `lib/version.ts`'s `getAppVersion()` (built 2026-07-02, format `YYYY.MM.DD-<short SHA>`, sourced from Vercel build-time git metadata) is already wired into the export document's footer only (`app/api/export/[applicationId]/route.ts`). Reusing it in the site footer needs no new decision -- just a small code change. Raised because WJ lost track of which of two similarly-named Henry Smith test applications he was viewing, and wants a way to confirm which deployed build he is testing. **(2) AI refine relevance-check consistency.** `buildRefinePrompt()` (`lib/prompts.ts`) has no relevance-check instruction at all. The "decline to help" behaviour WJ found live during Henry Smith Step 4 testing (screenshots supplied 2026-07-16) is emergent, inconsistent LLM behaviour -- declines on clearly irrelevant filler text when the answer is over its word limit, but passes the same content straight through unchanged once the answer is under limit -- not a designed safeguard. `DR-AI-003`'s mandatory human-review checklist (Option A) remains the only actual safety net against irrelevant AI output. WJ: "A human checklist will not be enough, let's refine the prompt to make it consistent and tighter." **Not yet designed** -- no options or decision recorded yet; next step is to agree the actual approach (and whether it needs its own PDR) before implementing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-16 | **Manually-added governance items no longer skip the assembly gate when blank.** While digesting a live Henry Smith test scenario, WJ ticked several financial/governance facts via the "Add a financial or governance detail" manual picker, left them all blank, and found "Ready to assemble" stayed active throughout, right up to the senior-review gate. Not a bug in the mechanism itself -- `GOVERNANCE_ITEMS`' `item_label` deliberately ends in "(optional)" (`lib/governance-items.ts`) so the existing skippable-when-blank gate treats it like any other optional question -- but that reasoning only fits the AI-auto-detected case: `PDR-AI-008` decided a low-signal AI detection should never become a forced question for a novice user. A manual add is the opposite: the charity actively chose to answer that fact, so leaving it blank afterwards should not silently bypass approval. **Fix:** `components/application-step4-draft.tsx`'s `allApproved` computation, and the matching check that shows the "Approve this answer" panel while an answer is empty, are both now also gated on the existing `addedManually` flag (already used for the "Added by you" badge) -- `!q.addedManually` is required alongside the "(optional)" text check before an empty item is treated as skippable. AI-detected governance facts (with or without a citation) are unaffected. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). New `acceptance-criteria.md` AC-FR-29-08; `PDR-AI-008` revision history updated; `PRD-Grant-Pathway.md` bumped 0.55 -> 0.56.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-16 | **PDR-AI-006 built: refined suggestions still over the limit now name the exact shortfall.** Third gap closed in this same-day cluster (after `DR-FD-001`'s free-text fallback and `PDR-AI-007` itself) -- decided 2026-07-04 alongside `PDR-AI-007`, user-guide half built at the time, but the in-app conditional message (inside the "Suggested improvement" card, shown only when the AI's own suggestion is still over the limit) sat unbuilt for 12 days until raised while discussing why both "Help me improve this" and "Trim to limit" are worth having. Fix: `components/application-step4-draft.tsx` computes the refined suggestion's own word/character count against the limit and shows the decided wording ("This suggestion is still [N] words over the limit...", adapted to characters per `limitType`) directly under the suggested text. **One snag found and fixed during the task:** an inline IIFE version tripped the `react-hooks/refs` lint rule ("Cannot access refs during render") despite touching no ref -- almost certainly a false positive from the experimental React Compiler misattributing an unrelated ref access elsewhere in the render to the IIFE; hoisting the computation to a plain `const` alongside the file's other per-question values resolved it immediately. Verified wording/pluralisation via a standalone script. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). New `acceptance-criteria.md` AC-FR-30-03A; `PRD-Grant-Pathway.md` bumped 0.54 -> 0.55.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-16 | **"Trim to limit" extended to ordinary narrative questions, alongside AI refine.** Follow-on from the same-day PDR-AI-007 build. WJ hit a non-budget narrative question over its limit, clicked "Help me improve this," and the AI correctly declined to help (the test answer was deliberately irrelevant filler text and the refine prompt never invents facts) -- but this left no way forward at all. Combined with `PDR-AI-006`'s already-documented finding that AI refine can undershoot and leave an answer still over limit, narrative questions had two known ways to get stuck with no fallback, unlike budget questions which by this point always had one. WJ agreed to extend the deterministic trim here too, as a secondary option alongside AI refine, not a replacement. No new logic needed -- `trimToLimit()`/`handleTrimToLimit()` were already generic, keyed only on the question's limit type, never on `is_budget_question` -- only a second render site for the existing button. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). New `acceptance-criteria.md` AC-FR-29-07; `PRD-Grant-Pathway.md` bumped 0.53 -> 0.54; `PDR-AI-007` updated with an Extension section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-16 | **PDR-AI-007 built: budget questions over their word/character limit now get a deterministic trim, not silence.** WJ live-testing Henry Smith hit a budget question (300-word limit) with a 503-word test sample and asked how the user is meant to reduce it, since AI assist is disabled for budget questions. Investigation found this had already been decided -- `PDR-AI-007`, decided 2026-07-04, formalised 2026-07-11 -- but never built: no task was ever added to this file or `IMPLEMENTATION-PLAN.md`'s tracked list, so it sat unbuilt for 12 days unnoticed, the same failure mode as `DR-FD-001`'s 2026-07-11 free-text-fallback amendment found stale in the same-day Phase 5 audit. Root cause confirmed live: `components/application-step4-draft.tsx` wrapped the entire "Help me improve this" block (including the over-limit warning) in `{!q.isBudgetQuestion && (...)}`, so budget questions showed no over-limit guidance at all. Built exactly as decided: a budget-specific over-limit message (no AI reference) plus a new `trimToLimit()` helper and "Trim to limit" button -- fully deterministic, no AI/LLM call, snaps to the last complete sentence within the limit, falling back to a hard word/character cut if even the first sentence alone exceeds it. Verified via a standalone reproduction script against WJ's exact 502-word sample (trims to 285 words, clean sentence boundary) plus a character-limit case and a no-sentence-fits hard-cut case. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged, no existing coverage of this component). New `acceptance-criteria.md` AC-FR-29-06; `PRD-Grant-Pathway.md` bumped 0.52 -> 0.53.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-16 | **Numbering extended to free-form funders' sections and governance items.** Follow-on from the same-day Step 3 count-mismatch fix — WJ asked whether Step 4's lack of numbering on Walton Charity (free-form) should be revisited too. Confirmed this was documented, intentional behaviour (`AC-FR-28-04`), but WJ asked for it to be extended anyway: that premise pre-dates PDR-AI-008's governance facts (2026-07-15), and a fact like "Are any of your trustees related to each other...?" reads as a discrete question regardless of the funder's classification. Fix: `components/application-step4-draft.tsx`'s number span no longer gated on `funderType === 'structured'` — every item numbered by list position, both funder types alike; free-form textarea `aria-label` now includes the number too. `actions/applications.ts`'s `assembleAndAdvance()` no longer detects or branches on funder type at all — the whole detection step removed as dead code once both branches produced the same numbered output. `AC-FR-28-04`/`AC-FR-31A-04` corrected with revision notes (decision reversed, not silently rewritten); `PRD-Grant-Pathway.md` bumped 0.51 -> 0.52. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged, no existing coverage). Live verification (needs a real application with saved item data) is WJ's next check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-16 | **Step 3 summary undercounted items whenever governance facts were detected.** WJ live-testing Walton Charity (fictitious test charity) saw Step 3 announce "4 sections to complete" then Step 4 show "0 of 7 sections approved". Root cause: the Step 3 banner (`components/application-step3-summary.tsx`) counted only `summary.sections`/`summary.questions`, never `summary.governanceFacts` — Step 4 renders governance facts as ordinary items in the same list, so its count always included them. Walton's guidelines raised 3 of the 5 governance facts alongside 4 narrative sections (4 vs 7); the same undercount applies to structured funders with detected governance facts too, just not yet hit in testing. Fix: both banner strings (free-form and structured) now add `governanceFacts?.length ?? 0` to their total. `tsc --noEmit`, `eslint --max-warnings 0` clean, all 75 tests pass (unchanged). Can't be verified live locally (needs a real Bedrock call; `dotenvx` redacts AWS credentials for this agent) — next live regeneration with governance facts detected is WJ's verification. **Separately raised, not a bug:** no numbering on Step 4 for this free-form funder is confirmed intentional per `AC-FR-28-04` — flagged back to WJ as a design question worth revisiting now that governance facts can appear inside a free-form funder's list too.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-16 | **Phase 5 audit follow-up: P5.4 migration-push task added, ToS/Privacy/Vercel Pro/cron status corrected.** Second pass on the same-day Phase 5 review. Added an explicit P5.4 task (plus a matching Phase 6 -> Go-Live Gate line) to apply Phase 6's ~10 dev-only migrations to `grant-pathway-prod` before go-live — previously only implied via scattered per-task scoping notes, never an actual checklist item. Corrected P5.1's Terms of Service / Privacy Policy rows, which read as not-yet-drafted despite both being live since 2026-07-10 and 2026-07-02 respectively (genuinely open items — effective date, solicitor review — called out explicitly instead). Corrected P5.4's Vercel Pro / cron-confirmation bullets, which read as pending despite both being active (corroborated independently: the 30-minute `cleanup-guidelines` cron requires Pro, since Hobby only permits once-daily crons). No code changed — planning-document corrections only. `IMPLEMENTATION-PLAN.md` bumped v3.16 -> v3.17.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-16 | **Phase 5 factual audit: Funder Directory section marked superseded, Admin Dashboard "Top funders" query fixed.** WJ asked for a review of `IMPLEMENTATION-PLAN.md`'s Phase 5 section, given a week of Phase 6 changes, ahead of Phase 5 starting next week. Found `P5.FD1–FD6` (funder picker/directory) still marked "✅ Complete" despite the design being fully reversed 2026-07-15 (`DR-FD-001` v1.4) — the Tier 3 decision record was updated at the time but never propagated back into the Tier 2 plan doc. Section re-marked superseded, tasks struck through and kept for audit trail only. Knock-on fix: P5.5b's "Top funders" admin dashboard query joined `applications.funder_id → funders.id`, which no application populates since the picker was removed — changed to group by `applications.funder_name` directly. No code changed — planning-document correction only (P5.5b/admin dashboard is not yet built). `IMPLEMENTATION-PLAN.md` bumped v3.15 → v3.16.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-16 | **Citation quote could highlight an incidental detail (e.g. a word limit) instead of the question itself.** WJ live-testing Lloyds Bank Foundation found the "view original guidelines" panel for a summary question highlighted a nearby "Suggested word count" line instead of the question text. Root cause: the citation prompt instruction (`lib/prompts.ts`) only required a verbatim, findable excerpt from the cited page — not specifically from the question/section's own wording. Fix: tightened the instruction to require the quote come from the question/section's own text, explicitly excluding adjacent word/character limits and formatting notes. Prompt-only change; no code path to unit test and Bedrock output can't be reproduced locally (`dotenvx` redacts real AWS credentials for this agent). `tsc --noEmit`, `eslint --max-warnings 0`, all 75 tests pass (unchanged). Live re-verification is WJ's next Step 3 generation. Files: `lib/prompts.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-16 | **Governance items now get a real sequential number, folded in with narrative questions (Step 4 and Step 5).** WJ reviewed a Clothworkers assembled draft showing "Total annual expenditure (£)" with no number at all, followed by "1. Please describe the community/group of people you support..." as the first narrative question — he expected the governance item to be "1." and the narrative question "2.", not for governance items to be excluded from numbering entirely. This reverses part of PDR-AI-008's original rendering design (governance items as unnumbered "ordinary cards"); confirmed the exact scope (both Step 4 and Step 5; governance items keep their existing sort-first position rather than moving to the end) before changing established, documented behaviour. **Fixed:** both `components/application-step4-draft.tsx` and `actions/applications.ts` (`assembleAndAdvance`) now compute the displayed number from each item's position in the already-ordered, answered list (`index + 1`), not the raw `item_order` column (which stays negative for governance items, sorting-only, never user-facing). Both files' previous number-suppression checks for governance items are removed. Also removed `field_key` from `assembleAndAdvance`'s query — only ever read for the now-removed check. `tsc --noEmit`, `eslint --max-warnings 0`, all 75 tests pass (unchanged — neither file has existing unit coverage, verified live instead, same precedent as the original 2026-07-15 numbering fix). **Files changed:** `components/application-step4-draft.tsx`, `actions/applications.ts`, `PDR-AI-008`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-16 | **Repeated-line stripping fix: genuine questions recurring verbatim across a funder's multiple forms were being deleted, not just truncated away.** Found while verifying the form-aware truncation fix (row below) against the real Clothworkers PDF with no ceiling applied at all — "Please describe the difference you expect your capital project to make" was still missing. WJ called it a serious bug and asked for an immediate fix rather than deferral. **Root cause:** `detectRepeatedLines()` (`lib/preprocess-text.ts`) treated any line appearing 3+ times identically as a PDF running header/footer and stripped it everywhere — Clothworkers repeats this exact question across all 3 of its forms, indistinguishable under the old rule from a genuine repeated header like "THE CLOTHWORKERS' FOUNDATION: OPEN GRANTS PROGRAMME GUIDANCE". **Fixed:** added a marker-adjacency requirement (`isAdjacentToMarker()`) — a line is only stripped if every one of its occurrences sits immediately next to a `[PAGE N]`/`[SECTION: ...]` boundary (a true running header/footer); if even one occurrence is embedded mid-page, none of that line's occurrences are stripped. Verified against the real Clothworkers PDF (question now survives, true header still strips cleanly) and spot-checked the cleaning step against 7 other scheduled-funder PDFs (A B Charitable Trust, Walton Charity, Idlewild, Garfield Weston, Nationwide, Heritage Fund, MK Community Foundation) per ADR-AI-010's cross-funder testing requirement — modest 0-2% reductions, no crashes. `tsc --noEmit`, `eslint --max-warnings 0`, all 75 tests pass (3 new). **Files changed:** `lib/preprocess-text.ts`, `__tests__/preprocess-text.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-16 | **Form-aware truncation fix: large multi-form guideline PDFs (Clothworkers) were losing their actual application questions.** WJ was live-testing Clothworkers' Foundation (Harry's Rainbow) and asked directly how many questions to expect, plus whether the recurring large-document truncation warning could be improved. Investigation confirmed a real, not cosmetic, problem: production's 50,000-char `PREPROCESS_CHAR_CEILING` (ADR-AI-010) truncated in raw document order, cutting off mid-way through the real Sample Small Grants Programme Application Form's most important narrative questions while keeping only front-matter guidance. Presented two fix options (raise the ceiling further vs. detect-and-prioritise the real form section) and let WJ choose before changing a production-wide parameter — he chose form-aware detection. `findFormStartIndex()` (`lib/preprocess-text.ts`) finds a strong "this is the actual sample form" heading (skipping numbered table-of-contents entries so an early listing isn't mistaken for the real thing) and splits the truncation budget so the form section reliably gets the majority share. **A real bug in the first version, found and fixed during verification, not shipped as designed:** it only triggered when the form heading's offset was beyond the ceiling — reproducing against the real 54-page Clothworkers PDF (not a synthetic fixture) showed that earlier header/footer stripping shrinks the document enough that the heading actually lands before the ceiling, so the condition never fired. Corrected to trigger whenever a form heading exists anywhere in a truncated document. Verified against the real PDF before and after the correction (confirmed broken, confirmed fixed) via a local script reproducing `lib/extract-text.ts`'s exact `unpdf` extraction (no Bedrock call needed, so no secrets involved). Client message (`components/application-step3-summary.tsx`) now distinguishes "form found and prioritised" from the old generic warning; API response carries a new `formSectionPrioritized` boolean. **A second, separate bug surfaced but not fixed here:** Clothworkers repeats one question identically across all 3 of its forms; the existing repeated-line header/footer stripping treats any 3+-identical line as noise and deletes it everywhere, including the one copy that's actually needed — confirmed this happens regardless of truncation, flagged to WJ as its own follow-up requiring cross-funder validation per ADR-AI-010's existing testing requirement. `tsc --noEmit`, `eslint --max-warnings 0`, all 72 tests pass (4 new). **Files changed:** `lib/preprocess-text.ts`, `app/api/generate-summary/route.ts`, `components/application-step3-summary.tsx`, `__tests__/preprocess-text.test.ts`, `ADR-AI-010`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-16 | **`generate-summary` fix: `free_form` sections with no stated word limit failed Zod validation on every attempt (Walton Charity eligibility-mismatch test).** WJ hit "We couldn't generate your summary right now" testing Walton Charity — asked whether it was a Claude outage. Real cause found via the Vercel CLI (newly installed and linked this session — `vercel logs --status-code 500`): `stop_reason: end_turn` on both the first attempt and the stricter-prompt retry, ruling out truncation; the actual failure was `[generate-summary] JSON parse/validation failed after retry`. Temporary diagnostic logging (added then removed once root-caused) surfaced the exact Zod error — 4x "Invalid input: expected number, received null". Claude returns `wordLimit: null` for a `free_form` section with no stated limit (same convention as `questions[].wordLimit`), but `aiSummarySectionSchema.wordLimit` was `z.number().optional()` with no `.nullable()`. Since this route runs at `temperature: 0` (2026-07-15 determinism fix), the failure was 100% reproducible on this document — "Try again" could never have worked. **Fixed:** `aiSummarySectionSchema` (`app/api/generate-summary/route.ts`) and the `AiSummarySection` type (`lib/types.ts`) now accept `wordLimit: number \| null`, matching `aiSummaryQuestionSchema`. Downstream consumers already normalised with `?? null`and needed no change. Confirmed fixed by WJ's live retry — summary generated successfully, eligibility-mismatch result rendered as expected.`tsc --noEmit`, `eslint --max-warnings 0`, all 68 tests pass. Vercel CLI is now installed and linked (`rapidglobes-projects/grant-pathway`) for future log investigations. **Files changed:** `app/api/generate-summary/route.ts`, `lib/types.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-15 | **Step 5 assembled-draft numbering fix: governance items no longer show their internal negative `item_order` to the user.** WJ live-tested Sapling Grants (a second MK Community Foundation programme) and predicted the same 3-of-5 governance trigger pattern seen earlier on Oak Grants — confirmed correct. He then flagged the Step 5 "Review and approve" screen showing governance items numbered "-4. Reserves", "-2. How many people are authorised as bank signatories?", "-1. Are any bank signatories related..." — the reserved negative `item_order` used internally to sort these items first was leaking into the user-facing assembled draft. **Root cause:** `assembleAndAdvance()` (`actions/applications.ts`) builds the structured-funder assembled draft as `` `${item_order}. ${item_label}` `` for every answered item — Step 4's rendering already suppresses this prefix for governance items (`!isGovernanceItem`, built during the PDR-AI-008 work), but the Step 5 assembly function, which formats independently from stored `item_order`/`item_label`/`answer_text` rows, was never given the same treatment; the free_form branch was already unaffected since it never prefixes any item with a number. **Fixed:** the query now also selects `field_key`; the numbering condition became `funderType === 'free_form' \|\| r.field_key !== null`— any governance item (identified the same way Step 4 identifies one) renders as a plain label, no number, matching Step 4's display exactly. No test added —`assembleAndAdvance`has no existing unit-test coverage (tightly coupled to the Supabase client, same pattern as other server actions in this file); verified via`tsc --noEmit`and`eslint --max-warnings 0`(both clean) — live re-verification of the Step 5 screen pending WJ. **Files changed:**`actions/applications.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-15 | **Citation-highlight fix: `findQuoteRange` now tolerates typographic punctuation, not just whitespace.** Live-testing PDR-AI-008 against MK Community Foundation's Oak Grants guidelines (WJ asked directly: "will the MKCF guidelines trigger a governance question?") surfaced a real bug: the Reserves item's citation badge linked to "Page 3" correctly, but clicking it opened the viewer with no highlight and no auto-scroll — landing on Page 1 by default. Traced with a direct query against the live `grant-pathway-dev` database (`supabase db query --linked`) comparing the stored `guideline_reference.quote` against the retained `application_guidelines.guideline_text`: the AI's quote read "...six months' free reserves..." (straight apostrophe) while the source PDF actually reads "...six months' free reserves..." (curly apostrophe, U+2019) — a single-character mismatch that defeats `findQuoteRange`'s literal word-for-word match. The two bank-signatory items (sharing a different quote with no smart punctuation) were confirmed working correctly by WJ, isolating this to punctuation variants specifically, not a broader regression. Fixed by matching each occurrence of a straight/curly apostrophe, straight/curly double-quote, or hyphen/en-dash/em-dash in the quote against its whole equivalence class in the source text (`EQUIVALENT_PUNCTUATION_CLASSES`, `lib/guideline-citations.ts`) — the displayed highlight still shows the source's real typesetting, only the matching is punctuation-tolerant. General fix, not governance-specific — benefits any funder's guidelines using proper typographic punctuation. Verified the fix against the real retained MKCF text before and after (script run via `node`, confirmed match found post-fix). `tsc --noEmit`, `eslint --max-warnings 0`, all 68 tests pass (4 new, including the exact live case verbatim).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-15 | **PDR-AI-008's manual-add fallback built (fast-follow, same day) — the zero-signal escape hatch deferred when the auto-detection half was built earlier the same day.** WJ asked for this to be built while stepping away briefly; proceeded directly rather than a blocking plan-mode approval gate, flagging the one real judgement call (a new column to distinguish a manually-added item from an AI-detected one with no citation) so it could be redirected on return. A quiet link — "Need to add something about your finances or governance that wasn't asked above? Add it." — appears below the Step 4 question list only when at least one of the 5 governance facts isn't already shown; hidden entirely once all 5 are present. Clicking it reveals checkboxes for just the missing facts, each with a plain-English explanation (`GOVERNANCE_FIELD_EXPLANATIONS`, `lib/governance-items.ts`) rather than a bare label — matching the earlier design conversation about not asking a novice user (Persona 1, Margaret) to judge relevance from jargon alone. New `application_items.added_manually` boolean column (migration `20260715000001`, `grant-pathway-dev` only) drives an honest "Added by you" badge in place of a citation badge — both otherwise look identical (`guideline_reference: null`). New server action `addManualGovernanceItems` (`actions/applications.ts`), scoped to the closed 5-field vocabulary only (rejects anything outside `GOVERNANCE_FIELD_KEYS`), reusing `resolveGovernanceInserts()` with `added_manually: true`. The existing sync paths' orphan-cleanup was extended so a manually-added item's `item_order` always counts as "in the summary," regardless of what the AI does or doesn't detect — otherwise the very next Step 4 sync would delete an unanswered manually-added item, since it's never part of the AI's own numbering. On success, the client does a hard reload so the server re-syncs and renders the new item at its correct position — consistent with this codebase's existing "mutate then hard-refresh" pattern. `tsc --noEmit`, `eslint --max-warnings 0`, all 64 tests pass (4 new). Migration confirmed applied to `grant-pathway-dev` only (`supabase migration list`, 24/24 matched) and `grant-pathway-prod` confirmed still unlinked (`supabase projects list`). Documentation updated: `PDR-AI-008` (status note), `PRD-DECISIONS-INDEX.md`, `data-model.md` (v1.13), `PRD-Grant-Pathway.md` (v0.51), `acceptance-criteria.md` (new AC-FR-12A-04).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-15 | **PDR-AI-008 built: governance/reserves facts now created only when a funder's guidelines actually raise the topic, superseding the always-on block from earlier the same day.** WJ's live testing of the fixed 5-item "Governance and reserves" block (commit `82e11d9`, then `7e68c9a` for currency formatting) found it disjointed from the rest of Step 4 — no citation, no rationale, shown regardless of relevance. Decision (`PDR-AI-008`) and build, same day: a new `"governanceFacts"` extraction category added to the AI JSON schema (`lib/prompts.ts`), Zod-validated and citation-reconciled in `app/api/generate-summary/route.ts` exactly like `questions`/`sections` — but with a deliberately **lower** trigger bar (a general eligibility/policy statement counts, not just a discrete question), and citation left optional (an entry with `citation: null` is still shown, matching how narrative items already work). `lib/governance-items.ts` gained `resolveGovernanceInserts()` — looks up `item_type`/`item_label`/`item_order`/`is_budget_question` from the fixed `GOVERNANCE_ITEMS` table (never trusted from the AI) for whichever facts were actually detected, deduping by `field_key`. Both sync paths (`setDraftInProgress`, Step 4's fallback sync) now call this conditionally instead of unconditionally upserting all 5. `isOrphanedItem()`'s governance-specific carve-out was removed — a detected fact's `item_order` now folds into the same `summaryOrders` set the narrative sync already uses, so a governance item whose fact is no longer detected is orphan-cleaned exactly like a dropped question (deleted if unanswered, kept if answered). Step 4's dedicated "Governance and reserves" heading was removed (`components/application-step4-draft.tsx`) — a shown item now renders as an ordinary card, with the AI's extracted wording surfacing via the same `guidance` slot free_form sections already use. **Deliberately not built this round:** a manual-add affordance for the zero-signal case — scoped as a separate, lower-priority follow-up per `PDR-AI-008`'s own decision, not an oversight. `tsc --noEmit`, `eslint --max-warnings 0`, all 60 tests pass (5 new, 2 removed for the retired carve-out). No DB migration — `field_key`/`guideline_reference` already existed. Documentation updated: `ADR-DATA-006` (second 2026-07-15 amendment), `PDR-AI-008` (status → built), `PRD-DECISIONS-INDEX.md`, `data-model.md` (v1.12), `PRD-Grant-Pathway.md` (v0.50), `acceptance-criteria.md` (FR-12A rewritten again).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-15 | **DR-FD-001 v1.4 built: curated funder picker/directory removed entirely, free-text field restored.** Live review of Step 1 (WJ) questioned whether the searchable "Who is offering this grant?" picker (introduced 2026-06-01) was still needed. Investigation found the 2026-07-11 amendment relaxing the picker's hard gate to a free-text fallback had never actually been implemented — no corresponding task ever existed in `IMPLEMENTATION-PLAN.md`, only inside the decision record itself. Rather than finally building that fallback, the picker was removed outright: Step 3/4/5 processing is already driven per-application by the uploaded guidelines, not by which funder was selected, so a curated directory no longer serves a purpose. `Who is offering this grant?` is a plain free-text input again. Full dependency sweep found a contained blast radius: `getActiveFunders()`/`FunderOption` deleted (`actions/applications.ts`); `saveApplicationStep1()` dropped its `funderId` parameter; the whole picker/dropdown UI, keyboard nav, and "Request a Funder" mailto link removed from `components/application-step1-form.tsx`; the Step 1 page (`app/(authenticated)/applications/[id]/step/1/page.tsx`) stopped fetching the funder list. One real behaviour change: `getPreviousApplicationForFunder` (P6.5's "start from your last application") previously matched "same funder" by exact `funder_id` equality — with no stable funder identity left, it now matches a trimmed, case-insensitive `funder_name` comparison (ILIKE, with `%`/`_`/`\` escaped so a funder name containing a literal one isn't treated as a wildcard). This is a deliberate soft-miss trade-off, confirmed with WJ: if a charity types a funder's name slightly differently across two applications, the reuse prompt simply won't offer itself — it will never wrongly match two different funders. The `funders` table and `applications.funder_id` are left in place, unused, matching the precedent already set for the dormant `funder_type` column — no drop migration. `tsc --noEmit`, `eslint --max-warnings 0`, all 55 tests pass. Documentation updated: `DR-FD-001` (v1.4), `DECISIONS-INDEX.md`, `PRD-Grant-Pathway.md` (v0.49), `acceptance-criteria.md` (FR-15 rewritten), `moscow-feature-register.md` (v1.17), `data-model.md` (v1.11).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-15 | **PDR-AI-008 decided: governance-facts placement to be reworked, superseding the always-on block built earlier the same day.** WJ's live testing of the fixed 5-item group (previous two rows) found it felt disjointed from the rest of Step 4 — no citation badge, no funder-specific rationale, shown regardless of whether the funder cares. Decision (see `docs/PRD decisions/PDR-AI-008-governance-fact-detection-and-fallback.md`): fold these 5 facts into the same guideline-driven extraction pipeline that produces ordinary narrative questions, so they only appear when a funder's guidelines actually raise them, with a real citation. For funders where the signal is present but imprecise (no one quotable line), show the item anyway rather than requiring a strict citation match — because a manual "you decide if this applies" picker would ask novice users (Persona 1, Margaret — no fundraising training, finds funder language confusing) to make a relevance judgement her persona says she can't reliably make. A manual-add picker stays available as a fallback, but is scoped as a rare, experienced-user shortcut (Persona 2, David) for the residual case where the AI finds no signal at all — never proactively suggested to a novice user. **Not yet built** — this is a decision only; scheduling is pending. Supersedes the "placement only, always show all 5" scope agreed earlier the same day, before live testing surfaced the UX mismatch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-15 | **Governance Step 4 £ fields reformatted to UK currency style.** During his live verification of the governance-items redesign (row below), WJ confirmed the 5 items render correctly, then asked for the two £ fields (total annual expenditure, reserves) to display as "£ n,nnn,nnn" rather than a plain unformatted number input. `components/application-step4-draft.tsx`'s governance number-input branch split in two: the two budget fields now render a text input with a fixed "£" affix and live UK thousands-separator formatting (new `formatThousands()` helper, `toLocaleString('en-GB')`); the bank-signatory count field (not a budget question) is unaffected. Stored `answer_text` stays raw digits — formatting is display-only — so `saveAnswer`, Step 5 export, and the DB column needed no change. `tsc --noEmit`, `eslint --max-warnings 0`, all 55 tests pass. `docs/PRD-Grant-Pathway.md` bumped to v0.48 (Step 4 table row corrected to describe the three number-field input types precisely).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-15 | **Governance/reserves facts re-sited from `charity_profiles` into `application_items` (P6.7 track; migration `20260715000000`, `grant-pathway-dev` only).** Investigation (prompted by WJ noticing the `/profile` governance section) found the 5 fields P6.1 added (2026-07-05) were never actually consumed anywhere — not by Step 3's summary/eligibility logic, not by P6.5's clone — so the "helps flag issues before you apply" copy was untrue. WJ chose Option C (re-site into the item-graph, matching how narrative questions already work) over keeping them at Step 3 or a hybrid design, since the schema already had dormant `number`/`data` item types and a `charity_profile` source_of_truth value that looked purpose-built for this. Scope confirmed: placement only this round (no AI-driven per-funder relevance detection/citation yet — deferred); `/profile`'s governance section and the 5 `charity_profiles` columns retired outright, no backfill (dev/test data); **no seeding between applications, including P6.5 reuse** — WJ was explicit that carrying forward financial/governance figures silently risks stale data, so every application collects these 5 facts fresh, every time. New `application_items.field_key` column (CHECK-constrained to the 5 known values) identifies them robustly; reserved negative `item_order` (-5 to -1) sorts them first without touching existing narrative-item numbering; created via the same dual sync path (`setDraftInProgress` + Step 4's fallback sync) every other item uses, with `answer_text` omitted from the upsert so existing answers are never clobbered on repeat syncs. `answer_text` stores the literal display value as plain text, so `saveAnswer`/`approveAnswer`/Step 5 export needed zero changes — only the Step 4 input widget is type-aware (number input or Yes/No/Not sure yet select instead of a textarea). Cloning (P6.5) explicitly excludes these rows (`field_key IS NOT NULL` filtered out) so reuse never carries them forward either. 9 new Vitest tests added covering the item definitions and the new `isOrphanedItem()` helper (extracted from Step 4's previously-duplicated orphan-cleanup filter, which also had to learn to never treat a governance item as orphaned regardless of the current AI summary's numbering). `tsc --noEmit`, `eslint --max-warnings 0`, all 55 tests pass. Migration applied to `grant-pathway-dev` (confirmed via `supabase migration list` before/after — 22/22 matched, then 23/23); `grant-pathway-prod` remains unlinked and untouched. **Live browser verification pending WJ.** `data-model.md` bumped to v1.10.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-15 | **Step 3 extraction determinism fix live-verified by WJ — confirmed working.** Re-ran "Regenerate summary" three times on MK Community Foundation — Oak Grants after the `temperature: 0` fix shipped: all three runs consistently returned 12 questions. Separately spot-checked against a different funder's guidelines (MK Community Foundation — Sapling Grants): Claude's own manual read of the PDF, applying the same extraction rules as `lib/prompts.ts`, predicted 6 narrative questions; the live app returned exactly 6. **The fix is confirmed working, not just code-reviewed** — see the entry immediately below for the original root cause and fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-15 | **Step 3 extraction non-determinism found and fixed — 12→10 question regression on MK Community Foundation — Oak Grants.** During ad-hoc E2E testing (brand-new test user, fresh guidelines upload from scratch), WJ found the AI extracted only 10 questions instead of the 12 confirmed correct on 2026-07-14 (see that day's P6.3 live-test entry below) — the two funding/fundraising questions were missing again. `git log` confirmed zero commits touching extraction/prompt files between the two dates and a clean working tree; "Regenerate summary" was also confirmed to run through the exact same code path as a first-time upload (same route, same fresh sessionStorage-extracted text — GAP-10's post-generation sessionStorage clear rules out any stale-cache explanation). **Root cause:** the Bedrock call in `app/api/generate-summary/route.ts` set no `temperature` parameter, leaving the model at its non-zero API default — the same guideline text can legitimately produce different extraction results, including different question counts, across separate calls. Compounding this, `lib/prompts.ts`'s extraction rules forbade merging questions _across_ multiple forms but said nothing about merging two adjacent, related-but-distinct questions _within_ one form — exactly the funding-secured/fundraising-plans pair that went missing. **Fixed:** `temperature: 0` pinned on the Step 3 summary Bedrock call only (deliberately scoped — `/api/refine-answer`'s rewording task is a different, more variation-tolerant use case and was left unchanged); the prompt gained an explicit "do not merge adjacent questions within a form" rule alongside the existing cross-form rule. Not scoped to this one funder — this was a reliability gap in extraction generally. `tsc --noEmit` and `eslint --max-warnings 0` both clean; no schema change. **Live repeat-run verification (re-running "Regenerate summary" 2–3 times, confirming a consistently correct question count) still pending WJ** — same pattern as this project's other AI-behaviour changes, since no real Bedrock call can be made from this local environment. `ADR-AI-004` gained a matching dated note.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-14 | **P6.6 (Transparency Status) retired — will not be built.** Discussing what a redesigned P6.6 would even signal, now that P6.5's pivot removed its "approved playbook" basis, surfaced that the task's entire reason for existing was to distinguish curator-reviewed funders from unreviewed ones. With no curator role built or planned, every funder is now in the identical state (live AI extraction, never human-vetted) — a badge that would read the same for 100% of funders isn't a feature, it's decoration. Candidate replacement signals were considered (extraction/citation quality per funder, structured-vs-free-text guideline parsing) and rejected in favour of simply retiring the task rather than inventing a substitute just to keep it alive; WJ confirmed. **Effect on the Go-Live Gate:** now requires P6.1–P6.5 complete (was P6.1–P6.6) — P6.6 removed as a launch-blocking requirement, not deferred. Phase 6 task count changes from 8 to 7. `IMPLEMENTATION-PLAN.md` bumped to v3.15; `ADR-DATA-006` gained a 2026-07-14 (later same day) amendment; `ADR-TRACEABILITY.md` bumped to v2.12; `docs/v1-out-of-scope.md`, `docs/constraints-and-assumptions.md`, and `docs/BRD plus decisions Mark Two/build-plan-any-guideline-or-form.md` updated to match.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-14 | **P6.5 re-tested live by WJ — confirmed working, two UI tweaks made.** Tested against MK Community Foundation — Oak Grants: the "Start fresh" / "Start from your last application" choice appeared correctly with the source grant name and update date, Step 2 was correctly skipped straight to the cloned Step 3 summary, and Step 4 showed the citation badge plus "Carried over — please review" badge on the carried-over answer. Two adjustments requested: (1) the page citation badge now reads "Page N of the guidelines" instead of just "Page N" (`citationLabel()`); (2) the "Carried over — please review" badge is now bold and larger (`text-[13px] font-bold`, up from `text-[11px] font-medium`) to better signal it needs attention. `tsc --noEmit`, `eslint --max-warnings 0`, all 46 tests pass. **P6.5's first milestone is now fully live-verified, not just automated-checks-passed.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-14 | **P6.5 built — private, per-charity, per-funder reuse, not the originally-designed shared "playbook."** During the design walkthrough (this doc's task description, unchanged since 2026-07-05, still described a versioned, curator-approved playbook shared across all charities applying to a funder), WJ directly challenged that premise: why shouldn't a charity applicant be their own curator? Working through it surfaced the real justification for a _shared_ playbook — reliability for every future applicant, not just whoever curated it first — and that a charity reviewing their own past work has no reason to catch mistakes that don't affect them personally, which a shared record would then silently trust for every unrelated applicant afterwards. The feature actually built instead: Step 1 offers a charity starting a new application "Start fresh" or "Start from your last application to [Funder]" (only shown when they have a prior application to that same funder that reached Step 4); choosing reuse carries across the question list (including their own previous answers, reset to "needs review"), retained guidelines, and AI summary, entirely within their own account — no cross-charity sharing, no curator role, no versioning, no new table (`application_items.cloned_from_application_id`, migration `20260714000002`, is the only schema change). Step 2 is skipped; the application lands on Step 3 with the carried-over summary already in place. **Two follow-on effects, not silently absorbed:** (1) P6.6 (transparency status) was designed to derive its signal from "the approved playbook" — that basis no longer exists; P6.6 now needs a new design decision before it can start, tracked as an open item, not glossed over. (2) The rubric-criteria table (originally a P6.5 field) has no home under the new design — moved to **P6.7** as an explicit named task (not folded into P6.7's open-ended "ongoing" framing) specifically so it isn't lost, per WJ's concern about exactly that. `tsc --noEmit`, `eslint --max-warnings 0`, all 46 Vitest tests pass; a direct SQL round-trip against `grant-pathway-dev` confirmed the clone mechanics (answer text carried across, `is_approved` reset, and — critically — deleting the source application does not delete or corrupt the clone, only nulls the `cloned_from_application_id` marker). **Live browser verification pending WJ.** `ADR-DATA-006`/`ADR-DATA-007` amended the same day with the full design history; `BRD-Grant-Pathway.md` (v0.51), `PRD-Grant-Pathway.md` (v0.46), `acceptance-criteria.md`, `technical-design.md` (v1.16), `data-model.md` (v1.9), and `ADR-TRACEABILITY.md` (v2.11) all updated in the same pass. `IMPLEMENTATION-PLAN.md` bumped to v3.15.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-14 | **P6.4 re-tested by WJ after the quote-highlighting fix — confirmed working.** Reopened question 1's citation ("Page 5") — the panel now correctly scrolls to and highlights "Explain the need for your project..." exactly where it wraps across the PDF's line break. Also confirmed on question 11 (the new budget question found during `P6.3`'s live test) — its citation ("Have you secured additional funding...") highlights correctly further down the same page. **P6.4's first milestone is now fully live-verified, not just automated-checks-passed.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-14 | **P6.4 quote-highlighting bug found live and fixed.** WJ live-tested P6.4 within minutes of it shipping: the citation badge and "view original guidelines" panel both opened correctly, but the panel showed the top of page 1 instead of jumping to and highlighting the cited quote on page 5. **Root cause:** the retained text preserves the PDF's own line-wraps, so a sentence wrapping mid-line becomes `"...project\nwill address..."` in the stored text while the AI's quote has an ordinary space there (`"...project will address..."`) — the exact-substring match silently failed and fell into its "show unhighlighted" fallback far more often than intended (the fallback itself worked as designed; it was just triggering on real, valid citations, not just edge cases). **Fixed:** new `findQuoteRange()` (`lib/guideline-citations.ts`) matches the quote's words in order joined by `\s+`, tolerating any whitespace difference between source and quote. Moved out of the component into the shared citation-logic module so it's unit-testable (this codebase has no DOM-testing setup). 6 new Vitest tests added, including one reproducing the exact reported bug verbatim; `tsc --noEmit`, `eslint --max-warnings 0`, all 46 tests pass. Full detail in `CHANGELOG.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-14 | **P6.4 built (first milestone) — Step 4 shows citations and a "view original guidelines" panel.** Each question card now shows a small clickable badge (e.g. "Page 5" or a heading trail) when it carries a valid `guideline_reference` (`P6.3`) — clicking it opens a dialog rendering the retained text (`application_guidelines`, GAP-33) with the cited quote highlighted and auto-scrolled into view. **Design correction made before coding, not after:** `ADR-SEC-004`, `ADR-DATA-007`, and `ADR-OPS-006` all assumed this viewer would fetch the raw PDF and render it to a `<canvas>` (pdf.js-style) — that assumption predates GAP-33's fix and was already known-stale (flagged in GAP-33's own entry above) once retention turned out to be text-only. All three ADRs corrected 2026-07-14 to describe a plain text panel instead: `ADR-SEC-004`'s `worker-src 'self' blob:` CSP consequence removed entirely (no rendering library, no web worker, no CSP change needed); `ADR-DATA-007`'s viewer description corrected from canvas-rendering to text-panel; `ADR-OPS-006`'s three-item accessibility checklist reduced to two (the canvas screen-reader item no longer applies — a text panel is natively accessible). **Implementation:** `app/(authenticated)/applications/[id]/step/4/page.tsx` now selects `guideline_reference` (previously written but never read back) and fetches `application_guidelines.guideline_text` for the application; `components/application-step4-draft.tsx` gained the citation badge, a single reused `Dialog` (Base UI, matching the existing `components/ui/dialog.tsx` primitive already used for the senior-review and timeout-warning modals — no new UI library), and a `GuidelineTextPanel` helper that finds the cited quote in the retained text and highlights it, degrading gracefully to "shown but not highlighted" if the quote isn't found verbatim (the citation was validated against real page/section markers in `P6.3`, not a verbatim-substring guarantee). **Scope confirmed with WJ before coding:** "walk the graph respecting visibility conditions" is a no-op this pass — no item type or branching condition exists yet to walk; building that logic now would be speculative. `tsc --noEmit`, `eslint --max-warnings 0`, all 40 Vitest tests pass. **Live browser verification pending WJ** — same pattern as `P6.2a`/`P6.3`. `IMPLEMENTATION-PLAN.md` bumped to v3.14; Go-Live Gate checklist updated: P6.4 checked off.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-14 | **P6.3 first milestone live-verified against the real MK Community Foundation — Oak Grants PDF.** WJ reopened the test application, re-supplied the guidelines, and regenerated the Step 3 summary (~5 seconds — Bedrock call including citation validation). Verified via direct SQL query: `application_guidelines` now holds the real, page-tagged text (12,512 chars, `[PAGE N]` markers present); `applications.ai_summary` gained 12 questions, all citation-validated (page numbers cross-checked against real markers). Cross-checked all 12 against the actual PDF (`MK Comm oak-grants-criteria-final-nov-2025.pdf`, supplied by WJ): questions 1–10 matched the test plan's existing verified baseline (recorded 2026-07-03) word-for-word; questions 11–12 ("Have you secured additional funding..." / "overview of fundraising plans...") are genuine, verbatim page-5 questions the AI newly picked up — both correctly flagged as budget questions, both citing real page 5 content, and both filling a gap the original test-plan baseline had explicitly flagged as missing ("no dedicated match-funding question found"). **Conclusion: not a regression — a correct improvement over the previous extraction**, confirming P6.3's citation validation (`lib/guideline-citations.ts`) works correctly end to end against a real Bedrock call, which local automated tests alone could not prove (no real AWS credentials in this environment). **Follow-up agreed with WJ, not yet started:** the MK Community Foundation test plan and the user guide will need refreshing to reflect the corrected 12-question baseline — tracked here as a pending item, not yet actioned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-14 | **GAP-33 fixed — guideline-text retention built (`application_guidelines`, migration `20260714000001`).** New table stores `guideline_text` (the marker-tagged text sent to the AI, `textForPrompt` post-preprocessing — validated against for citations in `P6.3`) per application, upserted in `/api/generate-summary` alongside the existing `ai_summary` save, refreshed on every regeneration. Not the raw uploaded file, which `ADR-DATA-002`/`ADR-FILE-001` never retain. RLS (select/insert/update/delete restricted to own rows, hardened `(select auth.uid())` form) and cascade-delete/upsert-refresh behaviour verified by a direct SQL round-trip against a real test application (insert → confirm `created_at` fixed while `updated_at` bumps on re-upsert → clean up) — chosen over a live Bedrock call, since this environment has no real AWS credentials configured. `app/api/account/delete/route.ts` updated to delete this table explicitly alongside `application_items`, matching that route's existing explicit-deletion convention (the FK's own `on delete cascade` would also cover it, but consistency with the documented pattern was judged worth the extra step). **New GAP-34 registered** (Low priority, not yet fixed): Step 2/3's "guidelines are not saved" UI copy is now stale, since retention exists server-side (invisibly — nothing surfaces it to a user yet, that's `P6.4`); wording fix deliberately left out of this pass to keep GAP-33's fix narrowly scoped. `tsc --noEmit`, `eslint --max-warnings 0`, all 40 Vitest tests pass. `P6.4` is now unblocked. `IMPLEMENTATION-PLAN.md` bumped to v3.13; `ADR-TRACEABILITY.md` to v2.9; `docs/data-model.md` to v1.8; `technical-design.md` to v1.14.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-14 | **GAP-33 found while scoping P6.4 — guideline-text retention was never actually built.** Before writing any P6.4 code, checked what the "view original guidelines" panel would actually render — and found nothing: the raw guideline file is deleted from Storage immediately after Step 2 extraction (`app/api/upload/process/route.ts`, `ADR-FILE-001`), and the extracted text only ever lives in `sessionStorage`, cleared the moment the AI summary saves (`lib/guidelines-session.ts`, `ADR-FILE-004`). Neither is retained in Postgres. `ADR-DATA-002`'s 2026-07-10 reversal decided guideline text _should_ be retained for this exact reason, and `ADR-TRACEABILITY.md` carried a task pointer for it — "P6.2a (groundwork) → P6.2 (storage)" — but that pointer was written 2026-07-10, before P6.2's real task bullets existed. When P6.2's actual spec was written (2026-07-13) and built (2026-07-14), it only covered the item-graph/citation-shape half; the "store the text" half was never added to any task, anywhere, by anyone. **This is a planning-process gap, not a defect in P6.2's own build** — P6.2 was built correctly and completely against what it actually promised, and WJ confirmed this understanding directly. A related documentation inconsistency also surfaced: `ADR-SEC-004`'s consequence note for the P6.4 viewer assumes it can "fetch the file as bytes" from Storage, which contradicts `ADR-DATA-002`'s "no raw file ever stored" decision — left uncorrected for now, since the right wording depends on how GAP-33 is actually resolved (fixing it before that would risk correcting it twice). Registered as **⚠️ GAP-33** in `ADR-TRACEABILITY.md` (v2.8) with two adjacent rows' stale "until P6.2a/P6.2 ship" wording corrected alongside it. **P6.4 is now blocked on deciding how to plug this gap** — a design conversation with WJ is in progress before any P6.4 code is written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-14 | **P6.3 built (first milestone) — extraction now records citations, not just questions.** Implements `ADR-DATA-007` Option B's third part: extraction cites a specific chunk of `P6.2a`'s tagged text rather than free-typing a page number. Scope confirmed with WJ before coding: questions/sections only this pass (the ones feeding `application_items`), not the Step 3 summary bullets (`aboutGrant`/`whoCanApply`/`lookingFor`/`keyRequirements`) — those have no database column to store a citation in yet and nothing displays any citation regardless (that's `P6.4`); adding storage for them now would be work ahead of need. **`lib/prompts.ts`:** `buildSummaryPrompt`'s JSON schema gained an optional `citation` field per question/section (`source_type`, `page_number`, `heading_path`, `quote`, matching the shape agreed 2026-07-13), with an explicit instruction to omit it entirely rather than guess if no marker applies. **`lib/guideline-citations.ts` (new):** a citation is never trusted purely on the AI's word — `extractValidMarkers()` reads the real `[PAGE N]`/`[SECTION: ...]` markers out of the text actually sent to the AI, and `validateCitation()` cross-checks the AI's reported citation against them, returning null if it doesn't check out (wrong page number, non-existent heading path, or an empty quote). Confirmed with WJ: an invalid citation is silently dropped (the question/section itself is unaffected), not treated as a reason to reject/retry the whole AI response — and if over half of what the AI offered for a given document turns out invalid, a warning is logged (visible in error-monitoring, never shown to the user) as a signal worth investigating, without blocking anyone. `toGuidelineReferenceColumn()` converts a validated citation into the exact JSONB shape the `P6.2` migration's `guideline_reference` CHECK constraint requires — critically, the unused key (`page_number` or `heading_path`) must be an **absent object key**, not a `null` value, since the constraint tests key presence via the `?` operator. **`app/api/generate-summary/route.ts`:** citations are reconciled once, after either the first-attempt or retry-attempt JSON parse succeeds, using the same `textForPrompt` (post-truncation) the AI actually saw. **Both `application_items` write points** (`actions/applications.ts`'s `setDraftInProgress`, and Step 4's page-load sync fallback) now pass the validated citation into the `guideline_reference` column. No other field or item-type changes — `item_type`/`source_of_truth` stay hardcoded `'narrative'`/`'user_input'` exactly as in `P6.2`, matching this milestone's "same item types as today, new shape only" scope. 11 new Vitest tests added (`__tests__/guideline-citations.test.ts`) covering marker extraction, valid/invalid page and heading citations (including the hallucination-guard case of a citation pointing at a real-looking but non-existent page/heading), empty-quote rejection, and the key-omission behaviour of `toGuidelineReferenceColumn`. `tsc --noEmit`, `eslint --max-warnings 0` (40/40 tests pass), and `prettier --check` all clean. **Live verification limitation:** this local environment has no real AWS credentials configured (`.env.local`'s `AWS_ACCESS_KEY_ID` is an empty placeholder) — a real Bedrock call against the actual MK Community Foundation guideline PDF could not be made from here, so "no information loss versus the current prompt" and citation accuracy against real content are confirmed only by code review and unit tests, not an end-to-end run. **Live browser verification against the real PDF, through Step 2/3, is still pending WJ** — same pattern as `P6.2a`. `IMPLEMENTATION-PLAN.md` bumped to v3.12; Go-Live Gate checklist updated: P6.3 checked off (pending WJ's live-test confirmation, noted inline).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-14 | **P6.2a built — guideline extraction now tags structure instead of discarding it.** Implements `ADR-DATA-007` Option B ("the text itself carries verifiable structure, e.g. `[PAGE 3]` markers") ahead of `P6.3` actually consuming it. **`lib/extract-text.ts`:** PDF extraction switched from `unpdf`'s `mergePages: true` (all pages flattened, page boundaries lost) to per-page extraction, joined with `[PAGE N]` markers between pages. Docx extraction switched from `mammoth.extractRawText` (headingless) to `mammoth.convertToHtml`, walking Word's `<h1>`–`<h6>` heading tags with a nesting stack to emit `[SECTION: A > B]` markers (docx has no fixed pages, so headings are the fallback reference unit, per the P6.2a task spec). **`lib/preprocess-text.ts`:** the existing page-number/repeated-line/boilerplate stripping steps updated to never touch a `[PAGE N]`/`[SECTION: ...]` marker line. Pasted guidelines (no file, so no marker source) get the same `[SECTION: ...]` tagging via a new heuristic reusing the existing numbered ("1. Title"/"2.3 Section") and ALL-CAPS heading detection — nesting tracked by numbering depth, ALL-CAPS treated as top-level; skipped entirely if the text already carries markers from `extract-text.ts`. Explicitly a heuristic guess for pasted text, not a guarantee — text with no heading-like lines gets no markers, an accepted limitation. **ADR consequence gap found and closed:** `ADR-DATA-007`'s consequences section names an `ADR-AI-007` follow-on — `preprocessText`'s character-ceiling truncation must become marker-aware — that was not listed as one of `IMPLEMENTATION-PLAN.md`'s three P6.2a bullets; added as a fourth step per AGENTS.md's mandatory pre-task ADR consequences check. Truncation now snaps back to the last complete marker before the ceiling (dropping a would-be-cut-off page/section in its entirety) rather than the last newline (which could leave a page/section half-populated with no citation to anchor to); the ceiling _value_ itself (20,000 chars dev / 50,000 prod) is unchanged — confirmed with WJ this task fixes only where the cut lands, not how much is cut. Design walked through with WJ item-by-item in plain language before coding (jargon-free re-explanation requested); all decisions confirmed, no open questions. Nothing consumes the new markers yet — extraction result shape (`ExtractionResult.text: string`) and the `sessionStorage`/API contract are unchanged, keeping this groundwork-only per the task's own framing; `P6.3` is where the extraction prompt starts citing them. 7 new Vitest tests added (`__tests__/preprocess-text.test.ts`) covering marker protection, pasted-text nesting (including the ALL-CAPS reset case), no-double-tagging of already-marked text, and marker-aware truncation (including the no-marker-in-range fallback). `tsc --noEmit`, `eslint --max-warnings 0` (29/29 tests pass), and `prettier --check` all clean on every file this task touched. Live browser testing (uploading a real PDF/docx through Step 2) not performed by Claude — per this project's established convention, that's WJ's live-testing pass, not a Claude Preview substitute. `IMPLEMENTATION-PLAN.md` bumped to v3.11; Go-Live Gate checklist updated: P6.2a checked off.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-14 | **P6.2 full end-to-end write path live-tested by WJ.** Beyond the re-open test recorded in the entry below, WJ exercised the remaining write path on the same test application: edited and saved an answer, approved all 10 items individually and the whole application, passed the senior-review gate, assembled the draft, and exported to Word — the output document correctly reflected the edit and all 10 numbered Q&A pairs. Closes out P6.2's exit criterion in full ("proven end to end — extraction, storage, Step 4 rendering, export — with zero regression against the test funder"), not just the read/re-open path covered same day. No code or schema changes; verification only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-14 | **P6.2 built — `application_answers` replaced with `application_items` (item-graph model, compatibility mode; migration `20260714000000`, `grant-pathway-dev` only).** New table created carrying the full typed-item shape from `ADR-DATA-006`/`ADR-DATA-007`: `item_type` (10-value enum, only `narrative` populated today), `item_label`/`item_order` (renamed from `question_text`/`question_order`), `visibility_condition`, `source_of_truth`, `validation_mode`, `rubric_criterion_link` (no FK yet — target table is P6.5), `decision_maker_visible`, `output_mode` (CHECK-constrained to `generic_export` only — `native_template_fill` stays permanently unreachable at the DB level, not just by convention, per the ADR-DATA-006 2026-07-11 amendment), `guideline_reference` (ADR-DATA-007's discriminated union, CHECK-enforced on the JSON keys rather than trusted by convention). All 169 existing rows copied across as `item_type = 'narrative'` with zero information loss, verified by direct query against the MK Community Foundation — Oak Grants test application ("Community Mental Health Drop-In Programme 2026–27 - Retest 040726", 10 items, labels/order/approval/answer-source all intact) before the old table was dropped. `funders.funder_type` also dropped in the same migration (`ADR-DATA-006` consequence 5, DR-FD-001 formally superseded). `approve_application`/`reopen_application` RPCs repointed at the new table. Seven application code files (`actions/applications.ts`, both Step 4/5 pages, export/refine-answer/account-delete/cron-inactivity-deletion routes) and `supabase/seed.sql` updated in the same change — `tsc --noEmit`, `eslint --max-warnings 0`, and all 22 Vitest tests pass clean. Three design calls made with WJ before writing SQL: rename `question_text`/`question_order` (no code-diff cost since the table is new either way); hard-CHECK `output_mode` rather than rely on application-code discipline; create-new-table-and-drop-old rather than in-place `ALTER TABLE` rename (cleaner rollback path, dev-only so no data-preservation risk). `docs/data-model.md` updated to v1.7. This was the last of three settled prerequisites (v3.7–v3.9) — see the three entries below. **Live-tested by WJ same day:** re-opened the application in the browser — all 10 items loaded with original labels/answers and `is_approved` correctly reset to `false` on every row via the repointed `reopen_application` RPC. This caught a documentation error in this entry's original draft, which had misquoted the item count as 13 — a conflation with the unrelated "13/13 test cases" figure from this funder's `TEST-DASHBOARD.md` test plan. Corrected here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-13 | **P6.2 environment scoping confirmed: `grant-pathway-dev` only.** Same convention as P6.1 — `grant-pathway-prod` remains untouched and unlinked, only re-entered at P5.4. Worth confirming explicitly rather than assuming: this migration is far larger than P6.1's five nullable columns, since it supersedes `application_answers` entirely, so the blast radius if the assumption were wrong would be much bigger. This was the last of three open prerequisites for starting P6.2 build — test funder (Oak Grants, v3.7) and the citation-field shape (v3.8) were the other two. All three are now settled and P6.2 is ready to start. `IMPLEMENTATION-PLAN.md` bumped to v3.9.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-13 | **P6.2's guideline-reference field shape agreed with WJ.** The task list previously said "page number or section/heading" without a concrete data shape — a real gap, since an ambiguous shape here would force a schema rewrite once P6.2's migration was already written. Presented as a visual mockup (two worked examples — a PDF page-anchored citation and a docx heading-anchored citation, each shown as it would appear in the app, plus the underlying data shape) rather than raw JSON, so WJ could approve or request changes without reading schema code. Approved as proposed. **Shape:** a discriminated union — `source_type: 'page' \| 'heading'`, `page_number` (page citations only), `heading_path: string[]` (heading citations only, an array to preserve nesting — e.g. `['Eligibility', 'Who can refer a family']`), `quote` (short verbatim excerpt, always present — what the P6.4 viewer highlights, and a second check against a hallucinated citation). Recorded in full in `ADR-DATA-007`; P6.2 and P6.2a task wording updated to reference it directly. WJ noted live testing will be the real confirmation once built — this is the design going in, not a claim it's already proven. `IMPLEMENTATION-PLAN.md` bumped to v3.8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-13 | **P6.2's test funder chosen: MK Community Foundation — Oak Grants.** Needed to prove the item-graph migration "end to end... with zero regression," which requires a funder whose current baseline is already trustworthy. It's the only funder at 🟢 (`docs/Test Plans/TEST-DASHBOARD.md`) — fully re-verified against the current schema, 13/13 test cases, 2026-07-04 — every other funder is 🔁 (passed but predates the 2026-07-01 schema fix) or 🟡 (untested), which would mean re-verifying that funder first just to establish a baseline before P6.2 work could even begin. Checked its test plan for entanglement with the parked Group Profile Score (R16) — none found, so migrating it stays within compatibility-mode scope. `IMPLEMENTATION-PLAN.md` bumped to v3.7.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-13 | **Phase 6 plan audit found and fixed a P6.6/FR-46 wording conflict, plus three uncovered ADR consequences.** A full cross-check of the Phase 6 task list (`IMPLEMENTATION-PLAN.md`) against the BRD, PRD, `acceptance-criteria.md`, `technical-design.md`, and `technology-stack.md` found that P6.6 (Transparency Status) specified a support-status field "per funder/playbook," surfaced in the Step 1 picker — the same funder-identity-scoped premise that FR-46 (three-tier coverage badge, see 2026-07-11 entry below) was withdrawn for, six days after `ADR-DATA-006` first specified P6.6. Root cause: the FR-46 withdrawal was reasoned entirely within `moscow-feature-register.md`/`v1-out-of-scope.md` without a cross-check against the live Phase 6 task list for the same concept — a gap AGENTS.md's Tier 2 rule for `moscow-feature-register.md` did not previously require. **Fixed:** P6.6 reworded to scope status to the approved _playbook_ (pinned to a specific curated guideline version), not raw funder identity; `ADR-DATA-006` and `v1-out-of-scope.md` both updated with matching reconciliation notes. Also fixed while auditing: P6.2 gained an explicit bullet to drop `funders.funder_type` (`ADR-DATA-006` consequence 5, previously an undocumented task gap) and to name the `scoring criterion` item type as R16's resolution mechanism; P6.5 gained an explicit `funder_note` field (R17's disclosure mechanism). `IMPLEMENTATION-PLAN.md` bumped to v3.6. AGENTS.md's Tier 2 rule for `moscow-feature-register.md` updated to require this cross-check going forward, so this failure mode does not recur.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-13 | **`acceptance-criteria.md` full section-by-section review completed.** All 11 sections reviewed against current implementation; committed as `2a7c96c`. See `docs/PRD inputs/acceptance-criteria.md` document history for the itemised list of corrections.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-13 | **`docs/PRD inputs/screen-requirements.md` retired — merged into `PRD-Grant-Pathway.md` Section 7.** The file was ~95% stale duplicate of already-corrected PRD content, plus one genuine gap (the `/verify-email/confirm` intermediate screen), which was added to PRD Section 7 and verified against `components/confirm-email-form.tsx`. The file was then deleted (`git rm`). All other docs referencing it as a live document — including two entries in the archived Notes log (2026-07-10 and 2026-07-01, see [IMPLEMENTATION-STATUS-ARCHIVE.md](IMPLEMENTATION-STATUS-ARCHIVE.md)) — now point to a deleted file; those entries are left as historical record but readers should treat `screen-requirements.md` citations anywhere in either document as referring to `PRD-Grant-Pathway.md` Section 7 instead. `AGENTS.md` Tier 1 table updated accordingly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-11 | **FR-46 (three-tier funder coverage model) withdrawn — see `moscow-feature-register.md` v1.16.** Never built since being added 2026-05-29. Withdrawn rather than merely deferred: the model assumed coverage level is a stable property of a funder, which was disproven — the same funder can be fully or partially supported depending on which specific guidelines document is uploaded for a given application, not the funder's identity. Charities are no worse off — no such display has ever existed. See BD-07 (BRD Section 10) and `docs/v1-out-of-scope.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-10 | **ADR-DATA-002 formally reversed — funder guidelines will be retained, not discarded.** The original "never store" decision (2026-04-17) rested on the Context's claim that funder guidelines "may contain commercially sensitive information provided by the funder." That claim was checked against the real document corpus Grant Pathway processes (`docs/Grant Org Guidelines/` — 23 real funder documents across 14 funders [corrected from an initial miscount of 21, caught during a later same-batch verification pass — see `ADR-DATA-002`]) and found unsupported: these are funders' own publicly published application guidance, not confidential material. New decision: extracted, page/section-tagged guideline text will be retained in Postgres — for the life of the owning application (cascade-deletes per `ADR-DATA-003`, same as `application_answers`), or indefinitely when the text backs an approved playbook (P6.5), independent of any user's lifecycle. No calendar-based expiry job is introduced — both a 24-hour and a 7-day window were considered and rejected, since grant applications routinely take longer than either to complete. Raw guideline files (PDF/Word) are still never stored in Supabase Storage — only extracted text, and only in Postgres. **This is a documentation-level reversal only: no code has changed today** — the app still uses `sessionStorage` and discards guidelines exactly as before, and continues to do so until the Phase 6 work below (P6.2a groundwork, then P6.2/P6.3/P6.5) is actually built; no new phase or task numbers were added, only the underlying rationale for that already-tracked work changed. Dependent ADRs updated the same day with matching notes: `ADR-DATA-003` (cascade rule extended to two future Phase 6 tables), `ADR-ARCH-004` and `ADR-FILE-004` (forward-pointers — their `sessionStorage`/"can't be stored" descriptions remain accurate until P6.2a/P6.3 ship), `ADR-FILE-001` (raw-file handling in `guidelines-temp` unaffected), `ADR-FILE-003`/`ADR-AI-007`/`ADR-AI-010` (page-marker extraction consequences for the coming citation feature), `ADR-SEC-004`/`ADR-OPS-006` (future CSP/accessibility consequences of the P6.4 guideline viewer). `technical-design.md` bumped to v1.9 with matching reversal/forward-reference notes. **Still outstanding, not yet actioned:** `FR-22` and its three acceptance criteria (`AC-FR-22-01/02/03`) assert the opposite of this decision and need rewording; the Privacy Policy's "commercially sensitive, therefore discarded" framing (already flagged for P5.1) needs updating to reflect retention instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-10 | **Guideline source-reference feature blended into Phase 6 (planning only, no code yet).** Followed from a design discussion on adding page/section citations to the Phase 3 (AI Summary) and Phase 4 (Draft Answers) screens, so users can cross-reference the AI output against their own copy of the guidelines — discussed as a standalone "Option 2" design (chunk-anchored citations + a clickable "view original guidelines" panel, versus a simpler free-text label, versus a full retained-document viewer). Investigation found it would touch the exact same data model (`application_answers` → item-graph), extraction prompt (`lib/prompts.ts`), and Step 4 rendering that Phase 6 (P6.2–P6.5) is already rewriting — building it as a separate track first would mean rebuilding it against the new shape shortly after. WJ decided to fold it in as one blended plan rather than run two: added as a new groundwork task **P6.2a** (page-tagged PDF extraction, independent, can start immediately) plus new bullets under **P6.2** (guideline reference field on each item), **P6.3** (extraction records the citation), **P6.4** (reference display + viewer panel), and **P6.5** (human curator confirms/corrects the reference once per funder rather than trusting a fresh AI guess per application). `IMPLEMENTATION-PLAN.md` bumped to v3.2. No ADR or PDR written yet for the citation mechanism itself — worth adding one when P6.2/P6.3 build actually starts. **Formalised same day, later:** `PDR-DH-004` (this design decision) and `ADR-DATA-007` (the technical mechanism, FR-48 in `moscow-feature-register.md`) now exist — see those documents.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-05 | **P6.1 complete — Profile schema extension, dev only.** Migration `20260705000000_p6_1_charity_governance_and_reserves.sql` adds five nullable columns to `charity_profiles`: `total_expenditure`, `reserves` (new — feeds the derived ratio), `trustees_related`, `bank_signatory_count`, `bank_signatories_related`. Scoped minimally per WJ's decision: only what R13 needs, not the rest of the documented-but-never-built "thick profile" fields in `data-model.md` §2.2–2.5 (address/contact, remaining financial fields, supporting-doc booleans) — that gap pre-dates this task and is left untouched. `actions/charity.ts` and `charity-profile-form.tsx` extended; all five fields optional, existing profiles unaffected. Months-of-reserve-cover ratio (`reserves ÷ (total_expenditure ÷ 12)`) computed client-side and surfaced live during profile setup once both figures are entered, satisfying the "at least one derived ratio" exit criterion. Applied only to `grant-pathway-dev` (confirmed via `supabase migration list` before and after — 18/18 matched, then 19/19); `grant-pathway-prod` untouched and remains unlinked, consistent with prod only being re-entered at P5.4. `lib/database.types.ts` updated by hand rather than regenerated wholesale — the installed CLI (2.109.0) reshapes unrelated RPC return types (`reserve_ai_slot` etc.) to generic `Json`, which would have been an out-of-scope regression across every AI-usage call site. Verified: `tsc --noEmit` clean, `eslint --max-warnings 0` clean, all 24 Vitest tests pass, `next build` succeeds. **Browser-verified by WJ same day** — screenshot of the live `/profile` screen confirmed the new "Governance and reserves" section renders correctly below the existing fields, all five fields clearly marked optional, save button unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-05 | **Revised same day: Phase 6 now gates launch.** WJ decided launching on the current flat model, with known gaps (R1–R20, in particular non-narrative fields being silently invisible to the user), risks the "trusted partner" objective more than a later launch does — there is no commercial deadline forcing 31 July 2026. Added a **Phase 6 → Go-Live Gate** to `IMPLEMENTATION-PLAN.md`, immediately before P5.6 (DNS and Go-Live): requires P5.1–P5.5 and P6.1–P6.6 complete. P6.7 (ongoing funder-by-funder extension) remains excepted — no completion state, does not block. Target launch changed from 31 July 2026 to "not committed, working estimate August–September 2026" everywhere it was recorded. Direction: work starts on P6.1 immediately (tonight or 2026-07-06).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-05 | **Phase 6 — Application Item-Graph Rearchitecture added (P6.1–P6.7).** Expands `docs/BRD plus decisions Mark Two/build-plan-any-guideline-or-form.md` into this document's and `IMPLEMENTATION-PLAN.md`'s tracked task format, per ADR-DATA-006 (see note below). Numbering maps the build plan's own Phase 0–6 onto P6.1–P6.7 (shifted by one to avoid a confusing "P6.0" and a phase literally named "6" containing a sub-item also named "6"). Explicitly a parallel track — does not gate Phase 5 or the 31 July launch; sequencing driven by capacity and ongoing funder testing, not a fixed timeline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-05 | **ADR-DATA-006 decided — Application Item-Graph Model.** Not a task from the current plan; a forward-looking architectural decision for a rearchitecture that has not been built yet. Follows a nine-funder review (`docs/BRD plus decisions Mark Two/question-coverage-analysis.md`) finding the flat, narrative-only `application_answers` model (ADR-DATA-001) false in twenty distinct ways. Decision: a typed item-graph data model, populated via AI-drafted, human-reviewed playbooks per funder, with visible per-funder support-status transparency (`docs/BRD plus decisions Mark Two/clean-slate-design-proposal.md`). Supersedes `application_answers` (ADR-DATA-001, in part) and formally retires the unused `funders.funder_type` column (DR-FD-001). Phased build plan in `docs/BRD plus decisions Mark Two/build-plan-any-guideline-or-form.md` — does not gate or change anything currently in production; Phase 1 has not started.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

For the full Phase 0–5 (and earlier Phase 6-track) Notes-log entries, see [IMPLEMENTATION-STATUS-ARCHIVE.md](IMPLEMENTATION-STATUS-ARCHIVE.md).

---

## Phase 6 — Application Item-Graph Rearchitecture

**Decided in `ADR-DATA-006-application-item-graph-model.md` (2026-07-05). Revised the same day: this phase now gates launch** — P6.1–P6.6 must be complete before go-live (P5.6); P6.7 is excepted, ongoing by design. There is no commercial deadline forcing the original 31 July 2026 date, and WJ judged the trust cost of launching with known gaps (R1–R20) higher than the cost of waiting. Full detail in `docs/BRD plus decisions Mark Two/build-plan-any-guideline-or-form.md`; this section tracks completion only, matching this document's usual format.

**Blended 2026-07-10:** the Phase 3/4 guideline source-reference feature (page/section citation per summary bullet and question, previously discussed as a standalone "Option 2" design) is folded into P6.2a and the added bullets below rather than run as a separate track — see note below.

- [x] **P6.1** Profile schema extension: governance facts (trustee relatedness, bank-signatory count/relatedness) and derived-metric inputs (e.g. reserves ÷ monthly expenditure) added to `charity_profiles`; at least one derived ratio surfaced during profile setup — complete 2026-07-05, dev only, see note below
- [x] **P6.2a** Guideline page/section reference extraction (groundwork): PDF page boundaries preserved during extraction (currently flattened); pre-processing no longer strips page-number markers; docx/pasted guidelines fall back to heading/section structure. Independent of P6.1 and P6.2 — can start immediately. **Complete 2026-07-14** — see Notes below.
- [x] **P6.2** Item-graph data model (compatibility mode): typed item schema designed, including a guideline reference field per item — shape agreed with WJ 2026-07-13 (`source_type: 'page' | 'heading'`, `page_number`, `heading_path: string[]`, `quote`; see `ADR-DATA-007`), from P6.2a; `application_answers` migrated into the new shape using only today's item types; proven end to end against **MK Community Foundation — Oak Grants** (chosen 2026-07-13 — only funder currently 🟢 against the current schema, no R16 entanglement) with zero regression; drops the unused `funders.funder_type` column (`ADR-DATA-006` consequence 5); `scoring criterion` item type is the R16 resolution mechanism (visible, non-fillable). **Complete 2026-07-14, `grant-pathway-dev` only** — see Notes below.
  - ⚠️ **Highest-risk task in Phase 6** — nothing in P6.3–P6.7 can meaningfully start before this is proven.
  - ✅ **Complete 2026-07-14.** All three build prerequisites (test funder v3.7, citation-field shape v3.8, dev-only scoping v3.9) settled 2026-07-13; build itself completed the following day.
  - ✅ **Full end-to-end path live-tested by WJ, 2026-07-14:** re-open (data intact, `is_approved` correctly reset), edit/save, approve all, senior-review gate, assemble, and Word export — all reading/writing `application_items` correctly. See Notes below.
- [x] **P6.3** Extraction rewrite: `lib/prompts.ts` and the extraction route rewritten to produce the graph shape, compatibility-mode first, then extended item type by item type. Also records which guideline page/section each item was drawn from, citing a specific chunk of the page-tagged text rather than free-typing a page number. **First milestone complete 2026-07-14** — see Notes below.
- [x] **P6.4** Step 4 rendering rework: rendering walks the graph and respects visibility conditions (branching), compatibility-mode first, then extended item type by item type. Shows each item's guideline reference and adds a "view original guidelines" panel with jump-to-source. **First milestone complete 2026-07-14** — see Notes below.
- [x] ~~**P6.5** Playbook infrastructure and curation workflow: versioned, human-reviewed playbook store built; runtime prefers an approved playbook over live extraction. Human review also confirms or corrects each item's guideline reference before approval. Playbook carries a `funder_note` field (R17 disclosure mechanism).~~ **P6.5** Reuse previous application (private, per-charity): complete 2026-07-14, supersedes the playbook design above — see note below.
- ~~[ ] **P6.6** Transparency status: support status visible in the Step 1 picker and Step 3 summary, scoped to the approved **playbook**, not funder identity (reconciled 2026-07-13 with the FR-46 withdrawal — see `ADR-DATA-006`)~~ **Retired 2026-07-14, will not be built** — see note below.
- [ ] **P6.7** Funder-by-funder capability extension (ongoing): no fixed completion point — tracked here as "in progress" once P6.1–P6.5 land and funder-specific work begins. **Note (2026-07-24):** the _testing_ side of this (previously one plan per named funder) is superseded by `DR-TEST-001`'s capability/guideline-shape structure — this task label is retained as-is since it describes ongoing product capability extension, not the test suite's own organisation.

**Parked, not scheduled under Phase 6:** native-document output (its own design problem); scoring criteria driven by cross-application funder history (blocked on a BD-06 reversal decision, not an engineering task).

- [ ] **Phase 6 → Go-Live Gate:** P5.1–P5.5, P6.2a, and P6.1–P6.5 all complete (P6.6 retired 2026-07-14, no longer a gate requirement); ADR-DATA-006 and this gate reviewed for any amendment needed in light of what was actually built. **Signed off:** _pending_
